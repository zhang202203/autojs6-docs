# AutoJs6 知识库 · 导航

> 官方文档 [https://docs.autojs6.com](https://docs.autojs6.com) 的完整本地化编译版
建立于 2026-09-22 | 原文 131 份 / 2.0 MB | API 条目 2444 条
应用项目: [https://github.com/SuperMonster003/AutoJs6](https://github.com/SuperMonster003/AutoJs6)

## 这个知识库怎么用

写脚本时的典型路径是 **「速查表找 API → 点进官方文档核对」**。

### 第一步：找 API — 查速查表

**[→ 全量 API 速查表（2444 条）](https://www.workbuddy.cn/space/d/0iKbcGhO9icHJqm4XNKzix)**

按模块列出全部签名，**含所有重载**，例如：

```
| `colors.toInt(color)` | 6.2.0 | 将颜色参数转换为 颜色整数. |
| `automator.click(x, y)` | — | 点击指定屏幕坐标. |
| `automator.click(text, index?)` | — | 查找文本包含 text 的控件并点击. |
```

- **版本列** = 该 API 所需的最低 AutoJs6 版本；`—` 表示官方未标注
- 每条都带官方文档链接，可一键跳转核对

### 第二步：核对 — 查官方原文

速查表里的摘要只是第一句话。**涉及参数个数与顺序、返回值类型、默认值时，
务必点进官方文档原文确认**。原文入口：

**[→ 文档总目录（131 页）](https://www.workbuddy.cn/space/d/0U5pYjbRBGwQ45rb5n9QJE)**

目录按官方 9 大分组组织，每条附官方摘要和 API 数量。

### 第三步：按场景学 — 主题页

| 主题 | 覆盖内容 |
| --- | --- |
| [入门：运行环境与脚本基础](https://www.workbuddy.cn/space/d/SIq6XFGz5cDi1iHbcVtlWC) | Rhino 引擎 / ES 支持范围 / CommonJS / 全局对象清单 |
| [UI 自动化：选择器 / 控件 / 无障碍](https://www.workbuddy.cn/space/d/c7WOEuFdTejJfoAEt1igkU) | 无障碍服务 / 选择器链式调用 / 阻塞风险 / 坐标与 Root |
| [脚本引擎：线程 / 引擎 / 事件](https://www.workbuddy.cn/space/d/Jd1vrVz5uIdiSFC0P3bQ2A) | threads / engines 执行配置 / 死锁警告 |
| [图像 · 颜色 · 视觉识别](https://www.workbuddy.cn/space/d/hC6GjFgbaaKNgyYyspauyX) | 截图权限 / 找色阈值 / colors / OCR / YOLO |
| [文件 · 存储 · 网络 · 数据库](https://www.workbuddy.cn/space/d/9qRRAtI96nEgyo75pQv5sg) | files / storages / http / sqlite / zip |

## 高频陷阱速记

| 陷阱 | 说明 |
| --- | --- |
| `untilFindOne()` / `untilFind()` | **会永久阻塞**，等价于 `findOne(-1)`，其后代码可能永不执行 |
| 坐标点击 | 受 `setScreenMetrics(w, h)` 缩放影响，注意是否调用过 |
| `images.requestScreenCapture()` | 同步方法**不能在 UI 线程调用**，UI 模式用 `...Async()` |
| 找色 `threshold` | 默认 `4`；与 `similarity` **互斥**，不可同时传 |
| `http` 的 `insecure` | 自 6.8.0 起**仅作用于当前请求**，不会继承 |
| `threads` 死锁 | `join()` / `blockedGet()` 会阻塞调用线程，勿在 UI 线程等待 UI 任务 |
| `threads.start()` | 脚本退出时返回 `undefined`，需判空 |
| `base64` vs `crypto` | `base64` 是编码不是加密，保密用 `crypto` |
| ES6 支持 | 仅**部分**支持（ES5 全支持），`Promise` / `class` 等需实测 |

## 已知注意点

- 官方「使用手册」章节标记为**未完成**，内容为空，入门请以综述与答疑替代。
- 官方有专门的「存疑内容」页，记录无法从源码还原的 API，行为不符时优先查它。
- 部分模块（automator / app / dialogs / events / files / floaty）官方文档仍在补充中。

## 原文获取与离线检索

上面每篇文档中的链接都指向官方在线文档（docs.autojs6.com），点开即为 100% 原文，核对时以它为准。另外，本机工作区保留了一份完整离线副本（131 个 md 文件，2.0 MB），位于 autojs6-wiki/raw/，可直接全文检索：

```bash
grep -rn "requestScreenCapture" raw/     # 找某个 API
grep -rln "无障碍服务" raw/                # 找某个概念在哪些模块出现
```

## 更新

官方文档会持续更新。需要同步时重新抓取并重新生成速查表即可（本地 `_tools/` 下已备好脚本）。
