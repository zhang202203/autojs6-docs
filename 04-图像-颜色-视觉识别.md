# 图像 · 颜色 · 视觉识别

> **类型**: topic
**创建时间**: 2026-09-22
**来源**: [📄 image](https://docs.autojs6.com/#/image), [📄 color](https://docs.autojs6.com/#/color), [📄 canvas](https://docs.autojs6.com/#/canvas), [📄 ocr](https://docs.autojs6.com/#/ocr), [📄 yolo](https://docs.autojs6.com/#/yolo), [📄 barcode](https://docs.autojs6.com/#/barcode), [📄 qrcode](https://docs.autojs6.com/#/qrcode)

## 摘要

AutoJs6 的视觉能力分四层: **取图** (截图 / 读文件) → **处理** (OpenCV / 图像处理) →
**识别** (找色 / 找图 / OCR / 条码 / YOLO) → **绘制** (Canvas).
核心模块为 `images`, 颜色工具为 `colors`.

## 一、images 模块结构

官方文档 [📄 image](https://docs.autojs6.com/#/image) 的章节划分 (可作为查文档的地图):

| 章节 | 内容 |
| --- | --- |
| OpenCV 与区域 | OpenCV 初始化与区域构建 |
| 读取与编解码 | 图像读取 / 解码 |
| 保存与压缩 | 写出与压缩 |
| 图像处理 | 裁剪 / 缩放 / 旋转 等 |
| 截图 | 屏幕捕获权限与截图 |
| 找色 | 单点与区域找色 |
| 找图与特征匹配 | 模板匹配 |
| 相似度 | 图像相似度计算 |
| 资源状态 | 回收与状态 |
| 类型 | 相关类型定义 |

## 二、截图权限

```js
images.requestScreenCapture(options?)
```

- 同步方法, **请求屏幕捕获权限并等待用户选择**, 返回是否获得权限.
- 默认选项: `orientation = "auto"`, `width = -1`, `height = -1`, `isAsync = false`.
- `orientation` 支持 `auto` / `none` / `portrait` / `landscape` 或对应整数 `0` / `-1` / `1` / `2`.
- `isAsync` 控制截图器是否持续异步产生帧, `async` 是其兼容别名.

> ⚠️ **此同步方法不能在 UI 线程调用.**
UI 模式应使用 `images.requestScreenCaptureAsync()`.
来源: [📄 image](https://docs.autojs6.com/#/image)

## 三、找色

找色选项中的 `threshold` **默认为 `4`**, 表示颜色通道差异阈值.
也可使用 `similarity`, 此时阈值按 `round(255 * (1 - similarity))` 换算.
**同一选项对象不能同时包含 `threshold` 和 `similarity`**.

```js
images.detectColor(image, color, x, y, threshold?, algorithm?)
// image: ImageWrapper | string
// color: ColorInt | ColorHex | ColorName
// threshold = 4
// algorithm = "diff"
// returns: 指定位置是否匹配颜色
```

> `images.detectsColor(...)` 已标记 **`DEPRECATED`**, 新脚本请用 `detectColor`.
来源: [📄 image](https://docs.autojs6.com/#/image)

## 四、colors 模块

`colors` 用于颜色模式转换、色彩空间转换、颜色分量合成与分解 ([📄 color](https://docs.autojs6.com/#/color)):

| API | 版本 | 说明 |
| --- | --- | --- |
| `colors.toInt(color)` | 6.2.0 | 转为颜色整数 ColorInt |
| `colors.toHex(color)` | — | 转为颜色代码 ColorHex |
| `colors.toHex(color, alpha)` | — | 根据 `alpha` 决定是否显示 A 分量 |
| `colors.toHex(color, length)` | — | 根据 `length` 决定显示长度 |
| `colors.toFullHex(color)` | 6.2.0 | 强制转为完整形式 `#AARRGGBB` |
| `colors.build(...)` | — | 构建 `Color` 实例, 等价于 `new Color(...)` |

## 五、其他视觉模块

| 模块 | 用途 | 原文 |
| --- | --- | --- |
| `canvas` | `Canvas` 全局构造函数, 画布绘制 | [📄 canvas](https://docs.autojs6.com/#/canvas) |
| `ocr` | 识别图像中的文本 | [📄 ocr](https://docs.autojs6.com/#/ocr) |
| `yolo` | 通过 YOLO Provider 插件加载 NCNN 模型做目标检测 | [📄 yolo](https://docs.autojs6.com/#/yolo) |
| `barcode` | 从图片或当前屏幕同步识别一维码与二维码 | [📄 barcode](https://docs.autojs6.com/#/barcode) |
| `qrcode` | 二维码相关 | [📄 qrcode](https://docs.autojs6.com/#/qrcode) |

> 注: `yolo` 依赖**独立插件**, 使用前需确认插件已安装.

## 核对要点

## 关联

- 相关概念: \[\[topic-getting-started\]\], \[\[topic-ui-automation\]\]
- 参见: 《文档总目录》, 《API 速查表》

## 引用来源

- \[1\] [📄 image](https://docs.autojs6.com/#/image) — 模块章节、截图权限、找色阈值与 detectColor 签名
- \[2\] [📄 color](https://docs.autojs6.com/#/color) — colors 转换 API
- \[3\] [📄 canvas](https://docs.autojs6.com/#/canvas) — Canvas 构造函数
- \[4\] [📄 ocr](https://docs.autojs6.com/#/ocr) / \[5\] [📄 yolo](https://docs.autojs6.com/#/yolo) / \[6\] [📄 barcode](https://docs.autojs6.com/#/barcode) — 识别类模块

## 变更记录

- 2026-09-22: 初始创建, 来源 [📄 image](https://docs.autojs6.com/#/image), [📄 color](https://docs.autojs6.com/#/color)
