# AutoJs6 全量 API 速查表

> 由脚本从 `raw/` 原文自动提取, 共 2444 条. **版本列** 表示该 API 所需的最低 AutoJs6 版本.
摘要为原文首句截取, 完整签名与参数说明请点进对应原文页面核对.

## 目录

- **模块 - 核心功能**
  - [人工智能 (AI)](#ai) `13`
  - [通用应用 (App)](#app) `41`
  - [AutoJs6 本体应用](#autojs) `12`
  - [自动化 (Automator)](#automator) `126`
  - [条码 (Barcode)](#barcode) `37`
  - [Base64](#base64) `2`
  - [画布 (Canvas)](#canvas) `100`
  - [颜色 (Color)](#color) `99`
  - [控制台 (Console)](#console) `64`
  - [续体 (Continuation)](#continuation) `1`
  - [单位转换 (Converter)](#cvt) `5`
  - [密文 (Crypto)](#crypto) `10`
  - [设备 (Device)](#device) `63`
  - [对话框 (Dialogs)](#dialogs) `15`
  - [引擎 (Engines)](#engines) `25`
  - [事件监听 (Events)](#events) `28`
  - [文件 (Files)](#files) `39`
  - [悬浮窗 (Floaty)](#floaty) `18`
  - [流程 (Flow)](#flow) `49`
  - [格式化 (Formatter)](#fmt) `5`
  - [全局对象 (Global)](#global) `108`
  - [HTTP](#http) `20`
  - [图像 (Images)](#image) `92`
  - [国际化 (Internationalization)](#i18n) `11`
  - [JavaScript 对象扩展 (Jsox)](#jsox) `3`
  - [按键 (Keys)](#keys) `22`
  - [邮件 (Mail)](#mail) `17`
  - [多媒体 (Media)](#media) `11`
  - [媒体信息 (MediaInfo)](#mediainfo) `15`
  - [MIME](#mime) `8`
  - [模块 (Module)](#modules) `3`
  - [Nano ID](#nanoid) `1`
  - [消息通知 (Notice)](#notice) `14`
  - [光学字符识别 (OCR)](#ocr) `24`
  - [OpenCC](#opencc) `34`
  - [文本间距 (Pangu)](#pangu) `2`
  - [拼音 (Pinyin)](#pinyin) `7`
  - [Pinyin4j](#pinyin4j) `4`
  - [插件 (Plugins)](#plugins) `7`
  - [电源管理 (PowerManager)](#powerManager) `2`
  - [二维码 (QR Code)](#qrcode) `16`
  - [记录器 (Recorder)](#recorder) `16`
  - [传感器 (Sensors)](#sensors) `5`
  - [应用设置 (Settings)](#settings) `2`
  - [Shell](#shell) `15`
  - [Shizuku](#shizuku) `8`
  - [SQLite](#sqlite) `74`
  - [标准化 (Standardization)](#s13n) `5`
  - [存储 (Storages)](#storages) `5`
  - [系统属性 (Sysprops)](#sysprops) `6`
  - [任务 (Tasks)](#tasks) `20`
  - [线程 (Threads)](#threads) `31`
  - [定时器 (Timers)](#timers) `19`
  - [消息浮动框 (Toast)](#toast) `5`
  - [文本转语音 (TTS)](#tts) `29`
  - [用户界面 (UI)](#ui) `44`
  - [工具 (Util)](#util) `53`
  - [万维网 (Web)](#web) `4`
  - [计划任务兼容接口 (WorkManager)](#workManager) `11`
  - [目标检测 (YOLO)](#yolo) `1`
  - [压缩文件 (Zip)](#zip) `24`
- **类型 - 主要对象**
  - [选择器 (UiSelector)](#uiSelectorType) `237`
  - [控件节点 (UiObject)](#uiObjectType) `147`
  - [控件集合 (UiObjectCollection)](#uiObjectCollectionType) `67`
  - [控件节点行为 (UiObjectActions)](#uiObjectActionsType) `44`
  - [流程对象 (Flow)](#flowType) `23`
  - [邮件客户端 (MailClient)](#mailClientType) `55`
  - [邮件消息 (MailMessage)](#mailMessageType) `5`
  - [包装图像类 (ImageWrapper)](#imageWrapperType) `9`
  - [WebSocket](#webSocketType) `13`
  - [事件发射器 (EventEmitter)](#eventEmitterType) `17`
  - [应用枚举类 (App)](#appType) `12`
  - [Color (颜色类)](#colorType) `82`
  - [版本工具类 (Version)](#versionType) `14`
  - [代码填泥 (Polyfill)](#polyfill) `1`
  - [Arrayx (Array 扩展)](#arrayx) `15`
  - [Numberx (Number 扩展)](#numberx) `15`
  - [Mathx (Math 扩展)](#mathx) `33`
- **运行时与环境**
  - [异常 (Exceptions)](#exceptions) `4`
  - [运行时 (Runtime)](#runtime) `40`
- **数据类型与类型定义**
  - [Storage (存储类)](#storageType) `10`
  - [AndroidRect](#androidRectType) `12`
  - [CryptoKey](#cryptoKeyType) `1`
  - [CryptoKeyPair](#cryptoKeyPairType) `1`
  - [HttpResponseBody](#httpResponseBodyType) `7`
  - [InjectableWebClient](#injectableWebClientType) `2`
  - [InjectableWebView](#injectableWebViewType) `1`
  - [MailAccountOptions](#mailAccountOptionsType) `2`
  - [MailSearchQuery](#mailSearchQueryType) `1`
  - [NoticeBuilder](#noticeBuilderType) `15`
  - [Okhttp3Request](#okhttp3RequestType) `2`
  - [OpenCVPoint](#opencvPointType) `3`
  - [OpenCVSize](#opencvSizeType) `4`

## 模块 - 核心功能

### 人工智能 (AI) — [原文](https://docs.autojs6.com/#/ai)

| API | 版本 | 说明 |
| --- | --- | --- |
| `ai.ai(input, options?)` | — | 发起文本生成请求. 此调用与 [ai.ask(input, options?)](#m-ask) 等价. |
| `ai.ask(input, options?)` | — | 发起非流式请求, 并仅返回响应中的文本结果. 结构化输出仍以完整 JSON 文本字符串兑现. |
| `ai.chat(input, options?)` | — | 发起非流式请求, 并返回包含精确目标, 用量, 完成原因和推理文本的完整响应. |
| `ai.stream(input, options?)` | — | 发起流式请求并立即返回事件对象. `open`, `delta`, `chunk`, `usage` 和 `done` 分别提供目标元数据, 增量, 用量与完整响应. |
| `ai.session(options?)` | — | 规划并打开一个插件目标的多轮会话. 不传选项或传入 `null` 时选择官方 3-Stone AI 插件声明的默认目标. |
| `ai.catalog(options?)` | — | 读取一个 AI Provider 插件公开的完整目标目录. 不传选项或传入 `null` 时选择官方 3-Stone AI 插件. 此方法只读取目录, 不创建生成会话, 也不接受 `target` 或生成控制选项. |
| `ai.AiSession#ask(prompt)` | — | 在保留既有 Conversation 上下文的同时发起一个非流式轮次, 并仅返回文本结果. 结构化会话返回完整 JSON 文本字符串. 此方法不接受消息数组或按轮次生成选项. |
| `ai.AiSession#chat(prompt)` | — | 在保留既有 Conversation 上下文的同时发起一个非流式轮次, 并返回完整响应. |
| `ai.AiSession#stream(prompt)` | — | 在保留既有 Conversation 上下文的同时发起一个流式轮次. 事件形状与普通流式请求相同. |
| `ai.AiSession#close()` | 6.8.0 | 关闭会话, 取消活动轮次并释放插件 Conversation, Binder 连接和目标资源. 重复调用不会产生额外效果. |
| `ai.AiStream#on(eventName, listener)` | 6.8.0 | 注册持续事件监听器. |
| `ai.AiStream#once(eventName, listener)` | 6.8.0 | 注册仅执行一次的事件监听器. |
| `ai.AiStream#cancel()` | 6.8.0 | 取消当前请求并终止流. 重复调用不会再次取消. 成功接受取消操作后触发 `cancelled` 事件. |

### 通用应用 (App) — [原文](https://docs.autojs6.com/#/app)

| API | 版本 | 说明 |
| --- | --- | --- |
| `app.launchDual(app)` | — | 在非当前用户配置文件中启动应用. 此方法等同于 [app.launchDualPackage](#m-launchdualpackage). |
| `app.launchDualPackage(app)` | — | 按包名在非当前用户配置文件中启动应用. 预设别名和应用枚举会先转换为对应包名. |
| `app.launchDualApp(app)` | — | 按应用显示名在非当前用户配置文件中启动应用. 预设别名和应用枚举优先使用对应包名启动. |
| `app.getAppByAlias(alias)` | 6.2.0 | 按大小写敏感的完整别名查找内置 [App](appType) 枚举实例. 未找到匹配项时返回 `null`. |
| `app.isInstalled(name)` | — | 解析应用标识并检查对应包是否已安装. 无法解析应用标识时返回 `false`. |
| `app.isDualInstalled(name)` | — | 解析应用标识, 并通过 `cmd package list packages --user` 检查双开应用. 此方法需要可用的 Shizuku 或 Root 权限. 无法解析应用标识, 命令执行失败或应用未安装时返回 `false`. |
| `app.kill(name)` | — | 解析应用标识并使用 `am force-stop` 强制停止对应包. 执行时依次选择可用的 Shizuku, Root Shell 或普通 Shell. |
| `app.killDual(name)` | — | 解析应用标识, 并在非当前用户配置文件中执行 `am force-stop`. 此方法需要可用的 Shizuku 或 Root 权限. |
| `app.launchAppDetailsSettings(app)` | — | 打开应用详情设置页. `null` 或 `undefined` 输入返回 `false`. |
| `app.launchSettings(app)` | — | [app.launchAppDetailsSettings](#m-launchappdetailssettings) 的兼容别名. |
| `app.openAppSetting(app)` | — | [app.launchAppDetailsSettings](#m-launchappdetailssettings) 的兼容别名. |
| `app.openAppSettings(app)` | — | [app.launchAppDetailsSettings](#m-launchappdetailssettings) 的兼容别名. |
| `app.launchDualAppDetailsSettings(app)` | — | 在非当前用户配置文件中打开应用详情设置页. 此方法需要可用的 Shizuku 或 Root 权限. |
| `app.launchDualSettings(app)` | — | [app.launchDualAppDetailsSettings](#m-launchdualappdetailssettings) 的别名. |
| `app.openDualAppSetting(app)` | — | [app.launchDualAppDetailsSettings](#m-launchdualappdetailssettings) 的别名. |
| `app.openDualAppSettings(app)` | — | [app.launchDualAppDetailsSettings](#m-launchdualappdetailssettings) 的别名. |
| `app.uninstallDual(app)` | — | 在非当前用户配置文件中打开指定应用的卸载界面. 此方法需要可用的 Shizuku 或 Root 权限. |
| `app.openDualUrl(url)` | 6.6.1 | 在非当前用户配置文件中使用 `ACTION_VIEW` 打开 URL. URL 不包含 `://` 时自动添加 `http://` 前缀. |
| `app.startActivity(target, options?)` | — | 启动 Activity. 字符串包含 `://` 或符合网站格式时作为 URL 打开, 其他字符串作为 [AutoJs6 Activity 简称](dataTypes#intentshortformforactivity) 处理. URI 对象直接按 URL 打开. |
| `app.startDualActivity(target, options?)` | — | 在非当前用户配置文件中启动 Activity. 字符串和 URI 的识别规则与 [app.startActivity](#m-startactivity) 相同. `target` 为 Intent 或对象时可传入第二个选项对象; 对象合并时 `options` 中的同名属性优先. |
| `app.sendLocalBroadcastSync(intent)` | — | 按 `intent.action` 查找 AutoJs6 本地定时任务, 并将匹配任务调度到主线程执行. 查找在 I/O 线程中完成, 此方法不会等待任务执行结束. |
| `app.launchApp(appName)` | — | 通过应用名称启动应用. 如果该名称对应的应用不存在, 则返回 false; 否则返回 true. 如果该名称对应多个应用, 则只启动其中某一个. |
| `app.launch(packageName)` | — | 通过应用包名启动应用. 如果该包名对应的应用不存在, 则返回 false; 否则返回 true. |
| `app.launchPackage(packageName)` | — | 相当于 `app.launch(packageName)`. |
| `app.getPackageName(name)` | — | 解析应用标识并返回包名. 输入本身是已安装包名时原样返回; 输入为显示名且匹配多个应用时返回其中一个包名. 找不到匹配应用时返回 `null`. |
| `app.getAppName(name)` | — | 解析应用标识并返回显示名. 输入本身是已安装应用的显示名时原样返回. 找不到匹配应用时返回 `null`. |
| `app.viewFile(path)` | — | 用其他应用查看文件. 文件不存在的情况由查看文件的应用处理. |
| `app.editFile(path)` | — | 用其他应用编辑文件. 文件不存在的情况由编辑文件的应用处理. |
| `app.uninstall(packageName)` | — | 卸载应用. 执行后会会弹出卸载应用的提示框. 如果该包名的应用未安装, 由应用卸载程序处理, 可能弹出 "未找到应用" 的提示. |
| `app.openUrl(url)` | — | 用浏览器打开网站 url. |
| `app.sendEmail(options)` | — | 根据选项 options 调用邮箱应用发送邮件. 这些选项均是可选的. |
| `app.intent(options)` | — | 根据选项, 构造一个意图 Intent 对象. |
| `app.sendBroadcast(options)` | — | 根据选项构造一个 Intent, 并发送该广播. |
| `app.startService(options)` | — | 根据选项构造一个 Intent, 并启动该服务. |
| `app.sendBroadcast(name)` | — | 发送以上特定名称的广播可以触发 AutoJs6 的布局分析, 方便脚本调试. 这些广播在 AutoJs6 发送才有效, 在打包的脚本上运行将没有任何效果. |
| `app.intentToShell(options)` | — | 根据选项构造一个 Intent, 转换为对应的 shell 的 intent 命令的参数. |
| `app.parseUri(uri)` | — | 转换为 Android `Uri`. Android `Uri` 输入原样返回, Java `URI` 先转换为字符串. 其他输入类型返回 `null`. |
| `app.getUriForFile(path)` | — | 解析文件路径并创建 FileProvider `content://` URI. 路径无法解析时返回 `null`. |
| `app.getInstalledApps(options?)` | 6.8.0 | 获取当前用户的已安装应用信息. |
| `app.getInstalledPackages(options?)` | 6.8.0 | 获取当前用户的已安装包信息. 标志解析规则与 [app.getInstalledApps](#app-getinstalledapps-options) 相同. 返回对象的 `applicationInfo` 属性使用 [AppInfo](#c-appinfo) 包装. |
| `app.getApkInfo(path, options?)` | 6.8.0 | 读取尚未安装的 APK 文件包信息. 标志解析规则与 [app.getInstalledApps](#app-getinstalledapps-options) 相同. |

### AutoJs6 本体应用 — [原文](https://docs.autojs6.com/#/autojs)

| API | 版本 | 说明 |
| --- | --- | --- |
| `autojs.isScreenPortrait()` | 6.7.0 | 屏幕当前是否为竖屏方向 |
| `autojs.isScreenLandscape()` | 6.7.0 | 屏幕当前是否为横屏方向 |
| `autojs.restart(scripts?)` | 6.7.0 | 重启 AutoJs6 应用. 指定 **scripts** 时, AutoJs6 重启后依次运行这些脚本. |
| `autojs.exit(scripts?)` | 6.7.0 | 退出 AutoJs6 应用. 指定 **scripts** 时, AutoJs6 下次启动后依次运行这些脚本. |
| `autojs.getLanguage()` | 6.2.0 | 获取 AutoJs6 `语言` 设置选项. |
| `autojs.getLanguageTag()` | 6.2.0 | 获取 AutoJs6 语言设置选项. |
| `autojs.isRootAvailable()` | 6.2.0 | 获取 AutoJs6 的 Root 权限有效性. |
| `autojs.getRootMode()` | 6.2.0 | 获取 AutoJs6 的 Root 权限状态. |
| `autojs.setRootMode(rootMode, isWriteIntoPreference?)` | — | 设置 AutoJs6 的 Root 模式. |
| `autojs.canModifySystemSettings()` | 6.2.0 | 获取 AutoJs6 的 `修改系统设置` 权限状态. |
| `autojs.canWriteSecureSettings()` | 6.2.0 | 获取 AutoJs6 的 `修改安全设置` 权限状态. |
| `autojs.canDisplayOverOtherApps()` | 6.2.0 | 获取 AutoJs6 的 `显示在其他应用上层` 权限状态. |

### 自动化 (Automator) — [原文](https://docs.autojs6.com/#/automator)

| API | 版本 | 说明 |
| --- | --- | --- |
| `auto.click(x, y)` | — | 点击指定屏幕坐标. 坐标受 [setScreenMetrics](#setscreenmetrics-width-height) 设置的缩放规则影响. |
| `auto.click(point)` | — | 点击坐标点. 数组使用前两个元素作为 X 和 Y 坐标; 仅有一个数值元素时, 该值同时作为 X 和 Y 坐标. |
| `auto.click(text, index?)` | — | 查找文本包含 `text` 的控件并执行点击行为. 省略 `index` 时尝试点击全部匹配控件. |
| `auto.click(left, top, right, bottom)` | — | 查找边界与四个坐标匹配的控件并执行点击行为. |
| `auto.click(widget)` | — | 控件可点击时直接执行 [UiObject#click](uiObjectType#m-click). 否则点击控件边界中心坐标. |
| `auto.click(bounds)` | — | 点击矩形中心坐标. |
| `auto.longClick(x, y)` | — | 长按指定屏幕坐标. |
| `auto.longClick(point)` | — | 长按坐标点. 数组使用前两个元素作为 X 和 Y 坐标; 仅有一个数值元素时, 该值同时作为 X 和 Y 坐标. |
| `auto.longClick(text, index?)` | — | 查找文本包含 `text` 的控件并执行长按行为. 省略 `index` 时尝试长按全部匹配控件. |
| `auto.longClick(left, top, right, bottom)` | — | 查找边界与四个坐标匹配的控件并执行长按行为. |
| `auto.longClick(widget)` | — | 控件可长按时直接执行 [UiObject#longClick](uiObjectType#m-longclick). 否则长按控件边界中心坐标. |
| `auto.longClick(bounds)` | — | 长按矩形中心坐标. |
| `auto.gestureAsync(duration, points, callback?)` | — | 异步提交单个手势. `points` 可作为一个扁平坐标数组, 一个坐标点数组, 或拆成多个坐标点实参. 扁平数组按 `x1, y1, x2, y2, ...` 解析, 元素数量必须为偶数. |
| `gesturesAsync(...strokesAndCallback)` | — | 异步提交一个或多个同步或错时执行的手势笔画. 每个笔画使用以下形式之一: |
| `auto.clearCache()` | 6.6.0 | 清除 Android 无障碍交互客户端维护的控件缓存. 系统接口不可访问或调用异常时返回 `false`. |
| `auto.registerEvent(name, listener)` | — | 注册当前脚本的无障碍事件监听器. `name` 对应 Android [AccessibilityEvent](https://developer.android.com/reference/android/view/accessibility/AccessibilityEvent) 的 `TYPE_*` 常量名, 省略 `TYPE_` 前缀且不区分大小写, 如 \`window\_state\_cha |
| `auto.registerEvent(name, listener, options)` | — | 带过滤选项的注册, 用于抑制密集重复的事件: |
| `auto.registerEvents(name, listener)` | — | [auto.registerEvent](#m-registerevent) 的兼容别名. 此方法仍只注册一个事件名称. |
| `auto.removeEvent(name)` | — | 移除当前脚本通过 [auto.registerEvent](#m-registerevent) 注册的指定事件监听器. [auto.removeEvents](#m-removeevents) 是此方法的兼容别名. |
| `auto.removeEvents(name)` | — | [auto.removeEvent](#m-removeevent) 的兼容别名. 此方法仍只移除一个事件名称. |
| `auto.setFlags(flags)` | — | 设置 automator 的特性标志. 每次调用以给出的标志集合替换之前的设置, 未知的标志名抛出异常. |
| `auto.setWindowFilter(filter?)` | — | 设置窗口过滤器. 过滤器决定哪些窗口是选择器的搜索目标, 同时影响 [auto.root](#auto-root) 与 [auto.windowRoots](#auto-windowroots) 的结果. |
| `auto.findWindows(filter?)` | — | 按过滤器对象筛选当前全部窗口. 省略参数时返回全部窗口 (同 [auto.windows](#auto-windows)). 不受 [auto.setWindowFilter](#m-setwindowfilter) 影响. |
| `auto.findWindowRoots(filter?)` | — | [auto.findWindows](#m-findwindows) 的窗口根节点数组, 没有根节点的窗口被跳过. 与 [auto.windowRoots](#auto-windowroots) 不同, 不受 [auto.setWindowFilter](#m-setwindowfilter) 影响. |
| `auto.wait(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `auto.wait(cond, options, onOk?, onErr?)` | — | [flow.wait](flow#m-wait) 的别名: 异步等待选择器, 函数, 节点, Flow 或 Promise, 立即返回 [Flow](flowType) 对象. 参数见 [流程 (Flow)](flow). |
| `auto.explain(selector, root?)` | — | 解释一个选择器为何匹配或不匹配: 遍历根节点之下的全部节点 (`nodes` 为节点数, `roots` 为根节点数, 超出预算时 `truncated` 为 `true`), 对选择器的每个过滤器分别统计单独匹配数 (`matched`) 与逐步叠加后的匹配数 (`cumulative`), 找出首个把候选集合清空的过滤器 (`failingIndex` 从 0 计, `-1` 表示没有; \`f |
| `auto.dump(options?)` | — | 导出控件树: 各窗口根节点 (或 `root`) 之下的全部节点, 以文本 (默认; 每行一个节点, 缩进表示层级), JSON (嵌套对象, 子节点在 `children` 中) 或 XML (uiautomator 风格) 输出. |
| `auto.smartClick(target, options?)` | — | 智能点击: 目标可点击时直接点击 (`method` 为 `'node'`); 不可点击时上溯到最近的可点击祖先 (`climb`, 默认 `true`, 最多 `maxClimb` 层, 默认 `5`; 相当于罗盘 `k`; `method` 为 `'ancestor'`); 没有可点击祖先或点击被拒绝时, 在目标中心 (加 `offset` 像素与拟人偏移) 做点按手势 (\`gestureFa |
| `auto.smartClickBounds(target, options?)` | — | 显式坐标点击: 在目标控件的边界中心加上 `offset` 与拟人偏移后执行点按手势. 不调用目标或祖先的控件点击方法, 即使控件 `click()` 返回成功也不会采用该方法. 适合控件动作返回成功但界面没有响应的情况. |
| `auto.clickIfExists(target, options?)` | — | 目标存在且已点击为 `true`, 不存在为 `false` |
| `auto.clickIfExists(target, timeout)` | — | 目标在时限内存在时智能点击并返回 `true`, 不存在时返回 `false` 而不报错; 点击本身失败时仍抛出 [FlowError](flowErrorType). |
| `auto.clickBoundsIfExists(target, options?)` | — | 目标存在且已点击为 `true`, 不存在为 `false` |
| `auto.clickBoundsIfExists(target, timeout)` | — | 目标在时限内存在时在控件中心坐标点按并返回 `true`, 不存在时返回 `false` 而不报错; 点击本身失败时仍抛出 [FlowError](flowErrorType). |
| `auto.clickAny(targets, options?)` | — | — |
| `auto.clickAny(targets, timeout)` | — | 每轮按顺序检查候选目标, 智能点击首个存在的. 结果的 `index` 为候选在数组中的序号, `target` 为候选本身, `node` 为找到的节点. |
| `auto.clickBoundsAny(targets, options?)` | — | — |
| `auto.clickBoundsAny(targets, timeout)` | — | 每轮按顺序检查候选目标, 在控件中心坐标点按首个存在的. 结果的 `index` 为候选在数组中的序号, `target` 为候选本身, `node` 为找到的节点. |
| `auto.findAny(targets, options?)` | — | — |
| `auto.findAny(targets, timeout)` | — | 返回首个存在的候选目标, 不点击. 适合 "页面 A 或页面 B, 哪个先出现" 的分支判断. |
| `auto.scrollUntil(target, options?)` | — | 滚动直到目标出现: 先查找目标, 不存在时把容器向 `direction` (默认 `'down'`) 滚动一步, 等待 `settle` 毫秒 (默认 `300`) 后再查找, 直到找到目标, 已滚动 `maxSteps` 次 (默认 `20`), 或已到底 (连续两次滚动内容无变化; `stopAtEnd` 为 `false` 时不检测). |
| `auto.typeInto(target, text, options?)` | — | 向输入框输入文本: 定位可编辑节点 (选择器匹配的节点本身不可编辑时, 取其首个可编辑后代); `clear` 为 `true` (默认) 时替换全部文本, 否则追加到现有文本之后; `verify` 为 `true` (默认) 时重读节点直到其文本符合预期 (`verifyTimeout` 默认 `1000`, `verifyInterval` 默认 `100`; 密码框的文本被掩码, 跳过校验 |
| `auto.dismissPopups(targets, options?)` | — | 见下文 |
| `auto.dismissPopups(targets, timeout)` | — | 关闭弹窗. 候选项不存在不算错误; 点击使用智能点击, `click` 为传给 [smartClick](#m-smartclick) 的嵌套选项对象. |
| `auto.collectList(container, item, options?)` | — | 采集滚动列表: 每轮在容器内查找全部 `item` 匹配项, 按 `resultType` ([拾取结果类型](dataTypes#pickupresult), 默认 `'$'` 即节点内容; `'w'` 为节点本身, `'txt'` 为文本) 取值, 按节点内容指纹去重 (`dedupe`, 默认 `true`) 后加入结果; 然后把容器滚动一步 (`direction` 默认 `'down'` |
| `auto.launchAndWait(app, options?)` | — | — |
| `auto.launchAndWait(app, timeout)` | — | 启动应用并等待其到达前台: 把 `app` 解析为包名, 启动它 (已在前台且 `bringToFront` 为 `false` 时不启动, 此时 `launched` 为 `false`), 然后每 `interval` 毫秒 (默认 `200`) 检查 [currentPackage()](global#m-currentpackage), 直到变为该包名或超过 `timeout` 毫秒 (默 |
| `auto.backUntil(cond, options?)` | — | 条件成立时的值 (节点, 拾取结果或函数返回值) |
| `auto.backUntil(cond, maxTimes)` | — | 反复按返回键直到条件成立: 先检查一次条件; 之后每按一次返回键 ([back](#back)), 在 `delay` 毫秒 (默认 `500`) 内每 `interval` 毫秒 (默认 `100`, 须为正数) 检查条件; 最多按 `maxTimes` 次 (默认 `10`). 此函数不使用 `timeout` 选项. |
| `auto.backToApp(app, options?)` | — | 到达前台的包名 |
| `auto.backToApp(app, maxTimes)` | — | 反复按返回键直到指定应用回到前台, 行为同 [backUntil](#m-backuntil). 无法解析的应用抛出 `INVALID_TARGET` (`reason` 为 `unknownApp`). |
| `auto.toggle(target, checked, options?)` | — | 设置可勾选控件的状态: 目标不可勾选时取其首个可勾选后代; 已是期望状态时不做任何操作 (`changed` 为 `false`, `method` 为 `null`); 否则以 `click` 选项智能点击, 并在 `verifyTimeout` 毫秒 (默认 `1000`, 间隔 `verifyInterval`, 默认 `100`) 内重读直到状态符合. |
| `auto.retry(fn, options?)` | — | `fn` 成功时的返回值 |
| `auto.retry(fn, times)` | 6.8.0 | 在调用线程上执行 `fn`, 抛出异常时在 `delay` 毫秒 (默认 `500`) 后重跑, 之后每次重跑前的等待乘以 `backoff` (默认 `1`, 须不小于 `1`); 重跑 `times` 次 (默认 `3`) 仍失败时抛出最后一次的异常. 取消 (`CANCELLED` 的 [FlowError](flowErrorType)), 脚本中断与 JVM `Error` 不重试. |
| `auto.waitForIdle(quietFor?)` | — | — |
| `auto.waitForIdle(options)` | — | 等待界面安静: 连续 `quietFor` 毫秒内没有指定类型 (`eventTypes`, 默认为窗口状态, 窗口内容与窗口列表变化三种; `'*'` 表示全部类型) 的无障碍事件时返回. 结果含实际使用的 `quietFor`, 总耗时 `elapsed` 与期间观察到的事件数 `events`. |
| `auto.waitForEvent(type?, filter?, timeout?)` | — | 等待首个符合条件的无障碍事件. `filter` 与 `timeout` 的顺序可以互换. |
| `auto.waitForToast(text?, timeout?)` | — | 等待首个符合条件的 Toast 消息. |
| `auto.waitForNotification(filter?, timeout?)` | — | 等待首个符合条件的系统通知. |
| `简易自动化 (SimpleActionAutomator)` | — | SimpleActionAutomator 提供按文本或控件序号执行点击, 滚动和文本输入等全局便捷方法. 这些方法依赖无障碍服务, 并在本页 [SimpleActionAutomator](#simpleactionautomator) 小节集中列出. |
| `高权限自动化 (RootAutomator)` | — | RootAutomator 通过 Root 权限向设备输入系统写入触摸事件, 可执行按下, 移动, 抬起及多指操作. 它与基于无障碍服务的 `automator` 方法具有不同的运行条件和生命周期, 详见 [RootAutomator](#rootautomator). |
| `自动化配置 (AutomatorConfiguration)` | — | 可调用对象 `auto` 用于管理无障碍服务状态, 自动化模式, 事件配置, 窗口过滤器及窗口根节点. 本页后半部分以临时作用域对象 `auto` 列出其成员; 选择器和控件节点的独立参考见 [UiSelector](uiSelectorType) 与 [UiObject](uiObjectType). |
| `选择器 (UiSelector)` | — | UiSelector (选择器), 亦可看作是 [控件节点](uiObjectType) 的条件筛选器, 用于通过附加不同的条件, 筛选出一个或一组活动窗口中的 `控件节点`, 并做进一步处理, 如 \[ 执行 [控件行为](uiObjectActionsType) (点击, 长按, 设置文本等) / 判断位置 / 获取文本内容 / 获取控件特定状态 / 在 \[控件层级\](glossaries#控件 |
| `控件节点 (UiObject)` | — | UiObject 通常被称为 \[ 控件 / 节点 / 控件节点 \], 可看做是一个通过安卓无障碍服务包装的 [AccessibilityNodeInfo](https://developer.android.com/reference/android/view/accessibility/AccessibilityNodeInfo) 对象, 代表一个当前活动窗口中的节点, 通过此节点可收集控件信息 |
| `控件集合 (UiObjectCollection)` | — | UiObjectCollection 代表 [控件节点 (UiObject)](uiObjectType) 的对象集合. |
| `automator.isServiceRunning()` | 6.1.0 | 与 [auto.isRunning](#auto-isrunning) 语义一致: 系统设置中已启用 AutoJs6 无障碍服务, 且当前进程中存在服务实例. |
| `automator.ensureService()` | 6.1.0 | 确保 AutoJs6 无障碍服务已经启动. 方法会先尝试已配置的 Root 或安全设置启动方式, 必要时打开系统无障碍设置页并等待服务. 服务仍不可用时抛出异常. |
| `automator.waitForService(timeout?)` | — | 尝试启动无障碍服务并阻塞当前线程, 直至服务实例建立. 超时或等待被中断时抛出脚本中断异常. |
| `automator.captureScreen()` | — | 使用 Android 无障碍服务截图接口截取屏幕. Android API 级别低于 `30` 时抛出异常. 此方法不使用媒体投影截图权限. |
| `automator.lockScreen()` | — | 触发锁屏全局操作. Android API 级别低于 `28` 时仍会尝试执行, 但系统可能返回 `false`. |
| `automator.takeScreenshot()` | — | 触发系统截图全局操作. 此方法仅返回操作是否被接受, 不返回图像. Android API 级别低于 `28` 时仍会尝试执行. |
| `automator.accessibilityButton()` | — | 触发无障碍按钮全局操作. Android API 级别低于 `31` 时仍会尝试执行. |
| `automator.accessibilityButtonChooser()` | — | 打开无障碍按钮目标选择器. Android API 级别低于 `31` 时仍会尝试执行. |
| `automator.accessibilityShortcut()` | — | 触发无障碍快捷方式全局操作. Android API 级别低于 `31` 时仍会尝试执行. |
| `automator.accessibilityAllApps()` | — | 打开系统全部应用界面. Android API 级别低于 `31` 时仍会尝试执行. |
| `automator.dismissNotificationShade()` | — | 收起通知栏. Android API 级别低于 `31` 时仍会尝试执行. |
| `automator.back()` | — | 触发系统返回操作. |
| `automator.home()` | — | 返回系统主页. |
| `automator.powerDialog()` | — | 打开系统电源菜单. |
| `automator.notifications()` | — | 展开系统通知栏. |
| `automator.quickSettings()` | — | 打开系统快速设置面板. |
| `automator.recents()` | — | 打开系统最近任务界面. |
| `automator.splitScreen()` | — | 触发系统分屏操作. 操作是否可用还取决于 Android 版本和系统实现. |
| `automator.headsetHook()` | — | 触发耳机按键全局操作. 此方法也是 `automator.headsethook()` 的驼峰命名别名. |
| `automator.switchToInputMethod(packageName)` | — | 查找指定包名对应的已启用输入法并切换到该输入法. 未找到对应输入法时返回 `false`. |
| `automator.switchToInputMethodWithId(inputMethodId)` | — | 通过输入法 ID 切换当前输入法. |
| `控件节点行为 (UiObjectActions)` | — | UiObjectActions 是一个 Java 接口, 代表 [控件节点 (UiObject)](uiObjectType) 的行为集合. |
| `setScreenMetrics(width, height)` | — | 设置脚本坐标点击所适合的屏幕宽高. 如果脚本运行时, 屏幕宽度不一致会自动放缩坐标. |
| `press(x, y, duration)` | — | 模拟按住坐标 (x, y), 并返回是否成功. 只有按住操作执行完成时脚本才会继续执行. |
| `swipe(x1, y1, x2, y2, duration)` | — | 模拟从坐标 (x1, y1) 滑动到坐标 (x2, y2), 并返回是否成功. 只有滑动操作执行完成时脚本才会继续执行. |
| `gesture(duration, [x1, y1], [x2, y2], ...)` | — | 模拟手势操作. 例如 `gesture(1000, [0, 0], [500, 500], [500, 1000])` 为模拟一个从 (0, 0) 到 (500, 500) 到 (500, 100) 的手势操作, 时长为 2 秒. |
| `gestures([delay1, duration1, [x1, y1], [x2, y2], ...], [delay2, duration2, [x3, y3], [x4, y4], ...], ...)` | — | 同时模拟多个手势. 每个手势的参数为 \\\[delay, duration, 坐标\\\], delay 为延迟多久 (毫秒) 才执行该手势; duration 为手势执行时长; 坐标为手势经过的点的坐标. 其中 delay 参数可以省略, 默认为 0. |
| `RootAutomator.tap(x, y[, id])` | — | 点击位置 (x, y). 其中 id 是一个整数值, 用于区分多点触摸, 不同的 id 表示不同的 "手指", 例如: |
| `RootAutomator.swipe(x1, x2, y1, y2[, duration, id])` | — | 模拟一次从 (x1, y1) 到 (x2, y2) 的时间为 duration 毫秒的滑动. |
| `RootAutomator.press(x, y, duration[, id])` | — | 模拟按下位置 (x, y), 时长为 duration 毫秒. |
| `RootAutomator.longPress(x, y[\, id\])` | — | 模拟长按位置 (x, y). |
| `RootAutomator.touchDown(x, y[, id])` | — | 模拟手指按下位置 (x, y). |
| `RootAutomator.touchMove(x, y[, id])` | — | 模拟移动手指到位置 (x, y). |
| `RootAutomator.touchUp([id])` | — | 模拟手指弹起. |
| `Tap(x, y)` | — | 点击位置 (x, y), 您可以通过 "开发者选项" 开启指针位置来确定点击坐标. |
| `Swipe(x1, y1, x2, y2, \[duration\])` | — | 滑动. 从 (x1, y1) 位置滑动到 (x2, y2) 位置. |
| `auto([mode])` | — | 检查无障碍服务是否已经启用, 如果没有启用则抛出异常并跳转到无障碍服务启用界面; 同时设置无障碍模式为 mode. mode 的可选值为: |
| `auto.start()` | — | 尝试通过已配置的 Root 或安全设置方式启动 AutoJs6 无障碍服务. 无法自动启动时打开系统无障碍设置页并返回 `false`. |
| `auto.stop()` | — | 尝试通过 Root, 安全设置或服务自身接口停止 AutoJs6 无障碍服务. 无法自动停止时打开系统无障碍设置页并返回 `false`. |
| `auto.enable()` | 6.6.2 | [auto.start](#auto-start) 的别名. |
| `auto.disable()` | 6.6.2 | [auto.stop](#auto-stop) 的别名. |
| `auto.hasInstance()` | 6.7.0 | 当前进程中是否存在无障碍服务实例 |
| `auto.hasService()` | 6.7.0 | 系统设置中是否已启用 AutoJs6 无障碍服务 |
| `auto.exists()` | — | [auto.hasService](#auto-hasservice) 的兼容方法. |
| `auto.isRunning()` | — | 服务是否已启用且当前进程中存在服务实例 |
| `auto.isOperational()` | 6.7.0 | 服务是否已运行并在当前进程中确认可工作 |
| `auto.stateListener(listener?)` | 6.3.3 | 设置当前脚本运行时的无障碍服务连接状态监听器. 服务连接时调用 `onConnected()`, 断开时调用 `onDisconnected()`. 省略监听器或传入 `null` 或 `undefined` 时清除监听器. |
| `auto.launchSettings()` | — | 打开 Android 系统无障碍设置页, 并提示用户选择 AutoJs6. |
| `auto.currentPackage()` | — | 返回无障碍运行时最近记录的应用包名. 结果是否依赖无障碍服务受 [auto.setFlags](#m-setflags) 配置影响. |
| `auto.currentActivity()` | — | 最近识别的前台 Activity 名称, 未识别时为空字符串 |
| `auto.currentComponent()` | — | 包名和 Activity 名称均可用时返回 `packageName/activityName`, 否则返回空字符串. |
| `auto.waitFor()` | — | 检查无障碍服务是否已经启用, 如果没有启用则跳转到无障碍服务启用界面, 并等待无障碍服务启动; 当无障碍服务启动后脚本会继续运行. |
| `auto.setMode(mode)` | — | 设置无障碍模式为 mode. mode 的可选值为: |
| `auto.getWindowRoot(window)` | — | 获取指定无障碍窗口的控件树根节点. `window` 通常来自 [auto.windows](#auto-windows). |
| `窗口过滤器对象 (WindowFilter)` | 6.8.0 | 用于 [auto.setWindowFilter](#m-setwindowfilter), [auto.findWindows](#m-findwindows) 与 [auto.findWindowRoots](#m-findwindowroots) 的窗口匹配条件. 给出的键全部满足时窗口通过; 未知的键抛出异常. |
| `scrollUp([i])` | — | 找到第 i + 1 个可滑动控件上滑或 **左滑**. 返回是否操作成功. 屏幕上没有可滑动的控件时返回 false. |
| `scrollDown([i])` | — | 找到第 i + 1 个可滑动控件下滑或 **右滑**. 返回是否操作成功. 屏幕上没有可滑动的控件时返回 false. |
| `setText([i, ]text)` | — | 返回是否输入成功. 当找不到对应的文本框时返回 false. |
| `input([i, ]text)` | 6.8.0 | 返回是否输入成功. 当找不到对应的文本框时返回 false. |
| `目标参数 (target)` | — | 工具函数的 `target` (以及 `targets` 数组的每一项, `container`, `item`, `verify`, `cond` 等) 接受: |
| `弹窗守护句柄 (PopupGuard)` | 6.8.0 | [dismissPopups](#m-dismisspopups) 以 `watch: true` 调用时的返回值: |

### 条码 (Barcode) — [原文](https://docs.autojs6.com/#/barcode)

| API | 版本 | 说明 |
| --- | --- | --- |
| `barcode.barcode(options?, isAll?)` | — | 识别当前屏幕. `isAll` 为 `false` 时返回首个结果的原始文本, 没有结果或该值为 `null` 时返回 `null`; 为 `true` 时返回全部非空原始文本, 未识别到文本时返回空数组. |
| `barcode.barcode(isAll)` | — | 使用默认选项识别当前屏幕. |
| `barcode.barcode(image, options?, isAll?)` | — | 识别指定图片. 末尾的 `isAll` 会覆盖 `options.isAll`. |
| `barcode.barcode(image, isAll)` | — | 使用默认选项识别指定图片. |
| `barcode.detect(options?, isAll?)` | — | 识别当前屏幕. `isAll` 为 `false` 时返回首个结果, 没有结果时返回 `null`; 为 `true` 时返回全部结果数组. |
| `barcode.detect(isAll)` | — | 使用默认选项识别当前屏幕. |
| `barcode.detect(image, options?, isAll?)` | — | 识别指定图片. 末尾的 `isAll` 会覆盖 `options.isAll`. |
| `barcode.detect(image, isAll)` | — | 使用默认选项识别指定图片. |
| `barcode.detectAll(options?)` | — | 识别当前屏幕并始终返回数组. 没有结果时返回空数组. |
| `barcode.detectAll(image, options?)` | — | 识别指定图片并始终返回数组. |
| `barcode.recognizeText(options?, isAll?)` | — | 识别当前屏幕并返回结果的原始文本. `isAll` 为 `false` 时, 没有结果或首个结果的原始文本为 `null` 均返回 `null`; 为 `true` 时会过滤原始文本为 `null` 的结果. |
| `barcode.recognizeText(isAll)` | — | 使用默认选项识别当前屏幕. |
| `barcode.recognizeText(image, options?, isAll?)` | — | 识别指定图片并返回结果的原始文本. 末尾的 `isAll` 会覆盖 `options.isAll`. |
| `barcode.recognizeText(image, isAll)` | — | 使用默认选项识别指定图片. |
| `barcode.recognizeTexts(options?)` | — | 识别当前屏幕并始终返回数组. 原始文本为 `null` 的结果会被过滤. |
| `barcode.recognizeTexts(image, options?)` | — | 识别指定图片并始终返回数组. |
| `barcode.WrappedBarcode#getFormat()` | — | 返回结果的格式数值, 对应 [BarcodeFormat](#barcodeformat) 表. |
| `barcode.WrappedBarcode#getFormatName()` | — | 返回带 `FORMAT_` 前缀的规范名称. 无法识别格式时返回 `"FORMAT_NAME_UNKNOWN"`. |
| `barcode.WrappedBarcode#getValueType()` | — | 返回条码内容的值类型. |
| `barcode.WrappedBarcode#getType()` | 6.4.0 | [getValueType()](#m-wrappedbarcodegetvaluetype) 的别名. |
| `barcode.WrappedBarcode#getValueTypeName()` | — | 返回带 `TYPE_` 前缀的规范名称. |
| `barcode.WrappedBarcode#getTypeName()` | — | `getValueTypeName()` 的别名. 无法识别类型时返回 `"TYPE_NAME_UNKNOWN"`. |
| `barcode.WrappedBarcode#getBoundingBox()` | — | 条码外接矩形 |
| `barcode.WrappedBarcode#getCornerPoints()` | — | 插件返回的扁平坐标会按 `(x, y)` 两个一组转换为 Android Point. |
| `barcode.WrappedBarcode#getDisplayValue()` | — | 便于显示的格式化值 |
| `barcode.WrappedBarcode#getRawValue()` | — | `recognizeText()` 和 `recognizeTexts()` 使用此值. |
| `barcode.WrappedBarcode#getRawBytes()` | — | 条码原始字节 |
| `barcode.WrappedBarcode#getCalendarEvent()` | — | 日历事件结构化数据 |
| `barcode.WrappedBarcode#getContactInfo()` | — | 联系人结构化数据 |
| `barcode.WrappedBarcode#getDriverLicense()` | — | 驾照结构化数据 |
| `barcode.WrappedBarcode#getEmail()` | — | 邮件结构化数据 |
| `barcode.WrappedBarcode#getGeoPoint()` | — | 经纬度结构化数据 |
| `barcode.WrappedBarcode#getPhone()` | — | 电话结构化数据 |
| `barcode.WrappedBarcode#getSms()` | — | 短信结构化数据 |
| `barcode.WrappedBarcode#getUrl()` | — | URL 结构化数据 |
| `barcode.WrappedBarcode#getWifi()` | — | 上述结构化方法只在条码类型与方法匹配且插件提供对应 JSON 对象时返回数据, 其他情况返回 `null`. 对象字段由当前插件结果决定. |
| `barcode.WrappedBarcode#toString()` | — | 摘要格式为 `[FORMAT] <TYPE> displayValue`, 其中格式名和类型名会移除 `FORMAT_` 与 `TYPE_` 前缀. |

### Base64 — [原文](https://docs.autojs6.com/#/base64)

| API | 版本 | 说明 |
| --- | --- | --- |
| `base64.encode(o, encoding?)` | — | 对数据进行 Base64 编码. |
| `base64.decode(o, encoding?)` | — | 对数据进行 Base64 解码. |

### 画布 (Canvas) — [原文](https://docs.autojs6.com/#/canvas)

| API | 版本 | 说明 |
| --- | --- | --- |
| `Canvas()` | — | 创建一个没有底层 Android Canvas 和位图的画布. 在调用 |
| `Canvas(bitmap)` | — | 画布直接绘制到传入的 Bitmap. Bitmap 必须可变; 不可变 Bitmap 会导致构造失败. |
| `Canvas(image)` | — | 复制 `image` 的 Bitmap 并创建独立的可变绘制目标. 绘制不会修改源 |
| `Canvas(width, height)` | — | 创建指定尺寸的透明位图并将其设为绘制目标. `width` 和 `height` 必须是可转换为 |
| `canvas.getAndroidCanvas()` | — | 返回当前委托的 Android Canvas. 无参构造且尚未调用 `setCanvas()` 时返回 `null`. |
| `canvas.setCanvas(canvas)` | — | 替换当前委托的 Android Canvas. `null` 会解除当前目标, 此后依赖绘制目标的方法 |
| `canvas.toImage()` | — | 复制构造 Canvas 时创建或传入的内部 Bitmap, 并将副本包装为 ImageWrapper. |
| `canvas.isHardwareAccelerated()` | — | 返回底层 Android Canvas 的硬件加速状态. Bitmap 支持的离屏 Canvas 通常不是 |
| `canvas.setBitmap(bitmap)` | — | 调用底层 Android Canvas 的 `setBitmap()` 更换绘制位图. 新 Bitmap 必须可变. |
| `canvas.isOpaque()` | — | 当前绘制图层是否不支持逐像素透明度 |
| `canvas.getWidth()` | — | 当前绘制图层宽度 |
| `canvas.getHeight()` | — | 当前绘制图层高度 |
| `canvas.getDensity()` | — | 目标密度用于决定绘制不同密度 Bitmap 时的自动缩放比例. 没有位图目标时通常为 |
| `canvas.setDensity(density)` | — | 设置画布目标密度. 若底层 Android Canvas 当前持有 Bitmap, 还会同时设置该 |
| `canvas.getMaximumBitmapWidth()` | — | 尝试绘制超过此宽度的 Bitmap 会失败. 限制值取决于当前 Android Canvas 实现. |
| `canvas.getMaximumBitmapHeight()` | — | 尝试绘制超过此高度的 Bitmap 会失败. 限制值取决于当前 Android Canvas 实现. |
| `canvas.save()` | — | 保存当前变换矩阵和裁剪区域. |
| `canvas.saveLayer(bounds, paint, saveFlags)` | — | 保存状态并把后续绘制重定向到离屏图层. 调用 `restore()` 时, 离屏内容使用 |
| `canvas.saveLayer(bounds, paint)` | — | 可传给 `restoreToCount()` 的保存计数 |
| `canvas.saveLayer(left, top, right, bottom, paint, saveFlags)` | — | 此重载等价于使用 RectF 边界的已弃用重载. |
| `canvas.saveLayer(left, top, right, bottom, paint)` | — | `saveLayer()` 会分配额外离屏绘制目标, 开销明显高于 `save()`. 应限制图层边界, |
| `canvas.saveLayerAlpha(bounds, alpha, saveFlags)` | — | 保存状态并创建带整体透明度的离屏图层. 带 `saveFlags` 的 Android 重载已弃用, |
| `canvas.saveLayerAlpha(bounds, alpha)` | — | 可传给 `restoreToCount()` 的保存计数 |
| `canvas.saveLayerAlpha(left, top, right, bottom, alpha, saveFlags)` | — | 可传给 `restoreToCount()` 的保存计数 |
| `canvas.saveLayerAlpha(left, top, right, bottom, alpha)` | — | 可传给 `restoreToCount()` 的保存计数 |
| `canvas.restore()` | — | 恢复最近一次 `save()`, `saveLayer()` 或 `saveLayerAlpha()` 前的状态. 恢复次数超过 |
| `canvas.getSaveCount()` | — | 返回值至少为 1. 初始状态本身占用一个栈层级. |
| `canvas.restoreToCount(saveCount)` | — | 连续恢复状态, 直至状态栈达到 `saveCount`. 通常传入 `save()`, |
| `canvas.translate(dx, dy)` | — | 将当前坐标系平移指定距离. |
| `canvas.scale(sx, sy)` | — | 以当前原点为中心缩放坐标系. 负系数会同时产生镜像效果. |
| `canvas.scale(sx, sy, px, py)` | — | — |
| `canvas.rotate(degrees)` | — | 以当前原点为中心旋转坐标系. |
| `canvas.rotate(degrees, px, py)` | — | — |
| `canvas.skew(sx, sy)` | — | — |
| `canvas.concat(matrix)` | — | 将 `matrix` 前乘到当前变换矩阵. `matrix` 为 `null` 时不执行操作. |
| `canvas.setMatrix(matrix)` | — | 完全替换当前变换矩阵. `matrix` 为 `null` 时重置为单位矩阵. 通常应优先使用 |
| `canvas.getMatrix(ctm)` | — | 把当前变换矩阵复制到 `ctm`. |
| `canvas.getMatrix()` | — | Android 已弃用读取 Canvas 当前矩阵的接口. 硬件加速画布的矩阵可能由调用环境决定, |
| `canvas.clipRect(rect, op)` | — | 运算后的裁剪区域是否非空 |
| `canvas.clipRect(rect)` | — | 相交后的裁剪区域是否非空 |
| `canvas.clipRect(left, top, right, bottom, op)` | — | 运算后的裁剪区域是否非空 |
| `canvas.clipRect(left, top, right, bottom)` | — | 此 JavaScript 签名覆盖底层的浮点和整数坐标重载. |
| `canvas.clipPath(path, op)` | — | 带 `op` 的 Android 裁剪重载已弃用, 并受与 `clipRect(rect, op)` 相同的 |
| `canvas.clipPath(path)` | — | 相交后的裁剪区域是否非空 |
| `canvas.getDrawFilter()` | — | 当前绘制过滤器 |
| `canvas.setDrawFilter(filter)` | — | 绘制过滤器可在绘制时修改 Paint 标志. Android 提供的常用实现为 |
| `canvas.quickReject(rect, type)` | — | 矩形是否完全位于当前裁剪区域之外 |
| `canvas.quickReject(path, type)` | — | 路径是否可确定完全位于当前裁剪区域之外 |
| `canvas.quickReject(left, top, right, bottom, type)` | — | `quickReject()` 用于跳过必然不可见的绘制. 返回 `true` 表示可以安全跳过; |
| `canvas.getClipBounds(bounds)` | — | 将当前局部坐标系中的裁剪边界写入 `bounds`. 虽然 ScriptCanvas 源码保留了可空 |
| `canvas.getClipBounds()` | — | 裁剪区域为空时返回 `Rect(0, 0 - 0, 0)`. |
| `canvas.drawRGB(r, g, b)` | — | 使用完全不透明的 RGB 颜色和 `PorterDuff.Mode.SRC_OVER` 填充当前裁剪区域. |
| `canvas.drawARGB(a, r, g, b)` | — | 使用 ARGB 颜色和 `PorterDuff.Mode.SRC_OVER` 填充当前裁剪区域. |
| `canvas.drawColor(color)` | — | 使用 `PorterDuff.Mode.SRC_OVER` 填充当前裁剪区域. |
| `canvas.drawColor(color, mode)` | — | 使用指定合成模式填充当前裁剪区域. 例如 `PorterDuff.Mode.CLEAR` 可清除 |
| `canvas.drawPaint(paint)` | — | 使用 `paint` 填充当前裁剪区域. 此方法相当于绘制一个无限大的矩形, 但效率更高. |
| `canvas.drawPoint(x, y, paint)` | — | 绘制一个点. 点的尺寸由 `paint.getStrokeWidth()` 决定, 形状由 |
| `canvas.drawPoints(pts, offset, count, paint)` | — | 每 2 个连续数值表示一个点. `count / 2` 的整数部分为实际处理的点数. |
| `canvas.drawPoints(pts, paint)` | — | 绘制数组中的全部点. |
| `canvas.drawLine(startX, startY, stopX, stopY, paint)` | — | 绘制线段. 线段始终按描边处理, 因此忽略 `Paint.Style`. 长度为 0 的线段不绘制. |
| `canvas.drawLines(pts, offset, count, paint)` | — | 每 4 个连续数值表示一条独立线段. `count / 4` 的整数部分为实际绘制的线段数. |
| `canvas.drawLines(pts, paint)` | — | 绘制数组中的全部完整线段. |
| `canvas.drawRect(rect, paint)` | — | — |
| `canvas.drawRect(left, top, right, bottom, paint)` | — | 矩形按 `paint.getStyle()` 填充, 描边, 或同时填充并描边. |
| `canvas.drawOval(oval, paint)` | — | — |
| `canvas.drawOval(left, top, right, bottom, paint)` | — | 绘制适配外接矩形的椭圆. |
| `canvas.drawCircle(cx, cy, radius, paint)` | — | 半径小于或等于 0 时不绘制. |
| `canvas.drawArc(oval, startAngle, sweepAngle, useCenter, paint)` | — | — |
| `canvas.drawArc(left, top, right, bottom, startAngle, sweepAngle, useCenter, paint)` | — | 角度 0 位于椭圆右侧的 3 点钟方向, 正角度按顺时针方向增加. |
| `canvas.drawRoundRect(rect, rx, ry, paint)` | — | — |
| `canvas.drawRoundRect(left, top, right, bottom, rx, ry, paint)` | — | — |
| `canvas.drawPath(path, paint)` | — | 路径按 `paint.getStyle()` 填充, 描边, 或同时填充并描边. |
| `canvas.drawBitmap(bitmap, left, top, paint)` | — | 在指定左上角绘制整张 Bitmap. 此重载会根据 Bitmap 与 Canvas 的密度差异自动缩放. |
| `canvas.drawBitmap(bitmap, src, dst, paint)` | — | 把源区域缩放并平移到目标区域. 此类显式源和目标矩形重载忽略 Bitmap 密度. |
| `canvas.drawBitmap(colors, offset, stride, x, y, width, height, hasAlpha, paint)` | — | 把 ColorInt 数组直接视为 Bitmap 像素并绘制. 此 JavaScript 签名覆盖底层使用 |
| `canvas.drawBitmap(bitmap, matrix, paint)` | — | 使用 `matrix` 变换 Bitmap 后进行绘制. 结果仍会继续经过 Canvas 的当前变换矩阵. |
| `canvas.drawBitmapMesh(bitmap, meshWidth, meshHeight, verts, vertOffset, colors, colorOffset, paint)` | — | 按均匀分布在源 Bitmap 上的网格变形并绘制. `verts` 至少需要 |
| `canvas.drawVertices(mode, vertexCount, verts, vertOffset, texs, texOffset, colors, colorOffset, indices, indexOffset, indexCount, paint)` | — | 把 `verts` 解释为三角形并绘制. `texs` 非空时, Paint 必须配置 Shader; |
| `canvas.drawImage(image, left, top, paint)` | — | 在指定左上角绘制整张 ImageWrapper. |
| `canvas.drawImage(image, left, top, width, height, paint)` | — | 缩放整张图片以填充指定目标区域. |
| `canvas.drawImage(image, sx, sy, swidth, sheight, left, top, width, height, paint)` | — | 把源图片的指定像素区域缩放并平移到目标区域. |
| `canvas.drawImage(image, src, dst, paint)` | — | — |
| `canvas.drawImage(image, matrix, paint)` | — | 使用 `matrix` 变换 ImageWrapper 的 Bitmap 后进行绘制. |
| `canvas.drawText(text, index, count, x, y, paint)` | — | — |
| `canvas.drawText(text, x, y, paint)` | — | 绘制完整字符串. |
| `canvas.drawText(text, start, end, x, y, paint)` | — | — |
| `canvas.drawTextRun(text, index, count, contextIndex, contextCount, x, y, isRtl, paint)` | — | — |
| `canvas.drawTextRun(text, start, end, contextStart, contextEnd, x, y, isRtl, paint)` | — | 绘制单一方向的文本运行, 并使用更大范围的文本作为复杂文字塑形上下文. |
| `canvas.drawPosText(text, index, count, pos, paint)` | — | — |
| `canvas.drawPosText(text, pos, paint)` | — | Android 已弃用逐字符定位文本. 此方法不支持字形组合, 分解, 补充字符或复杂文字. |
| `canvas.drawTextOnPath(text, index, count, path, hOffset, vOffset, paint)` | — | — |
| `canvas.drawTextOnPath(text, path, hOffset, vOffset, paint)` | — | 文本沿 `path` 绘制. Paint 的文本对齐方式决定文本起点相对路径的位置. |
| `canvas.drawPicture(picture)` | — | 结束 Picture 的录制并回放其内容. 此方法会自行保存和恢复 Canvas 状态. |
| `canvas.drawPicture(picture, dst)` | — | 缩放 Picture 以填充目标区域, 并自动保存和恢复 Canvas 状态. |
| `Canvas(flags)` | — | 常用标志包括 `Paint.ANTI_ALIAS_FLAG`, `Paint.DITHER_FLAG` 和 |
| `Canvas(paint)` | — | 复制源 Paint 的全部原生配置. |
| `canvas.setColor(color)` | — | 设置 Paint 颜色. |
| `canvas.setARGB(a, r, g, b)` | — | 使用 4 个颜色通道设置 Paint 颜色. 此原生 Android 方法不存在 ColorInt 与 |
| `canvas.on("draw", listener)` | — | 注册每帧绘制监听器. `listener` 接收以下参数: |
| `canvas.setMaxFps(maxFps)` | — | 设置绘制循环的最短帧间隔. 默认值为 30 FPS. 正整数使用 |

### 颜色 (Color) — [原文](https://docs.autojs6.com/#/color)

| API | 版本 | 说明 |
| --- | --- | --- |
| `colors.toInt(color)` | 6.2.0 | 将颜色参数转换为 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.toHex(color)` | — | 将颜色参数转换为 [颜色代码 (ColorHex)](dataTypes#colorhex). |
| `colors.toHex(color, alpha)` | — | 将颜色参数转换为 [颜色代码 (ColorHex)](dataTypes#colorhex), 并根据 `alpha` 参数决定颜色代码 `A (alpha)` 分量的显示状态. |
| `colors.toHex(color, length)` | — | 将颜色参数转换为 [颜色代码 (ColorHex)](dataTypes#colorhex), 并根据 `length` 参数决定颜色代码的显示状态. |
| `colors.toFullHex(color)` | 6.2.0 | 将颜色参数强制转换为 [颜色代码 (ColorHex)](dataTypes#colorhex) 的完整形式 (#AARRGGBB). |
| `colors.build(color?)` | — | 构建一个 [Color](colorType) 实例, 相当于 `new Color(color?)` 或 `Color(color?)`. |
| `colors.build(red, green, blue, alpha?)` | — | 构建一个 [Color](colorType) 实例, 相当于 `new Color(red, green, blue, alpha?)`. |
| `colors.summary(color)` | 6.3.0 | 获取颜色摘要. |
| `colors.parseColor(color)` | — | 将颜色参数转换为 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.toString(color)` | — | 将颜色参数转换为 [颜色代码 (ColorHex)](dataTypes#colorhex). |
| `colors.toString(color, alpha)` | — | 将颜色参数转换为 [颜色代码 (ColorHex)](dataTypes#colorhex), 并根据 `alpha` 参数决定颜色代码 `A (alpha)` 分量的显示状态. |
| `colors.toString(color, length)` | — | 将颜色参数转换为 [颜色代码 (ColorHex)](dataTypes#colorhex), 并根据 `length` 参数决定颜色代码的显示状态. |
| `colors.alpha(color)` | — | 获取颜色的 `A (alpha)` 分量, 取值范围 `[0..255]`. |
| `colors.alpha(color, options)` | — | 获取颜色的 `A (alpha)` 分量. |
| `colors.alphaDouble(color)` | 6.3.0 | 获取颜色的 `A (alpha)` 分量, 取值范围 `[0..1]`. |
| `colors.getAlpha(color)` | — | 获取颜色的 `A (alpha)` 分量, 取值范围 `[0..255]`. |
| `colors.getAlpha(color, options)` | — | 获取颜色的 `A (alpha)` 分量. |
| `colors.getAlphaDouble(color)` | 6.3.0 | 获取颜色的 `A (alpha)` 分量, 取值范围 `[0..1]`. |
| `colors.setAlpha(color, alpha)` | 6.3.0 | 设置颜色的 `A (alpha)` 分量, 返回新颜色的颜色整数. |
| `colors.setAlphaRelative(color, percentage)` | 6.3.1 | 针对 `A (alpha)` 分量设置其相对百分比, 返回新颜色的颜色整数. |
| `colors.removeAlpha(color)` | 6.3.0 | 去除颜色的 `A (alpha)` 分量, 返回新颜色的颜色整数. |
| `colors.red(color)` | — | 获取颜色的 `R (red)` 分量, 取值范围 `[0..255]`. |
| `colors.red(color, options)` | — | 获取颜色的 `R (red)` 分量. |
| `colors.redDouble(color)` | 6.3.0 | 获取颜色的 `R (red)` 分量, 取值范围 `[0..1]`. |
| `colors.getRed(color)` | — | 获取颜色的 `R (red)` 分量, 取值范围 `[0..255]`. |
| `colors.getRed(color, options)` | — | 获取颜色的 `R (red)` 分量. |
| `colors.getRedDouble(color)` | 6.3.0 | 获取颜色的 `R (red)` 分量, 取值范围 `[0..1]`. |
| `colors.setRed(color, red)` | 6.3.0 | 设置颜色的 `R (red)` 分量, 返回新颜色的颜色整数. |
| `colors.setRedRelative(color, percentage)` | 6.3.1 | 针对 `R (red)` 分量设置其相对百分比, 返回新颜色的颜色整数. |
| `colors.removeRed(color)` | 6.3.0 | 去除颜色的 `R (red)` 分量, 返回新颜色的颜色整数. |
| `colors.green(color)` | — | 获取颜色的 `G (green)` 分量, 取值范围 `[0..255]`. |
| `colors.green(color, options)` | — | 获取颜色的 `G (green)` 分量. |
| `colors.greenDouble(color)` | 6.3.0 | 获取颜色的 `G (green)` 分量, 取值范围 `[0..1]`. |
| `colors.getGreen(color)` | — | 获取颜色的 `G (green)` 分量, 取值范围 `[0..255]`. |
| `colors.setGreenRelative(color, percentage)` | 6.3.1 | 针对 `G (green)` 分量设置其相对百分比, 返回新颜色的颜色整数. |
| `colors.getGreen(color, options)` | — | 获取颜色的 `G (green)` 分量. |
| `colors.getGreenDouble(color)` | 6.3.0 | 获取颜色的 `G (green)` 分量, 取值范围 `[0..1]`. |
| `colors.setGreen(color, green)` | 6.3.0 | 设置颜色的 `G (green)` 分量, 返回新颜色的颜色整数. |
| `colors.removeGreen(color)` | 6.3.0 | 去除颜色的 `G (green)` 分量, 返回新颜色的颜色整数. |
| `colors.blue(color)` | — | 获取颜色的 `B (blue)` 分量, 取值范围 `[0..255]`. |
| `colors.blue(color, options)` | — | 获取颜色的 `B (blue)` 分量. |
| `colors.blueDouble(color)` | 6.3.0 | 获取颜色的 `A (blue)` 分量, 取值范围 `[0..1]`. |
| `colors.getBlue(color)` | — | 获取颜色的 `B (blue)` 分量, 取值范围 `[0..255]`. |
| `colors.getBlue(color, options)` | — | 获取颜色的 `B (blue)` 分量. |
| `colors.getBlueDouble(color)` | 6.3.0 | 获取颜色的 `A (blue)` 分量, 取值范围 `[0..1]`. |
| `colors.setBlue(color, blue)` | 6.3.0 | 设置颜色的 `B (blue)` 分量, 返回新颜色的颜色整数. |
| `colors.setBlueRelative(color, percentage)` | 6.3.1 | 针对 `B (blue)` 分量设置其相对百分比, 返回新颜色的颜色整数. |
| `colors.removeBlue(color)` | 6.3.0 | 去除颜色的 `B (blue)` 分量, 返回新颜色的颜色整数. |
| `colors.rgb(color)` | — | 获取 `color` 参数对应的 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.rgb(red, green, blue)` | — | 通过 [颜色分量](dataTypes#colorcomponent) 获取 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.rgb(components)` | — | 通过 [颜色分量数组](dataTypes#colorcomponents) 获取 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.argb(colorHex)` | — | 获取 `colorHex` 颜色代码对应的 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.argb(alpha, red, green, blue)` | — | 通过 [颜色分量](dataTypes#colorcomponent) 获取 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.argb(components)` | — | 通过 [颜色分量数组](dataTypes#colorcomponents) 获取 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.rgba(colorHex)` | — | 获取 `colorHex` 颜色代码对应的 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.rgba(red, green, blue, alpha)` | — | 通过 [颜色分量](dataTypes#colorcomponent) 获取 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.rgba(components)` | — | 通过 [颜色分量数组](dataTypes#colorcomponents) 获取 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.hsv(hue, saturation, value)` | — | 通过 [颜色分量](dataTypes#colorcomponent) 获取 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.hsv(components)` | — | 通过 [颜色分量数组](dataTypes#colorcomponents) 获取 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.hsva(hue, saturation, value, alpha)` | — | 通过 [颜色分量](dataTypes#colorcomponent) 获取 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.hsva(components)` | — | 通过 [颜色分量数组](dataTypes#colorcomponents) 获取 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.hsl(hue, saturation, lightness)` | — | 通过 [颜色分量](dataTypes#colorcomponent) 获取 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.hsl(components)` | — | 通过 [颜色分量数组](dataTypes#colorcomponents) 获取 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.hsla(hue, saturation, lightness, alpha)` | — | 通过 [颜色分量](dataTypes#colorcomponent) 获取 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.hsla(components)` | — | 通过 [颜色分量数组](dataTypes#colorcomponents) 获取 [颜色整数 (ColorInt)](dataTypes#colorint). |
| `colors.toRgb(color)` | 6.2.0 | 获取颜色参数的 RGB [颜色分量数组](dataTypes#colorcomponents). |
| `colors.toRgba(color)` | — | 获取颜色参数的 RGBA [颜色分量数组](dataTypes#colorcomponents). |
| `colors.toRgba(color, options)` | — | 根据 `options` 选项参数获取颜色参数的 RGBA [颜色分量数组](dataTypes#colorcomponents). |
| `colors.toArgb(color)` | — | 获取颜色参数的 ARGB [颜色分量数组](dataTypes#colorcomponents). |
| `colors.toArgb(color, options)` | — | 根据 `options` 选项参数获取颜色参数的 ARGB [颜色分量数组](dataTypes#colorcomponents). |
| `colors.toHsv(color)` | — | 获取颜色参数的 HSV [颜色分量数组](dataTypes#colorcomponents). |
| `colors.toHsv(red, green, blue)` | — | 获取颜色参数的 HSV [颜色分量数组](dataTypes#colorcomponents). |
| `colors.toHsva(color)` | — | 获取颜色参数的 HSVA [颜色分量数组](dataTypes#colorcomponents). |
| `colors.toHsva(red, green, blue, alpha)` | — | 获取颜色参数的 HSVA [颜色分量数组](dataTypes#colorcomponents). |
| `colors.toHsl(color)` | — | 获取颜色参数的 HSL [颜色分量数组](dataTypes#colorcomponents). |
| `colors.toHsl(red, green, blue)` | — | 获取颜色参数的 HSL [颜色分量数组](dataTypes#colorcomponents). |
| `colors.toHsla(color)` | — | 获取颜色参数的 HSLA [颜色分量数组](dataTypes#colorcomponents). |
| `colors.toHsla(red, green, blue, alpha)` | — | 获取颜色参数的 HSLA [颜色分量数组](dataTypes#colorcomponents). |
| `colors.toRgbString(color)` | — | 将颜色转换为 RGB 字符串. 各分量会四舍五入并限制在 `0` 至 `255`. |
| `colors.toRgbString(red, green, blue)` | — | console.log(colors.toRgbString(102, 51, 153)); // rgb(102, 51, 153) |
| `colors.toRgbaString(color, options?)` | 6.6.2 | 将颜色转换为 RGBA 字符串. A 分量范围为 `0` 至 `1`, 保留 1 位小数后移除无意义的末尾 `0`. |
| `colors.toArgbString(color, options?)` | 6.6.2 | 将颜色转换为 ARGB 字符串. `options` 的布尔值或对象属性 `keepTrailingZeroForFullAlpha` 与 [toRgbaString()](#m-torgbastring) 相同. |
| `colors.toHsvString(color)` | — | 将颜色转换为 HSV 字符串. H 分量四舍五入为整数, S 和 V 分量使用百分比并保留至多 1 位小数. |
| `colors.toHsvString(red, green, blue)` | — | console.log(colors.toHsvString(102, 51, 153)); // hsv(270, 66.7%, 60%) |
| `colors.toHsvaString(color, options?)` | 6.6.2 | 将颜色转换为 HSVA 字符串. H, S 和 V 的格式与 [toHsvString()](#m-tohsvstring) 相同. `options` 的布尔值或对象属性 `keepTrailingZeroForFullAlpha` 与 [toRgbaString()](#m-torgbastring) 相同. |
| `colors.toHslString(color)` | — | 将颜色转换为 HSL 字符串. H 分量四舍五入为整数, S 和 L 分量使用百分比并保留至多 1 位小数. |
| `colors.toHslString(red, green, blue)` | — | console.log(colors.toHslString(102, 51, 153)); // hsl(270, 50%, 40%) |
| `colors.toHslaString(color, options?)` | 6.6.2 | 将颜色转换为 HSLA 字符串. H, S 和 L 的格式与 [toHslString()](#m-tohslstring) 相同. `options` 的布尔值或对象属性 `keepTrailingZeroForFullAlpha` 与 [toRgbaString()](#m-torgbastring) 相同. |
| `colors.isSimilar(colorA, colorB, threshold?, algorithm?)` | — | 判断两个颜色是否相似. |
| `colors.isSimilar(colorA, colorB, options)` | — | 判断两个颜色是否相似. |
| `colors.isEqual(colorA, colorB, alphaMatters?)` | — | 判断两个颜色是否相等, 比较时由 `alphaMatters` 参数决定是否考虑 `A (alpha)` 分量: |
| `colors.equals(colorA, colorB)` | — | 判断两个颜色是否相等, 比较时忽略 `A (alpha)` 分量: |
| `colors.luminance(color)` | 6.2.0 | 获取颜色的 [亮度 (Luminance)](glossaries#luminance), 取值范围 `[0..1]`. |
| `colors.distance(colorA, colorB, algorithm?)` | 6.8.0 | 按指定 [颜色检测算法](dataTypes#colordetectionalgorithm) 计算两个颜色的距离, 量纲与 [颜色匹配阈值](glossaries#颜色匹配阈值) 一致, 因此 `colors.isSimilar(a, b, threshold, algorithm)` 等价于 `colors.distance(a, b, algorithm) <= threshold`: |
| `colors.invert(color)` | 6.8.0 | 反转颜色的 `R (red)`, `G (green)`, `B (blue)` 分量, 保留 `A (alpha)` 分量: |
| `colors.blend(colorA, colorB, ratio?)` | 6.8.0 | 按权重线性混合两个颜色 (含 `A (alpha)` 分量), `ratio` 为 `0` 时结果为 `colorA`, 为 `1` 时结果为 `colorB`: |
| `colors.contrast(foreground, background)` | 6.8.0 | 按 [WCAG](https://www.w3.org/TR/WCAG21/#dfn-contrast-ratio) 公式计算前景色与背景色的对比度, 取值范围 `[1..21]`. 前景色带透明度时先与背景色合成; 背景色带透明度时抛出异常. |
| `toColorStateList(...color)` | 6.2.0 | 将一个或多个颜色参数转换为 ColorStateList 实例. |
| `colors.setPaintColor(paint, color)` | 6.2.0 | 方法 `setPaintColor` 用于解决在 `Android API 29 (10) [Q]` 及以上系统中 `Paint#setColor(color)` 无法正常设置画笔颜色的问题. |

### 控制台 (Console) — [原文](https://docs.autojs6.com/#/console)

| API | 版本 | 说明 |
| --- | --- | --- |
| `err(data, ...args)` | — | [console.error](#m-error) 的全局别名. |
| `openConsole(isReset?)` | — | [console.show](#m-show) 的全局别名. |
| `showConsole(isReset?)` | — | [console.show](#m-show) 的全局别名. |
| `clearConsole()` | — | [console.clear](#m-clear) 的全局别名. |
| `launchConsole()` | — | [console.launch](#m-launch) 的全局别名. |
| `show(isReset?)` | — | 显示控制台浮动窗口. |
| `isShowing()` | 6.3.0 | 返回控制台浮动窗口是否未处于隐藏状态. |
| `hide()` | — | 隐藏控制台浮动窗口. |
| `reset()` | 6.3.0 | 重置控制台浮动窗口的样式及空间状态, 恢复其默认值. |
| `collapse()` | 6.3.0 | 折叠显示控制台浮动窗口, 即最小化窗口. |
| `expand()` | 6.3.0 | 展开显示控制台浮动窗口. |
| `launch()` | 6.3.0 | 启动控制台 Activity 活动窗口. |
| `build(options)` | 6.3.0 | 构建控制台浮动窗口的配置. |
| `setSize(width, height)` | — | 设置控制台浮动窗口的尺寸. |
| `setPosition(x, y)` | — | 设置控制台浮动窗口的位置. |
| `setGravity(gravity)` | 6.6.0 | 按重力将控制台浮动窗口贴靠到屏幕指定位置. 字符串可使用 `left`, `top`, `right`, `bottom`, `start`, `end`, `center`, `center_horizontal` 和 `center_vertical`, 并可使用 `\|` 组合. |
| `setExitOnClose(exitOnClose?)` | — | 设置控制台浮动窗口在脚本结束时是否自动关闭. |
| `setExitOnClose(timeout)` | — | 设置控制台浮动窗口在脚本结束时自动关闭的超时时间, 单位为毫秒. |
| `setTouchable(touchable?)` | 6.5.0 | 设置控制台浮动窗口是否响应点击事件, 默认为 `true`. |
| `setTouchThrough(touchThrough?)` | 6.6.0 | 设置控制台浮动窗口是否让触摸事件穿透. 此方法与 [setTouchable](#m-settouchable) 的布尔值语义相反. |
| `setAvoidStatusBar(avoid?)` | 6.8.0 | 设置控制台浮动窗口是否避让状态栏, 默认为 `true`. |
| `setTimeVisible(visible?)` | 6.8.0 | 设置控制台浮动窗口是否在每条日志前显示时间前缀 (如 `12:34:56.789/D: `), 默认为 `false`. |
| `setTimeFormat(pattern?)` | 6.8.0 | 设置控制台浮动窗口时间前缀的格式, 默认为 `'HH:mm:ss.SSS'`. |
| `setColorful(colorful?)` | 6.8.0 | 设置控制台浮动窗口是否按日志等级 (verbose / log / info / warn / error) 使用不同的文本颜色, 默认为 `true`. |
| `setInputVisible(visible?)` | 6.8.0 | 设置控制台浮动窗口是否常驻显示输入栏, 默认为 `false`. |
| `setTitle(title)` | — | 设置控制台浮动窗口的标题文本. |
| `setTitleTextSize(size)` | 6.3.0 | 设置控制台浮动窗口的标题文本字体大小, 单位 `sp`. |
| `setTitleTextColor(color)` | 6.3.0 | 设置控制台浮动窗口的标题文本字体颜色. |
| `setTitleBackgroundColor(color)` | 6.3.0 | 设置控制台浮动窗口的标题显示区域背景颜色. |
| `setTitleBackgroundTint(color)` | 6.6.0 | 设置控制台浮动窗口的标题显示区域背景着色, 保留原有透明度值. |
| `setTitleBackgroundAlpha(alpha)` | 6.3.0 | 设置控制台浮动窗口的标题显示区域背景颜色透明度. |
| `resetTitleBackgroundAlpha()` | 6.6.0 | 重置控制台浮动窗口的标题显示区域背景颜色透明度, 默认值为 `0.8`. |
| `setTitleIconsTint(color)` | 6.3.0 | 设置控制台浮动窗口的操作按钮着色. |
| `setContentTextSize(size: number)` | 6.3.0 | 设置控制台浮动窗口的日志文本字体大小, 单位 `sp`. |
| `setContentTextColor(colorMap)` | — | 设置控制台浮动窗口的日志文本字体颜色, 按日志等级设置一个或多个不同的字体颜色. |
| `setContentTextColor(color)` | — | 设置控制台浮动窗口的日志文本字体的统一颜色. |
| `setContentTextColors(...colors)` | 6.6.0 | [setContentTextColor](#m-setcontenttextcolor) 的兼容别名. 传入多个颜色时依次对应 `verbose`, `log`, `info`, `warn`, `error` 和 `assert`. |
| `setContentBackgroundColor(color)` | 6.3.0 | 设置控制台浮动窗口的日志显示区域背景颜色. |
| `setContentBackgroundTint(color)` | 6.6.0 | 设置日志显示区域背景着色并保留原有透明度. 传入 `null` 可清除着色. |
| `setContentBackgroundAlpha(alpha)` | 6.3.0 | 设置控制台浮动窗口的日志显示区域背景颜色透明度. |
| `resetContentBackgroundAlpha()` | 6.6.0 | 重置控制台浮动窗口的日志显示区域背景颜色透明度, 默认值为 `0.6`. |
| `setTextSize(size)` | 6.3.0 | 设置控制台浮动窗口的标题及日志文本字体大小, 单位 `sp`. |
| `setTextColor(color)` | 6.3.0 | 设置控制台浮动窗口的标题及日志文本字体颜色. |
| `setBackgroundColor(color)` | 6.3.0 | 设置控制台浮动窗口的标题及日志显示区域背景颜色. |
| `setBackgroundTint(color)` | 6.6.0 | 同时设置标题和日志显示区域的背景着色, 并分别保留原有透明度. |
| `setBackgroundAlpha(alpha)` | 6.3.0 | 设置控制台浮动窗口的标题及日志显示区域背景颜色透明度. |
| `resetBackgroundAlpha()` | 6.6.0 | 重置控制台浮动窗口的标题及日志显示区域背景颜色透明度, 分别为 `0.8` 及 `0.6`. |
| `verbose(data, ...args)` | — | 输出参数内容到控制台. |
| `log(data, ...args)` | — | 输出参数内容到控制台. |
| `info(data, ...args)` | — | 输出参数内容到控制台. |
| `warn(data, ...args)` | — | 输出参数内容到控制台. |
| `error(data, ...args)` | — | 输出参数内容到控制台. |
| `assert(bool, message?)` | — | 断言 `bool` 参数为真. |
| `assert(func, message?)` | — | 断言 `func` 参数的执行结果为真. |
| `clear()` | 6.3.0 | 清空控制台日志内容. |
| `print(data, ...args)` | — | 等效于 [console.log](#m-log). |
| `printAllStackTrace(e)` | 6.3.0 | 在控制台打印详细的栈追踪信息. |
| `trace(message, level?)` | 6.3.0 | 输出当前位置调用栈的追踪信息到控制台. |
| `time(label?)` | — | 启动计时器, 用以计算以 `label` 参数标记的时间间隔. |
| `timeEnd(label?)` | — | 与 console.time 配合使用, 用以计算以 `label` 参数标记的时间间隔. |
| `setGlobalLogConfig(config)` | — | 设置将全局日志写入文件的配置选项. |
| `resetGlobalLogConfig()` | 6.3.1 | 重置全局日志写入配置. |
| `input(data?, ...args)` | 6.8.0 | 读取用户在控制台输入栏提交的一行文本, 并将其作为 JavaScript 表达式求值后返回. |
| `rawInput(data?, ...args)` | 6.8.0 | 读取用户在控制台输入栏提交的一行文本并原样返回. |

### 续体 (Continuation) — [原文](https://docs.autojs6.com/#/continuation)

| API | 版本 | 说明 |
| --- | --- | --- |
| `continuation.create(scope?)` | — | 创建续体控制对象. 省略 **scope** 时使用当前脚本作用域. |

### 单位转换 (Converter) — [原文](https://docs.autojs6.com/#/cvt)

| API | 版本 | 说明 |
| --- | --- | --- |
| `cvt.bytes(source)` | — | 将 `source` 转换为自动选择的目标单位. |
| `cvt.bytes(source, toUnit)` | — | 将 `source` 转换为 `toUnit`. |
| `cvt.bytes(source, options)` | — | 使用选项转换 `source`. |
| `cvt.bytes(source, toUnit, options)` | — | 将 `source` 转换为 `toUnit`, 并应用 `options`. |
| `cvt.bytes(source, fromUnit, toUnit, options?)` | — | 将数值从 `fromUnit` 转换为 `toUnit`. |

### 密文 (Crypto) — [原文](https://docs.autojs6.com/#/crypto)

| API | 版本 | 说明 |
| --- | --- | --- |
| `crypto.digest(message, algorithm)` | — | 获取 `消息 (message)` 经指定 `算法 (algorithm)` 计算后的 `消息摘要 (message digest)`. |
| `crypto.digest(message)` | — | 获取 `消息 (message)` 的 MD5 `消息摘要 (message digest)`. |
| `crypto.digest(message, algorithm, options)` | — | 获取 `消息 (message)` 经指定 `算法 (algorithm)` 计算后的 `消息摘要 (message digest)`. |
| `crypto.digest(message, options)` | — | 获取 `消息 (message)` 的 MD5 `消息摘要 (message digest)`. |
| `crypto.encrypt(data, key, transformation, options?)` | — | 数据加密. |
| `crypto.decrypt(data, key, transformation, options?)` | — | 数据解密. |
| `crypto.generateKeyPair(algorithm)` | — | 使用安全提供程序为指定算法选择的默认参数生成随机密钥对. |
| `crypto.generateKeyPair(algorithm, length)` | — | 使用指定算法及密钥长度生成随机密钥对. |
| `crypto(data, options?)` | — | 生成一个自定义密钥. |
| `crypto(publicKeyData, privateKeyData, options?)` | — | 生成一个固定密钥对. |

### 设备 (Device) — [原文](https://docs.autojs6.com/#/device)

| API | 版本 | 说明 |
| --- | --- | --- |
| `device.summary()` | — | 返回 Android 构建版本, 屏幕分辨率, 品牌, 制造商, 设备名称, 型号, 产品名称, 硬件名称, 序列号, IMEI 和 ABI 等信息. |
| `device.digest()` | — | 返回以下 3 组信息, 并使用 `" / "` 分隔: |
| `device.getMarketName()` | 6.8.0 | 按以下系统属性的顺序返回第一个非空值: |
| `device.getIMEI()` | — | 返回 [imei](#p-imei). 此方法不会申请 `android.permission.READ_PHONE_STATE`. |
| `device.getSerial()` | — | 返回 [serial](#p-serial). Android 不允许当前应用读取时返回 `null`. |
| `device.getAndroidId()` | — | 返回 `Settings.Secure.ANDROID_ID`. Android 8.0 及以上系统通常按应用签名密钥, 用户和设备确定此值. 系统未提供值时返回 `null`. |
| `device.getSharedDeviceId()` | 6.7.0 | 从 AutoJs6 的设备 ID 内容提供器读取持久化 UUID. AutoJs6 及获准访问同一提供器的应用可取得相同结果, 且不需要读取 IMEI 或硬件序列号. 查询未返回数据时返回 `null`. |
| `device.getMacAddress()` | — | 依次尝试 Wi-Fi 连接信息, `wlan0` 网络接口和 `/sys/class/net/wlan0/address`. 无可用结果时返回 `null`. |
| `device.isManufacturer(manufacturer)` | 6.4.0 | 去除参数首尾空白后, 与 `Build.MANUFACTURER` 进行不区分大小写的完整匹配. |
| `device.hasReadPhoneStatePermission()` | 6.4.0 | 检查当前应用的 `android.permission.READ_PHONE_STATE` 是否为已授予状态. |
| `device.ensureReadPhoneStatePermission()` | 6.4.0 | 已授予 `android.permission.READ_PHONE_STATE` 时直接返回, 否则抛出 `SecurityException`. |
| `device.getBrightness()` | — | 读取 `Settings.System.SCREEN_BRIGHTNESS`. Android 通常使用 `0` 到 `255` 的范围. 设置项不存在时抛出 `Settings.SettingNotFoundException`. |
| `device.getBrightnessMode()` | — | 设置项不存在时抛出 `Settings.SettingNotFoundException`. |
| `device.setBrightness(brightness)` | — | 将参数写入 `Settings.System.SCREEN_BRIGHTNESS`. 源码不对参数范围进行校验. 当前处于自动亮度模式时, 写入值不一定立即成为屏幕的实际亮度. |
| `device.setBrightnessMode(mode)` | — | 将参数写入 `Settings.System.SCREEN_BRIGHTNESS_MODE`. 源码不对参数值进行校验. |
| `device.getMusicVolume()` | — | 当前媒体音量 |
| `device.getNotificationVolume()` | — | 当前通知音量 |
| `device.getAlarmVolume()` | — | 当前闹钟音量 |
| `device.getMusicMaxVolume()` | — | 媒体音量上限 |
| `device.getNotificationMaxVolume()` | — | 通知音量上限 |
| `device.getAlarmMaxVolume()` | — | 闹钟音量上限 |
| `device.setMusicVolume(volume)` | — | 此方法需要系统的 "修改系统设置" 权限. 可使用 [getMusicMaxVolume()](#getmusicmaxvolume) 查询设备支持的上限. |
| `device.setNotificationVolume(volume)` | — | 此方法需要系统的 "修改系统设置" 权限. 可使用 [getNotificationMaxVolume()](#getnotificationmaxvolume) 查询设备支持的上限. |
| `device.setAlarmVolume(volume)` | — | 此方法需要系统的 "修改系统设置" 权限. 可使用 [getAlarmMaxVolume()](#getalarmmaxvolume) 查询设备支持的上限. |
| `device.getBattery()` | — | 根据系统电池广播中的电量和满量程计算百分比, 并保留 1 位小数. 无法取得电池广播时返回 `-1`. |
| `device.isCharging()` | — | 电池状态为 `BATTERY_STATUS_CHARGING` 或 `BATTERY_STATUS_FULL` 时返回 `true`. |
| `device.isPowerSourceAC()` | 6.3.2 | 只有 [isCharging()](#ischarging) 为 `true`, 且系统报告 `BATTERY_PLUGGED_AC` 时返回 `true`. |
| `device.isPowerSourceUSB()` | 6.3.2 | 只有 [isCharging()](#ischarging) 为 `true`, 且系统报告 `BATTERY_PLUGGED_USB` 时返回 `true`. |
| `device.isPowerSourceWireless()` | 6.3.2 | 只有 [isCharging()](#ischarging) 为 `true`, 且系统报告 `BATTERY_PLUGGED_WIRELESS` 时返回 `true`. |
| `device.isPowerSourceDock()` | — | Android API 33 及以上系统中, 只有 [isCharging()](#ischarging) 为 `true`, 且系统报告 `BATTERY_PLUGGED_DOCK` 时返回 `true`. 更低版本固定返回 `false`. |
| `device.getTotalMem()` | — | 设备内存总量, 单位为字节 |
| `device.getAvailMem()` | — | 当前可用内存, 单位为字节 |
| `device.getRotation()` | 6.4.0 | 返回值与 [rotation](#p-rotation) 相同. |
| `device.getOrientation()` | 6.4.0 | 返回值与 [orientation](#p-orientation) 相同. |
| `device.isScreenOn()` | — | 仅当默认显示设备状态为 `Display.STATE_ON` 时返回 `true`. 息屏显示等不可交互状态不属于此处的点亮状态. |
| `device.isScreenOff()` | — | 返回 `!device.isScreenOn()` 的结果. |
| `device.isScreenPortrait()` | 6.4.0 | 屏幕旋转状态为 `Surface.ROTATION_0` 或 `Surface.ROTATION_180` 时返回 `true`. |
| `device.isScreenLandscape()` | 6.4.0 | 屏幕旋转状态为 `Surface.ROTATION_90` 或 `Surface.ROTATION_270` 时返回 `true`. |
| `device.wakeUp()` | — | 使用带 `ACQUIRE_CAUSES_WAKEUP` 的亮屏唤醒锁保持屏幕点亮约 `200` 毫秒, 以唤醒设备. |
| `device.wakeUpIfNeeded()` | — | 仅当 [isScreenOn()](#isscreenon) 返回 `false` 时调用 [wakeUp()](#wakeup). |
| `device.keepAwake(flags)` | — | 使用指定标志创建并无限期持有唤醒锁, 直到调用 [cancelKeepingAwake()](#cancelkeepingawake) 或以不同标志再次获取唤醒锁. |
| `device.keepAwake(flags, timeout)` | — | 使用指定标志创建并在限定时间内持有唤醒锁. |
| `device.keepScreenOn(timeout?)` | — | 使用 `SCREEN_BRIGHT_WAKE_LOCK ``\| ACQUIRE_CAUSES_WAKEUP` 保持屏幕点亮. 省略 `timeout` 时持续持有, 直到主动取消. |
| `device.keepScreenDim(timeout?)` | — | 使用 `SCREEN_DIM_WAKE_LOCK ``\| ACQUIRE_CAUSES_WAKEUP` 保持设备唤醒, 同时允许屏幕变暗. 省略 `timeout` 时持续持有, 直到主动取消. |
| `device.cancelKeepingAwake()` | — | 释放当前由 device 模块持有的唤醒锁. 没有正在持有的唤醒锁时不执行操作. |
| `device.vibrate(millis)` | — | 使设备振动一次. |
| `device.vibrate(timings)` | — | 模式数组从等待时长开始, 后续元素在等待和振动之间交替. |
| `device.vibrate(off, millis)` | — | — |
| `device.vibrate(timingsWithoutOff, off)` | — | 相当于 `device.vibrate([ off ].concat(timingsWithoutOff))`. |
| `device.vibrate(morseCode, delay?)` | — | 振动调用会立即返回, 振动由 Android 系统继续执行. 应用需要声明 `android.permission.VIBRATE`. |
| `device.cancelVibration()` | — | 取消 device 模块使用的系统振动器当前正在执行的振动. |
| `device.getIpAddress(useIPv4?)` | — | 扫描网络接口并返回第一个非回环地址. `useIPv4` 为 `true`, `null` 或 `undefined` 时返回 IPv4 地址, 为 `false` 时返回 IPv6 地址. IPv6 地址使用大写形式并移除区域标识. 无可用地址时返回 `"0.0.0.0"`. |
| `device.getIpv6Address()` | — | 相当于 `device.getIpAddress(false)`. |
| `device.getGatewayAddress()` | — | 返回当前 Wi-Fi DHCP 配置中的网关地址. 无可用地址时返回 `"0.0.0.0"`. |
| `device.isActiveNetworkMetered()` | — | 当前活动网络是否按流量计费 |
| `device.isConnectedOrConnecting()` | — | 活动网络是否已连接或正在连接 |
| `device.isWifiAvailable()` | — | 此结果表示正在使用的网络类型, 不表示 Wi-Fi 适配器是否已启用. |
| `device.setPointerLocation(enabled)` | 6.7.0 | 当前状态已经符合目标时直接返回 `true`. 需要修改时依次尝试 Root 和 Shizuku 命令, 无可用权限或修改后的状态校验失败时返回 `false`. |
| `device.setPointerLocationEnabled()` | 6.7.0 | 相当于 `device.setPointerLocation(true)`. |
| `device.setPointerLocationDisabled()` | 6.7.0 | 相当于 `device.setPointerLocation(false)`. |
| `device.isPointerLocationEnabled()` | 6.7.0 | 依次尝试系统设置接口, Root 命令和 Shizuku 命令读取状态. |
| `device.isPointerLocationDisabled()` | 6.7.0 | 依次尝试系统设置接口, Root 命令和 Shizuku 命令读取状态. 所有读取方式均失败时, 当前实现按禁用状态处理. |
| `device.togglePointerLocation()` | 6.7.0 | 读取当前状态并切换系统指针位置显示. 需要修改时依次尝试 Root 和 Shizuku 命令. |

### 对话框 (Dialogs) — [原文](https://docs.autojs6.com/#/dialogs)

| API | 版本 | 说明 |
| --- | --- | --- |
| `dialogs.alert(title, content?, callback?)` | — | 显示只有确定按钮的提示对话框. |
| `dialogs.confirm(title, content?, callback?)` | — | 显示带确定和取消按钮的对话框. |
| `dialogs.rawInput(title, prefill?, callback?)` | — | 显示输入对话框. |
| `dialogs.input(title, prefill?, callback?)` | — | 与 [rawInput](#m-rawinput) 的交互方式相同, 但会在当前顶层作用域中对输入字符串执行 `eval` 并返回求值结果. |
| `dialogs.prompt(title, prefill?, callback?)` | — | [rawInput](#m-rawinput) 的别名. |
| `dialogs.select(title, items, callback?)` | — | 选中索引, 结果 Promise 或 `null` |
| `select(title, ...items)` | — | 显示列表选择对话框. 取消时返回 `-1`. |
| `dialogs.singleChoice(title, items, index?, callback?)` | — | 显示单选列表对话框. 取消时返回 `-1`. 同步, Promise 和回调模式与 [select](#m-select) 相同. |
| `dialogs.multiChoice(title, items, indices?, callback?)` | — | 显示多选列表对话框. |
| `dialogs.build(properties?)` | — | 创建一个可自定义的对话框, 例如: |
| `dialogs.selectFile(title, prefill, callback)` | — | 显示文本输入对话框. 当前实现不会打开文件选择器, 行为与底层 `dialogs.rawInput` 相同, 但不提供 UI 线程 Promise 适配. |
| `dialogs.newBuilder()` | — | 创建使用浅色主题的底层 `JsDialogBuilder`. 此方法不应用 [dialogs.build](#m-build) 的属性名称转换, 默认预设或 UI 线程包装. 常规脚本应优先使用 `dialogs.build(properties?)`. |
| `dialog.getProgress()` | — | 获取当前进度条的进度值, 是一个整数 |
| `dialog.getMaxProgress()` | — | 获取当前进度条的最大进度值, 是一个整数 |
| `dialog.getActionButton(action)` | — | — |

### 引擎 (Engines) — [原文](https://docs.autojs6.com/#/engines)

| API | 版本 | 说明 |
| --- | --- | --- |
| `engines.execScript(name, script, config?)` | — | 在新引擎中执行字符串源码. |
| `engines.execScriptFile(path, config?)` | — | 在新引擎中执行本地脚本文件. 相对路径按当前脚本运行时路径解析. |
| `engines.execAutoFile(path, config?)` | — | 以自动化脚本模式在新引擎中执行 JavaScript 文件. 此方法保留 JavaScript 和原有自动化脚本语义, 并在执行前确保无障碍服务已启动. |
| `engines.myEngine()` | — | 当前脚本引擎 |
| `engines.all()` | — | 当前存在的脚本引擎数组 |
| `engines.getEngines()` | — | `getEngines` 返回 Java 集合. 需要 JavaScript 数组时使用 [all](#m-all). |
| `engines.stopAll()` | — | 停止全部脚本引擎, 包括当前引擎. |
| `engines.stopAllAndToast()` | — | 停止全部脚本引擎. 停止数量大于 `0` 时显示提示消息. |
| `engines.getEngine()` | — | 此次执行使用的脚本引擎 |
| `engines.getSource()` | — | 脚本源对象 |
| `engines.getConfig()` | — | 执行配置 |
| `engines.getListener()` | — | 执行监听器 |
| `engines.getId()` | — | 脚本引擎对象. `engines.myEngine()` 返回当前 JavaScript 引擎. |
| `engines.forceStop()` | — | 强制停止此脚本引擎. |
| `engines.cwd()` | — | 工作目录 |
| `engines.getStartTime()` | 6.7.0 | 返回 Unix 时间戳, 单位为毫秒. 无法取得启动时间时返回 `0`. |
| `engines.getSourcePath()` | — | 脚本来源路径 |
| `engines.getSourceUri()` | — | 脚本来源 URI |
| `engines.getSourceDirectory()` | — | 脚本来源目录或工作目录 |
| `engines.getExecArgv()` | — | 在 JavaScript 中也可通过属性 `engine.execArgv` 读取. |
| `engines.isDestroyed()` | — | 引擎是否已销毁 |
| `engines.hasFeature(feature)` | — | 当前脚本是否启用该功能 |
| `emit(eventName, ...args)` | — | 向此引擎的 [events](events) 模块异步发送事件. |
| `engines.getArgument(key)` | — | 参数值 |
| `engines.setArgument(key, value)` | — | — |

### 事件监听 (Events) — [原文](https://docs.autojs6.com/#/events)

| API | 版本 | 说明 |
| --- | --- | --- |
| `emit(eventName, ...args)` | — | 向当前进程的全部已注册脚本广播事件, 包括发送方自身. |
| `broadcast.unregister()` | — | 从进程内广播列表注销当前发射器. 注销后仍可发送广播, 但不再接收广播. 脚本运行时回收时会自动调用此方法. |
| `events.emitter()` | — | 返回一个新的 [EventEmitter](#events_eventemitter). 这个 EventEmitter 没有内置任何事件. |
| `events.observeKey()` | — | 启用按键监听, 例如音量键, Home 键. 按键监听使用无障碍服务实现, 如果无障碍服务未启用会抛出异常并提示开启. |
| `events.onKeyDown(keyName, listener)` | — | 注册一个按键监听函数, 当有 keyName 对应的按键被按下会调用该函数. 可用的按键名称参见 [Keys](keys). |
| `events.onKeyUp(keyName, listener)` | — | 注册一个按键监听函数, 当有 keyName 对应的按键弹起会调用该函数. 可用的按键名称参见 [Keys](keys). |
| `events.onceKeyDown(keyName, listener)` | — | 注册一个按键监听函数, 当有 keyName 对应的按键被按下时会调用该函数, 之后会注销该按键监听器. |
| `events.onceKeyUp(keyName, listener)` | — | 注册一个按键监听函数, 当有 keyName 对应的按键弹起时会调用该函数, 之后会注销该按键监听器. |
| `events.removeAllKeyDownListeners(keyName)` | — | 删除该按键的 KeyDown (按下) 事件的所有监听. |
| `events.removeAllKeyUpListeners(keyName)` | — | 删除该按键的 KeyUp (弹起) 事件的所有监听. |
| `events.setKeyInterceptionEnabled([key, ]enabled)` | — | 设置按键屏蔽是否启用. 所谓按键屏蔽指的是, 屏蔽原有按键的功能, 例如使得音量键不再能调节音量, 但此时仍然能通过按键事件监听按键. |
| `events.observeTouch()` | — | 启用屏幕触摸监听 (需要 root 权限). |
| `events.setTouchEventTimeout(timeout)` | — | 设置两个触摸事件分发的最小时间间隔. |
| `events.getTouchEventTimeout()` | — | 返回触摸事件的最小时间间隔. |
| `events.onTouch(listener)` | — | 注册一个触摸监听函数. 相当于 `on("touch", listener)`. |
| `events.removeAllTouchListeners()` | — | 删除所有事件监听函数. |
| `events.observeNotification()` | — | 开启通知监听. 例如 QQ 消息, 微信消息, 推送等通知. |
| `events.observeToast()` | — | 开启 Toast 监听. |
| `Notification.getPackageName()` | — | 获取发出通知的应用包名. |
| `Notification.getTitle()` | — | 获取通知的标题. |
| `Notification.getText()` | — | 获取通知的内容. |
| `Notification.click()` | — | 点击该通知. 例如对于一条 QQ 消息, 点击会进入具体的聊天界面. |
| `Notification.delete()` | — | 删除该通知. 该通知将从通知栏中消失. |
| `KeyEvent.getAction()` | — | 返回事件的动作. 包括: |
| `KeyEvent.getKeyCode()` | — | 返回按键的键值. 包括: |
| `KeyEvent.getEventTime()` | — | 返回事件发生的时间戳. |
| `KeyEvent.getDownTime()` | — | 返回最近一次按下事件的时间戳. 如果本身是按下事件, 则与 `getEventTime()` 相同. |
| `KeyEvent.keyCodeToString(keyCode)` | — | 把键值转换为字符串. 例如 KEYCODE\_HOME 转换为 "KEYCODE\_HOME". |

### 文件 (Files) — [原文](https://docs.autojs6.com/#/files)

| API | 版本 | 说明 |
| --- | --- | --- |
| `files.join(parent, ...child)` | — | 拼接父路径和任意数量的子路径. |
| `files.toFile(path)` | 6.6.0 | 将路径解析为绝对路径并创建对应的 `java.io.File` 对象. |
| `files.cwd()` | — | 返回脚本引擎的当前工作目录. |
| `files.path(relativePath?)` | — | 相对于 [files.cwd](#files_files_cwd) 解析路径. 绝对路径原样返回. |
| `files.isFile(path)` | — | 返回路径 path 是否是文件. |
| `files.isDir(path)` | — | 返回路径 path 是否是文件夹. |
| `files.isEmptyDir(path)` | — | 返回文件夹 path 是否为空文件夹. 如果该路径并非文件夹, 则直接返回 `false`. |
| `files.create(path)` | — | 创建一个文件或文件夹并返回是否创建成功. 如果文件已经存在, 则直接返回 `false`. |
| `files.createWithDirs(path)` | — | 创建一个文件或文件夹并返回是否创建成功. 如果文件所在文件夹不存在, 则先创建他所在的一系列文件夹. 如果文件已经存在, 则直接返回 `false`. |
| `files.exists(path)` | — | 返回在路径 path 处的文件是否存在. |
| `files.ensureDir(path)` | — | 确保路径 path 所在的文件夹存在. 如果该路径所在文件夹不存在, 则创建该文件夹. |
| `files.read(path[, encoding = "utf-8"])` | — | 读取文本文件 path 的所有内容并返回. 如果文件不存在, 则抛出 `FileNotFoundException`. |
| `files.readBytes(path)` | — | 读取文件 path 的所有内容并返回一个字节数组. 如果文件不存在, 则抛出 `FileNotFoundException`. |
| `files.write(path, text[, encoding = "utf-8"])` | — | 把 text 写入到文件 path 中. 如果文件存在则覆盖, 不存在则创建. |
| `files.writeBytes(path, bytes)` | — | 把 bytes 写入到文件 path 中. 如果文件存在则覆盖, 不存在则创建. |
| `files.append(path, text[, encoding = 'utf-8'])` | — | 把 text 追加到文件 path 的末尾. 如果文件不存在则创建. |
| `files.appendBytes(path, text[, encoding = 'utf-8'])` | — | 把 bytes 追加到文件 path 的末尾. 如果文件不存在则创建. |
| `files.copy(fromPath, toPath)` | — | 复制文件, 返回是否复制成功. 例如 `files.copy("/sdcard/1.txt", "/sdcard/Download/1.txt")`. |
| `files.move(fromPath, toPath)` | — | 移动文件, 返回是否移动成功. 例如 `files.move("/sdcard/1.txt", "/sdcard/Download/1.txt")` 会把 1.txt 文件从 sd 卡根目录移动到 Download 文件夹. |
| `files.rename(path, newName)` | — | 重命名文件, 并返回是否重命名成功. 例如 `files.rename("/sdcard/1.txt", "2.txt")`. |
| `files.renameWithoutExtension(path, newName)` | — | 重命名文件, 不包含拓展名, 并返回是否重命名成功. 例如 `files.rename("/sdcard/1.txt", "2")` 会把 "1.txt" 重命名为 "2.txt". |
| `files.getName(path)` | — | 返回文件的文件名. 例如 `files.getName("/sdcard/1.txt")` 返回 "1.txt". |
| `files.getNameWithoutExtension(path)` | — | 返回不含拓展名的文件的文件名. 例如 `files.getName("/sdcard/1.txt")` 返回 "1". |
| `files.getExtension(path)` | — | 返回文件的拓展名. 例如 `files.getExtension("/sdcard/1.txt")` 返回 "txt". |
| `files.remove(path)` | — | 删除文件或 **空文件夹**, 返回是否删除成功. |
| `files.removeDir(path)` | — | 删除文件夹, 如果文件夹不为空, 则删除该文件夹的所有内容再删除该文件夹, 返回是否全部删除成功. |
| `files.getSdcardPath()` | — | 返回 SD 卡路径. 所谓 SD 卡, 即外部存储器. |
| `files.listDir(path[, filter])` | — | 列出文件夹 path 下的满足条件的文件和文件夹的名称的数组. 如果不加 filter 参数, 则返回所有文件和文件夹. |
| `open(path[, mode = "r", encoding = "utf-8", bufferSize = 8192])` | — | 目前暂不支持二进制模式, 随机读写模式. |
| `ReadableTextFile.read()` | — | 返回该文件剩余的所有内容的字符串. |
| `ReadableTextFile.read(maxCount)` | — | 读取该文件接下来最长为 maxCount 的字符串并返回. 即使文件剩余内容不足 maxCount 也不会出错. |
| `ReadableTextFile.readline()` | — | 读取一行并返回 (不包含换行符). |
| `ReadableTextFile.readlines()` | — | 读取剩余的所有行, 并返回它们按顺序组成的字符串数组. |
| `close()` | — | 关闭该文件. |
| `PWritableTextFile.write(text)` | — | 把文本内容 text 写入到文件中. |
| `PWritableTextFile.writeline(line)` | — | 把文本 line 写入到文件中并写入一个换行符. |
| `PWritableTextFile.writelines(lines)` | — | 把很多行写入到文件中.... |
| `PWritableTextFile.flush()` | — | 把缓冲区内容输出到文件中. |
| `PWritableTextFile.close()` | — | 关闭文件. 同时会被缓冲区内容输出到文件. |

### 悬浮窗 (Floaty) — [原文](https://docs.autojs6.com/#/floaty)

| API | 版本 | 说明 |
| --- | --- | --- |
| `floaty.window(xml)` | — | 根据布局创建并显示一个带关闭, 调整大小和调整位置控件的悬浮窗. 可调用 `setAdjustEnabled()` 显示或隐藏调整控件. |
| `floaty.rawWindow(xml)` | — | 根据布局创建并显示原始悬浮窗. 与 [floaty.window](#m-window) 不同, 此方法不附加关闭或调整控件, 并支持覆盖状态栏的全屏布局. |
| `floaty.getClip(maxDelayAfterWindowReady?)` | 6.6.0 | 借助临时原始悬浮窗取得窗口焦点后读取剪贴板文本. 首次结果为空时, 在 `maxDelayAfterWindowReady` 时间内每隔约 `10` 毫秒重试. 临时窗口在读取时关闭. |
| `floaty.hasPermission()` | — | 检查当前应用是否可以在其他应用上层显示窗口. |
| `floaty.checkPermission()` | 6.8.0 | [floaty.hasPermission](#floaty-haspermission) 的兼容别名. |
| `floaty.requestPermission()` | — | 打开系统悬浮窗权限设置页面. |
| `floaty.ensurePermission()` | — | 确保当前应用已取得悬浮窗权限. 权限缺失时打开系统设置页面. |
| `floaty.closeAll()` | — | 关闭所有本脚本的悬浮窗. |
| `window.setAdjustEnabled(enabled)` | — | 如果 enabled 为 true, 则在悬浮窗左上角, 右上角显示可供位置, 大小调整的标示, 就像控制台一样; |
| `window.setPosition(x, y)` | — | 设置悬浮窗位置. |
| `window.getX()` | — | 返回悬浮窗位置的 X 坐标. |
| `window.getY()` | — | 返回悬浮窗位置的 Y 坐标. |
| `window.setSize(width, height)` | — | 设置悬浮窗宽高. |
| `window.getWidth()` | — | 返回悬浮窗宽度. |
| `window.getHeight()` | — | 返回悬浮窗高度. |
| `window.close()` | — | 关闭悬浮窗. 如果悬浮窗已经是关闭状态, 则此函数将不执行任何操作. |
| `window.exitOnClose()` | — | 使悬浮窗被关闭时自动结束脚本运行. |
| `window.setTouchable(touchable)` | — | 设置悬浮窗是否可触摸, 如果为 true, 则悬浮窗将接收到触摸, 点击等事件并且无法继续传递到悬浮窗下面; 如果为 false, 悬浮窗上的触摸, 点击等事件将被直接传递到悬浮窗下面. 处于安全考虑, 被悬浮窗接收的触摸事情无法再继续传递到下层. |

### 流程 (Flow) — [原文](https://docs.autojs6.com/#/flow)

| API | 版本 | 说明 |
| --- | --- | --- |
| `flow.wait(cond, timeout?, interval?, onOk?, onErr?)` | — | 创建一个等待 `cond` 的 Flow, 立即返回, 不阻塞当前线程. |
| `flow.wait(cond, options, onOk?, onErr?)` | — | 以选项对象形式创建等待. |
| `flow.waitUntil(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `flow.waitUntil(cond, options, onOk?, onErr?)` | — | 与 [flow.wait](#m-wait) 完全相同, 用于函数条件时语义更清晰. |
| `flow.waitWhile(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `flow.waitWhile(cond, options, onOk?, onErr?)` | — | 等待条件 **不再** 满足. 条件从满足变为不满足 (或一开始就不满足) 时以 `true` fulfill, 超时则以 `TIMEOUT` 拒绝. |
| `flow.waitForStable(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `flow.waitForStable(cond, options, onOk?, onErr?)` | — | 等待目标出现并保持稳定: 目标连续 `stableFor` 毫秒 (默认 0) 无变化时以目标 fulfill. 默认值表示首次匹配即完成, 需要连续观察时显式设置正数. |
| `flow.whenPresent(cond, handler, options?)` | — | 在检测期内未出现目标时跳过处理函数. 出现目标时以匹配结果调用 `handler`, 等待其返回的 Flow 或 Promise 完成; 普通返回值不改变链上的值. [链式形式](flowType#m-whenpresent) 始终透传进入本步骤时的值. |
| `flow.repeatUntil(action, cond, options?)` | — | 首次动作前检查条件, 已满足时不执行动作. 否则执行 `action(attempt)`, 等待其返回的 Flow 或 Promise 完成, 再检查条件. 后续动作前按 `interval` 暂停并再次检查条件. 工厂函数每轮重新调用, 应返回本轮新建的子流程. |
| `flow.waitForVisible(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `flow.waitForVisible(cond, options, onOk?, onErr?)` | — | 等待目标出现, 对用户可见 ([visibleToUser](uiObjectType#m-visibletouser)) 且保持稳定 `stableFor` 毫秒. |
| `flow.waitForHidden(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `flow.waitForHidden(cond, options, onOk?, onErr?)` | — | 等待目标消失: 目标连续 `stableFor` 毫秒不存在或不可见时 fulfill. |
| `flow.waitForActivity(activityName, timeout?, interval?, onOk?, onErr?)` | — | — |
| `flow.waitForActivity(activityName, options, onOk?, onErr?)` | — | 等待 [currentActivity()](global#m-currentactivity) 变为 `activityName`, 满足时以 `true` fulfill. |
| `flow.waitForPackage(packageName, timeout?, interval?, onOk?, onErr?)` | — | — |
| `flow.waitForPackage(packageName, options, onOk?, onErr?)` | — | 等待 [currentPackage()](global#m-currentpackage) 变为 `packageName`, 满足时以 `true` fulfill. |
| `flow.waitThenClick(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `flow.waitThenClick(cond, options, onOk?, onErr?)` | — | 等待目标出现后点击, 相当于 `flow.wait(cond, ...).click()`. |
| `flow.waitThenClickBounds(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `flow.waitThenClickBounds(cond, options, onOk?, onErr?)` | — | 等待目标出现后在控件中心坐标点按, 相当于 `flow.wait(cond, ...).clickBounds()`. |
| `flow.waitThenLongClick(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `flow.waitThenLongClick(cond, options, onOk?, onErr?)` | — | 等待目标出现后长按, 相当于 `flow.wait(cond, ...).longClick()`. |
| `flow.waitForStableThenClick(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `flow.waitForStableThenClick(cond, options, onOk?, onErr?)` | — | 等待目标稳定后点击, 相当于 `flow.waitForStable(cond, ...).click()`. |
| `flow.waitForStableThenClickBounds(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `flow.waitForStableThenClickBounds(cond, options, onOk?, onErr?)` | — | 等待目标稳定后在控件中心坐标点按, 相当于 `flow.waitForStable(cond, ...).clickBounds()`. |
| `flow.clickWhenStableAfter(cond, timeout?, interval?, delayMin?, delayMax?, onOk?, onErr?)` | — | — |
| `flow.clickWhenStableAfter(cond, options, onOk?, onErr?)` | — | 等待目标稳定, 再随机延时 `delayMin` 至 `delayMax` 毫秒 (只给 `delayMin` 时为固定延时), 然后点击. |
| `flow.clickBoundsWhenStableAfter(cond, timeout?, interval?, delayMin?, delayMax?, onOk?, onErr?)` | — | — |
| `flow.clickBoundsWhenStableAfter(cond, options, onOk?, onErr?)` | — | 等待目标稳定, 再随机延时 `delayMin` 至 `delayMax` 毫秒 (只给 `delayMin` 时为固定延时), 然后在控件中心坐标点按. |
| `flow.sleep(millis)` | — | — |
| `flow.sleep(millisMin, millisMax)` | — | 以一次休眠起链, 参数形式同全局 [sleep](global#m-sleep). 休眠结束后 Flow 以 `undefined` fulfill. |
| `flow.run(fn)` | 6.8.0 | 在 Flow 工作线程上执行 `fn`, 以其返回值 fulfill; `fn` 抛出异常时以该异常拒绝; `fn` 返回 Flow 或 Promise 时等待其结算并采纳结果. |
| `flow.of(value)` | 6.8.0 | 以一个已知值起链, 返回已 fulfill 的 Flow. |
| `flow.Flow(value)` | 6.8.0 | `flow.Flow` 是 Flow 对象的构造函数, 可用于 `instanceof` 判断. |
| `all(...sources)` | — | — |
| `flow.all(sources)` | — | 等待全部来源 fulfill, 以结果数组 (顺序与来源一致) fulfill; 任一来源拒绝时立即以该错误拒绝, 并取消其余由本方法创建的等待. |
| `race(...sources)` | — | — |
| `flow.race(sources)` | — | 以最先结算的来源的结果 (fulfill 或拒绝) 结算, 其余由本方法创建的等待被取消. |
| `any(...sources)` | — | — |
| `flow.any(sources)` | — | 以最先 fulfill 的来源的结果 fulfill; 全部来源都拒绝时以最后一个拒绝的错误拒绝. |
| `flow.cancelAll()` | 6.8.0 | 取消当前脚本全部待定的 Flow. 被取消的 Flow 以 [FlowError](flowErrorType) (`code` 为 `CANCELLED`) 拒绝. |
| `flow.defaults()` | — | 读取当前脚本的 Flow 默认值. |
| `flow.defaults(options)` | — | 修改当前脚本的 Flow 默认值, 对之后创建的步骤生效. 每个脚本各有一份, 互不影响. |
| `flow.trace(on?)` | 6.8.0 | 开启或关闭 Flow 追踪. 开启后每个步骤开始与结束时各在控制台输出一行, 包含步骤名, 目标, 节点短 id (用于区分并发的链), 耗时与结果摘要. |
| `条件 (cond)` | — | Flow 等待函数的首个参数 `cond` 支持以下形式: |
| `等待选项 (WaitOptions)` | — | 所有等待函数都接受两种参数形式: |

### 格式化 (Formatter) — [原文](https://docs.autojs6.com/#/fmt)

| API | 版本 | 说明 |
| --- | --- | --- |
| `fmt.bytes(source)` | — | 格式化 `source`, 并自动选择目标单位. |
| `fmt.bytes(source, toUnit)` | — | 将 `source` 格式化为 `toUnit`. |
| `fmt.bytes(source, options)` | — | 使用选项格式化 `source`. |
| `fmt.bytes(source, toUnit, options)` | — | 将 `source` 格式化为 `toUnit`, 并应用 `options`. |
| `fmt.bytes(source, fromUnit, toUnit, options?)` | — | 将数值从 `fromUnit` 格式化为 `toUnit`. |

### 全局对象 (Global) — [原文](https://docs.autojs6.com/#/global)

| API | 版本 | 说明 |
| --- | --- | --- |
| `global.toString()` | — | 返回固定字符串 `[object global]`. |
| `global.TODO(reason?)` | — | 始终抛出 `NotImplementedError`. `reason` 为空或省略时使用 AutoJs6 内置的未实现提示. |
| `err(...args)` | — | [console.error](console#m-error) 的全局别名. |
| `global.openConsole(isReset?)` | — | [console.show](console#m-show) 的全局别名, 用于显示控制台浮动窗口. |
| `global.showConsole(isReset?)` | — | [console.show](console#m-show) 的全局别名. 与 [openConsole](#m-openconsole) 等效. |
| `global.clearConsole()` | — | [console.clear](console#m-clear) 的全局别名, 用于清空控制台日志内容. |
| `global.launchConsole()` | — | [console.launch](console#m-launch) 的全局别名, 用于启动控制台 Activity. |
| `global.isUiThread()` | — | 当前线程是否为 Android UI 线程 |
| `global.isInteger(o)` | — | 是否为整数 |
| `global.isPrimitive(o)` | — | 此方法是 [isReference(o)](#isreference-o) 的逻辑取反. |
| `global.isReference(o)` | — | 是否为非空 JavaScript 对象或函数 |
| `global.isEmptyObject(o)` | — | 是否为不含自有属性的 JavaScript 对象 |
| `global.unwrapJavaObject(o)` | — | 递归解包 Rhino `Wrapper` 对象. 字符串, 数字和布尔值会规范化为对应的 JavaScript 值, Kotlin `Unit` 会转换为 `undefined`. |
| `global.isNullish(o?)` | — | 省略参数时返回 `true`. 多余参数会被忽略. |
| `global.isObjectSpecies(o)` | — | 此兼容方法按 JavaScript 的 `typeof` 结果判断对象, 并排除 `null` 和函数. 它与 [species.isObject(o)](#isobject-o) 的严格种类判断不同, 数组和日期等对象也会返回 `true`. |
| `global.sleep(millis)` | — | 使当前线程休眠一段时间. |
| `global.sleep(millisMin, millisMax)` | — | 使当前线程休眠一段时间, 该时间随机落在 millisMin 和 millisMax 之间. |
| `global.sleep(millis, bounds)` | — | 使当前线程休眠一段时间, 该时间随机落在 millis ± bounds 之间. |
| `global.toastVerbose(text, isLong?, isForcible?)` | — | 显示消息浮动框, 并将 `text` 传给 `console.verbose`. `toastverbose` 是此方法的全小写别名. |
| `global.toastLog(text?, isLong?, isForcible?)` | — | 显示消息浮动框, 并将 `text` 传给 `console.log`. 省略全部参数时显示字符串 `undefined`, 同时执行无参数的 `console.log()`. |
| `global.toastInfo(text, isLong?, isForcible?)` | — | 显示消息浮动框, 并将 `text` 传给 `console.info`. `toastinfo` 是此方法的全小写别名. |
| `global.toastWarn(text, isLong?, isForcible?)` | — | 显示消息浮动框, 并将 `text` 传给 `console.warn`. `toastwarn` 是此方法的全小写别名. |
| `global.toastError(text, isLong?, isForcible?)` | — | 显示消息浮动框, 并将 `text` 传给 `console.error`. `toasterror` 是此方法的全小写别名. |
| `global.random()` | — | 与 Math.random() 相同, 返回落在 \[0, 1) 区间的随机数字. |
| `global.random(value)` | — | 单参数调用固定返回 `NaN`. 如需生成指定范围内的随机整数, 必须同时提供下限和上限. |
| `global.random(min, max)` | — | 返回落在 \[min, max\] 区间的随机数字. |
| `global.wait(condition)` | — | 阻塞等待, 直到条件满足. |
| `global.wait(condition, limit)` | — | [wait(condition)](#wait-condition) 增加条件检测限制. |
| `global.wait(condition, limit, interval)` | — | [wait(condition, limit)](#wait-condition-limit) 增加条件检测间隔. |
| `global.wait(condition, callback)` | — | [wait(condition)](#wait-condition) 增加回调对象. |
| `global.wait(condition, limit, callback)` | — | [wait(condition, callback)](#wait-condition-callback) 增加条件检测限制. |
| `global.wait(condition, limit, interval, callback)` | — | [wait(condition, limit, callback)](#wait-condition-limit-callback) 增加条件检测间隔. |
| `global.waitForActivity(activityName)` | — | — |
| `global.waitForActivity(activityName, limit)` | — | — |
| `global.waitForActivity(activityName, limit, interval)` | — | — |
| `global.waitForActivity(activityName, callback)` | — | — |
| `global.waitForActivity(activityName, limit, callback)` | — | — |
| `global.waitForActivity(activityName, limit, interval, callback)` | — | — |
| `global.waitForPackage(packageName)` | — | — |
| `global.waitForPackage(packageName, limit)` | — | — |
| `global.waitForPackage(packageName, limit, interval)` | — | — |
| `global.waitForPackage(packageName, callback)` | — | — |
| `global.waitForPackage(packageName, limit, callback)` | — | — |
| `global.waitForPackage(packageName, limit, interval, callback)` | — | — |
| `global.waitAsync(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `global.waitAsync(cond, options, onOk?, onErr?)` | — | [wait](#m-wait) 的异步形式, 同 [flow.wait](flow#m-wait): 不阻塞当前线程, 立即返回 [Flow](flowType) 对象; 条件满足时以匹配结果 (选择器条件为 [UiObject](uiObjectType), 函数条件为返回值) fulfill, 超时则以 [FlowError](flowErrorType) 拒绝. `wait.async` 是 |
| `global.waitThenClick(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `global.waitThenClick(cond, options, onOk?, onErr?)` | — | 等待目标出现后点击, 同 [flow.waitThenClick](flow#m-waitthenclick), 相当于 `waitAsync(cond, ...).click()`. `clickWait` 是别名. |
| `global.waitThenClickBounds(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `global.waitThenClickBounds(cond, options, onOk?, onErr?)` | — | 等待目标出现后在控件中心坐标点按, 同 [flow.waitThenClickBounds](flow#m-waitthenclickbounds), 相当于 `waitAsync(cond, ...).clickBounds()`. `clickBoundsWait` 是别名. |
| `global.waitForStable(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `global.waitForStable(cond, options, onOk?, onErr?)` | — | 等待目标出现并连续 `stableFor` 毫秒 (默认 0) 无变化, 同 [flow.waitForStable](flow#m-waitforstable). |
| `global.waitForStableThenClick(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `global.waitForStableThenClick(cond, options, onOk?, onErr?)` | — | 等待目标稳定后点击, 同 [flow.waitForStableThenClick](flow#m-waitforstablethenclick). `clickWhenStable` 是别名. |
| `global.waitForStableThenClickBounds(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `global.waitForStableThenClickBounds(cond, options, onOk?, onErr?)` | — | 等待目标稳定后在控件中心坐标点按, 同 [flow.waitForStableThenClickBounds](flow#m-waitforstablethenclickbounds). `clickBoundsWhenStable` 是别名. |
| `global.waitForVisible(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `global.waitForVisible(cond, options, onOk?, onErr?)` | — | 等待目标出现, 对用户可见且保持稳定, 同 [flow.waitForVisible](flow#m-waitforvisible). |
| `global.waitForHidden(cond, timeout?, interval?, onOk?, onErr?)` | — | — |
| `global.waitForHidden(cond, options, onOk?, onErr?)` | — | 等待目标消失 (连续 `stableFor` 毫秒不存在或不可见), 同 [flow.waitForHidden](flow#m-waitforhidden). `waitForGone` 是别名. |
| `global.clickWhenStableAfter(cond, timeout?, interval?, delayMin?, delayMax?, onOk?, onErr?)` | — | — |
| `global.clickWhenStableAfter(cond, options, onOk?, onErr?)` | — | 等待目标稳定, 随机延时 `delayMin` 至 `delayMax` 毫秒后点击, 同 [flow.clickWhenStableAfter](flow#m-clickwhenstableafter). |
| `global.clickBoundsWhenStableAfter(cond, timeout?, interval?, delayMin?, delayMax?, onOk?, onErr?)` | — | — |
| `global.clickBoundsWhenStableAfter(cond, options, onOk?, onErr?)` | — | 等待目标稳定, 随机延时 `delayMin` 至 `delayMax` 毫秒后在控件中心坐标点按, 同 [flow.clickBoundsWhenStableAfter](flow#m-clickboundswhenstableafter). |
| `global.exit()` | — | 通过抛出 `ScriptInterruptedException` 异常实现脚本停止. |
| `global.exit(e)` | — | 停止脚本运行并抛出异常参数指定的异常. |
| `global.stop()` | — | 停止脚本运行. |
| `global.isStopped()` | — | 检测脚本主线程是否已中断. |
| `global.isShuttingDown()` | — | 检测脚本主线程是否已中断. |
| `global.isRunning()` | — | 检测脚本主线程是否未被中断. |
| `global.notStopped()` | — | 检测脚本主线程是否未被中断. |
| `global.requiresApi(api)` | — | 脚本运行的最低 API 级别要求. |
| `global.requiresAutojsVersion(versionName)` | — | 脚本运行的最低 AutoJs6 版本要求 (版本名称). |
| `global.requiresAutojsVersion(versionCode)` | — | 脚本运行的最低 AutoJs6 版本要求 (版本号). |
| `importPackage(...pkg)` | — | /\* 导入一个 Java 包. \*/ |
| `importClass(...cls)` | — | /\* 导入一个 Java 类. \*/ |
| `global.currentPackage(mode?)` | — | 获取当前前台应用的包名. |
| `global.currentActivity(mode?)` | — | 获取当前前台活动名称. 模式及别名与 [currentPackage(mode?)](#currentpackage-mode) 相同. |
| `global.currentComponent(mode?)` | — | 获取当前前台组件, 通常采用 `packageName/activityName` 形式. 模式及别名与 [currentPackage(mode?)](#currentpackage-mode) 相同. |
| `global.setClip(text)` | — | 设置系统剪贴板内容. |
| `global.getClip()` | — | 需额外留意, 自 [Android API 29 (10) \[Q\]](apiLevel) 起, 剪贴板数据的访问将受到限制: |
| `global.selector()` | — | 构建一个 "空" [选择器](uiSelectorType). |
| `existsAll(...selectors)` | — | 提供的选择器参数全部满足 "存在" 条件, 即 `selector.exists() === true`. |
| `existsOne(...selectors)` | — | 提供的选择器参数任一满足 "存在" 条件, 即 `selector.exists() === true`. |
| `global.setScreenMetrics(width, height)` | — | 设置基于坐标的触摸操作所使用的设计分辨率. 实际点击, 长按和滑动坐标会按当前设备尺寸缩放. |
| `global.getScaleBases()` | — | 默认返回 `{ x: 720, y: 1280 }`. |
| `global.getScaleBaseX()` | — | 默认值为 `720`. |
| `global.getScaleBaseY()` | — | 默认值为 `1280`. |
| `global.setScaleBases(baseX, baseY)` | — | 同时设置 [cX](#m-cx), [cY](#m-cy), [cYx](#m-cyx) 和 [cXy](#m-cxy) 使用的标度基数. 两个参数必须为正整数. |
| `global.setScaleBaseX(baseX)` | — | 设置横向标度基数. 参数必须为正整数, 且在同一个脚本运行时中最多设置一次. |
| `global.setScaleBaseY(baseY)` | — | 设置纵向标度基数. 参数必须为正整数, 且在同一个脚本运行时中最多设置一次. |
| `global.cX()` | — | 无参时, 返回当前设备宽度. |
| `global.cX(x, base, isRatio?)` | — | 由基数换算后得到的横坐标值. |
| `global.cX(x, isRatio)` | — | `isRatio` 参数默认为 `auto`, 即由 `x` 参数的范围自动决定 `x` 是否视为百分比, |
| `global.cX(x)` | — | 当参数 `x` 满足 `-1 < x < 1` 时, 相当于 `cX(x, /* isRatio = */ true)`, 即 `x` 将视为屏幕宽度百分比. |
| `global.cY()` | — | 无参时, 返回当前设备高度. |
| `global.cY(y, base, isRatio?)` | — | 由基数换算后得到的纵坐标值. |
| `global.cY(y, isRatio)` | — | `isRatio` 参数默认为 `auto`, 即由 `y` 参数的范围自动决定 `y` 是否视为百分比, |
| `global.cY(y)` | — | 当参数 `y` 满足 `-1 < y < 1` 时, 相当于 `cY(y, /* isRatio = */ true)`, 即 `y` 将视为屏幕高度百分比. |
| `global.cYx(coordinateY, baseX)` | — | 由横坐标基数换算后得到的纵坐标值. |
| `global.cYx(percentY, ratio)` | — | 由设备宽高比换算后得到的新纵坐标值. |
| `global.cYx(y, isRatio)` | — | `isRatio` 参数默认为 `auto`, 即由 `y` 参数的范围自动决定 `y` 是否视为百分比, |
| `global.cYx(y)` | — | 当参数 `y` 满足 `-1 < y < 1` 时, 相当于 `cYx(y, /* isRatio = */ true)`, 即 `y` 将视为屏幕高度百分比. |
| `global.cXy(coordinateX, baseY)` | — | 由纵坐标基数换算后得到的横坐标值. |
| `global.cXy(percentX, ratio)` | — | 由设备宽高比换算后得到的新横坐标值. |
| `global.cXy(x, isRatio)` | — | `isRatio` 参数默认为 `auto`, 即由 `x` 参数的范围自动决定 `x` 是否视为百分比, |
| `global.cXy(x)` | — | 当参数 `x` 满足 `-1 < x < 1` 时, 相当于 `cXy(x, /* isRatio = */ true)`, 即 `x` 将视为屏幕宽度百分比. |
| `global.species(o)` | — | 查看任意对象的 "种类", 如 `Object`, `Array`, `Number`, `String`, `RegExp` 等. |

### HTTP — [原文](https://docs.autojs6.com/#/http)

| API | 版本 | 说明 |
| --- | --- | --- |
| `http.client()` | 6.8.0 | 获取当前脚本共享的 OkHttp 客户端. 请求选项中的 `timeout` 和 `client` 配置会基于并替换该客户端. 自 AutoJs6 6.8.0 起, `isInsecure` / `insecure` 仅作用于当前请求, 不会使后续请求继承其证书和主机名校验豁免. |
| `http.buildRequest(url, options)` | — | 只构建请求, 不执行网络操作. |
| `http.request(url, options, callback?)` | — | 构建并执行自定义 HTTP 请求. 执行方式和回调参数参见本页开头的说明. |
| `http.requestAsync(url, options, callback?)` | — | 构建自定义 HTTP 请求, 并在后台线程执行网络操作. Promise 和可选回调语义参见本页开头的说明. |
| `http.get(url, options?, callback?)` | — | 发送 GET 请求. 此方法将 `options.method` 设置为 `"GET"`. |
| `http.getAsync(url, options?, callback?)` | — | 在后台线程发送 GET 请求. |
| `http.head(url, options?, callback?)` | — | 发送 HEAD 请求. 此方法将 `options.method` 设置为 `"HEAD"`. |
| `http.headAsync(url, options?, callback?)` | — | 在后台线程发送 HEAD 请求. |
| `http.post(url, data?, options?, callback?)` | — | 发送 POST 请求. 此方法将 `options.method` 设置为 `"POST"`. |
| `http.postAsync(url, data?, options?, callback?)` | — | 在后台线程发送 POST 请求. 请求数据转换规则与 [post](#m-post) 相同. |
| `http.postJson(url, data, options?, callback?)` | — | 使用 `JSON.stringify(data)` 构建请求体并发送 POST 请求. 此方法将 `method` 设置为 `"POST"`, 将 `contentType` 设置为 `"application/json"`. |
| `http.postJsonAsync(url, data, options?, callback?)` | — | 在后台线程发送 JSON POST 请求. |
| `http.postMultipart(url, files, options?, callback?)` | — | 发送 `"multipart/form-data"` POST 请求. `files` 属性值支持: |
| `http.postMultipartAsync(url, files, options?, callback?)` | — | 在后台线程发送 `"multipart/form-data"` POST 请求. `files` 格式与 [postMultipart](#m-postmultipart) 相同. |
| `http.put(url, data?, options?, callback?)` | — | 发送 PUT 请求. 此方法将 `options.method` 设置为 `"PUT"`. 请求数据转换规则与 [post](#m-post) 相同. |
| `http.putAsync(url, data?, options?, callback?)` | — | 在后台线程发送 PUT 请求. 请求数据转换规则与 [post](#m-post) 相同. |
| `http.delete(url, data?, options?, callback?)` | — | 发送 DELETE 请求. 此方法将 `options.method` 设置为 `"DELETE"`. 请求数据转换规则与 [post](#m-post) 相同. |
| `http.del(url, data?, options?, callback?)` | — | [delete](#m-delete) 的别名. |
| `http.deleteAsync(url, data?, options?, callback?)` | — | 在后台线程发送 DELETE 请求. 请求数据转换规则与 [post](#m-post) 相同. |
| `http.delAsync(url, data?, options?, callback?)` | — | [deleteAsync](#m-deleteasync) 的别名. |

### 图像 (Images) — [原文](https://docs.autojs6.com/#/image)

| API | 版本 | 说明 |
| --- | --- | --- |
| `images.initOpenCvIfNeeded()` | 6.8.0 | 按需初始化 OpenCV 运行环境. 依赖 OpenCV 的图像方法通常会自动调用此方法. |
| `images.buildRegion(image, region)` | — | 将区域限制在图片范围内. 数组格式为 `[ x, y, width, height ]`. `x` 和 `y` 默认为 `0`, `width` 和 `height` 默认延伸到图片边缘. 坐标和尺寸也支持屏幕度量值, 详见 [ScreenMetricNumberX](dataTypes#screenmetricnumberx) 和 \[ScreenMetricNumberY\](dataTypes# |
| `images.__buildRegion(region?, imageWidth?, imageHeight?)` | 6.8.0 | 按明确的图片尺寸规范化区域. `imageWidth` 或 `imageHeight` 缺失时返回 `null`. 此方法不解析屏幕度量比例值. |
| `images.read(path, isStrict?)` | — | 读取本地图片. 当图片不存在或无法解码时, 默认返回 `null`; `isStrict` 为 `true` 时抛出异常. |
| `images.imread(path)` | 6.6.0 | 使用 OpenCV 读取本地图片. 读取失败时可能返回空矩阵, 可使用 `Mat#empty()` 检查. |
| `images.load(src)` | — | 同步加载网络图片. 网络失败时抛出异常, 无法取得有效位图时可能返回 `null`. |
| `images.loadAsync(src)` | — | 异步加载网络图片. 网络或解码失败时 Promise 被拒绝. |
| `images.copy(image)` | — | 复制像素数据并返回具有独立生命周期的新图片. |
| `images.fromBase64(base64)` | — | 输入无法解码为图片时抛出异常. |
| `images.toBase64(image, format?, quality?)` | — | 不含换行的 Base64 数据 |
| `images.fromBytes(bytes)` | — | 解码后的图片 |
| `images.toBytes(image, format?, quality?)` | — | `toBase64()` 和 `toBytes()` 支持 `png`, `jpg`, `jpeg`, `webp`, `webp_lossy`, `webp-lossy`, `webp_lossless` 和 `webp-lossless`. 后 4 种显式 WebP 模式要求 Android API 30 或更高. |
| `images.readPixels(image)` | — | 读取图片的全部 ARGB 像素. `data` 按从左到右, 从上到下的顺序排列, 长度为 `width * height`. |
| `images.matToImage(mat)` | — | 使用矩阵创建包装图片. 返回对象接管矩阵数据的生命周期, 不应再独立释放同一矩阵. |
| `images.quantize(image, options?)` | — | 将图片量化为索引色 PNG, 同时返回编码字节, 实际质量和量化误差. 此方法要求插件声明 options API version 2 或更高. 其他保存与压缩方法保持原返回类型; 需要根据质量或输出体积决定是否替换原图时应使用本方法. |
| `images.quantizeToFile(image, path, options?)` | — | 将图片量化为索引色 PNG 并直接写入文件, 同时返回解析后的路径, 输出大小, 实际质量和量化误差. 此方法要求插件声明 options API version 3 或更高. 父目录不存在时会自动创建, 已有文件会被覆盖; 文件创建或写入失败时抛出 I/O 异常. |
| `images.save(image, path, format?, qualityOrOptions?)` | — | 保存图片并按需创建父目录. 已有文件会被覆盖. 支持的格式与 [toBytes()](#m-images-tobytes-image-format-quality) 相同. |
| `images.saveImage(image, path, format?, qualityOrOptions?)` | — | `images.save()` 的别名. |
| `images.compress(image, format?, qualityOrOptions?)` | — | 压缩会改变编码体积或质量, 但不会按比例缩小图片尺寸. 默认 PNG 质量为 `60`, 因此仅传入 `image` 时会加载 Image Quantization 插件. 量化或编码后的数据无法解码为图片时抛出异常. |
| `images.compressToBytes(image, format?, qualityOrOptions?)` | — | 默认 PNG 质量为 `60`, 因此仅传入 `image` 时会加载 Image Quantization 插件. |
| `images.downsample(src, reqWidth, reqHeight, withAlpha?)` | 6.6.3 | 字符串可为运行时文件路径或 URI. 此方法在解码阶段选择采样尺寸, 适合降低大图的解码内存. |
| `images.getSize(src)` | — | 图片路径只读取边界信息, 不解码完整位图. |
| `images.getWidth(src)` | — | 图片宽度 |
| `images.getHeight(src)` | — | 图片高度 |
| `images.clip(image, region)` | — | 剪切后的新图片 |
| `images.clip(image, x, y, width, height)` | — | 剪切后的新图片 |
| `images.pixel(image, x, y)` | — | 坐标超出图片范围时抛出异常. |
| `images.invert(image)` | 6.6.0 | 反转 RGB 通道并保留原透明通道. |
| `images.grayscale(image, dstCn?)` | — | 等价于使用颜色转换代码 `BGR2GRAY` 调用 [cvtColor()](#m-images-cvtcolor-image-code-dstcn). |
| `images.isGrayscale(image)` | 6.6.0 | 单通道矩阵直接视为灰度图. 多通道矩阵仅当每个像素的各通道值均相同时返回 `true`. |
| `images.threshold(image, threshold, maxVal, type?)` | — | 字符串类型可省略 `THRESH_` 前缀, 并忽略大小写. |
| `images.inRange(image, lowerBound, upperBound)` | — | 保留各通道均位于指定闭区间内的像素. |
| `images.interval(image, color, threshold)` | — | 二值化后的新图片 |
| `images.adaptiveThreshold(image, maxValue, adaptiveMethod, thresholdType, blockSize, C)` | — | `adaptiveMethod` 和 `thresholdType` 不带 OpenCV 常量前缀. |
| `images.blur(image, size, anchor?, type?)` | — | — |
| `images.medianBlur(image, size)` | — | 数组或 `OpenCVSize` 的宽度和高度必须相等. OpenCV 还要求核尺寸为大于 `1` 的奇数. |
| `images.gaussianBlur(image, size, sigmaX?, sigmaY?, type?)` | — | — |
| `images.bilateralFilter(image, d?, sigmaColor?, sigmaSpace?, borderType?)` | — | — |
| `images.cvtColor(image, code, dstCn?)` | — | `code` 不带 `COLOR_` 前缀, 例如 `BGR2GRAY` 或 `RGBA2BGR`. |
| `images.resize(image, size, interpolation?)` | — | `[ width ]` 同时指定宽度和高度, `[ width, height ]` 分别指定两个尺寸. 每个尺寸必须为 `1` 至 `2147483647` 范围内的有限正数. |
| `images.scale(image, fx, fy, interpolation?)` | — | 插值参数的可选值与 [resize()](#m-images-resize-image-size-interpolation) 相同. |
| `images.rotate(image, degree, centerX?, centerY?)` | — | — |
| `images.flip(image)` | — | 水平翻转后的图片 |
| `images.flip(image, orientation)` | — | 字符串支持 `horizontal`, `vertical`, `both` 及其缩写 `h`, `v`, `x`, `y`, `xy`. 数组格式为 `[ horizontal, vertical ]`. 对象可使用 `horizontal` 或 `h` 或 `x`, 以及 `vertical` 或 `v` 或 `y`. |
| `images.flip(image, horizontal, vertical)` | — | — |
| `images.concat(imageA, imageB, direction?)` | — | 方向支持 Android Gravity 常量 `START`, `END`, `TOP` 和 `BOTTOM`, 或对应整数. 水平拼接时图片垂直居中, 垂直拼接时图片水平居中. |
| `images.requestScreenCapture(options?)` | — | 请求屏幕捕获权限并等待用户选择. 默认选项为 `orientation = "auto"`, `width = -1`, `height = -1`, `isAsync = false`. |
| `images.requestScreenCapture(landscape)` | — | — |
| `images.requestScreenCapture(width, height)` | — | 此重载使用 `orientation = "none"`, 即不根据设备方向交换指定尺寸. |
| `images.requestScreenCaptureAsync(options?)` | — | 异步请求屏幕捕获权限. 参数和默认值与 `requestScreenCapture(options?)` 相同, 可在 UI 线程调用. |
| `images.requestScreenCaptureAsync(landscape)` | — | 兑现值为 [boolean](dataTypes#boolean) |
| `images.requestScreenCaptureAsync(width, height)` | — | 兑现值为 [boolean](dataTypes#boolean) |
| `images.stopScreenCapture()` | 6.6.0 | 停止当前截图器, 释放屏幕捕获资源, 并将仍在等待的授权请求以 `false` 完成. 之后再次截图需要重新请求权限. |
| `images.getScreenCaptureOptions()` | 6.6.0 | 尚未获得截图权限或截图器已停止时返回 `null`. |
| `images.captureScreen()` | — | 在后台脚本线程中, 若尚未请求权限, 此方法会先同步调用 `requestScreenCapture()`. UI 线程不会自动请求权限. |
| `images.captureScreen(path)` | — | 捕获当前屏幕并直接保存到指定路径. |
| `images.on("screen_capture_available", listener)` | — | 当截图请求使用 `isAsync = true` 时, 每个可用帧触发一次事件. 监听器收到的图片由调用方负责及时回收. |
| `images.detectColor(image, color, x, y, threshold?, algorithm?)` | — | 指定位置是否匹配颜色 |
| `images.detectsColor(image, color, x, y, threshold?, algorithm?)` | — | 已弃用的 `detectColor()` 别名. |
| `images.detectMultiColors(image, x, y, firstColor, paths, options?)` | 6.6.0 | `paths` 的每一项为 `[ dx, dy, color ]`, 坐标相对于 `(x, y)`. |
| `images.detectsMultiColors(image, x, y, firstColor, paths, options?)` | — | 已弃用的 `detectMultiColors()` 别名. |
| `images.findPointByColor(image, color, options?)` | — | 首个匹配点 |
| `images.findPointByColor(image, color, x?, y?, width?, height?, threshold?)` | — | — |
| `images.findColor(image, color, options?)` | — | 使用 `findPointByColor(image, color, options?)` 代替. |
| `images.findColorInRegion(image, color, x?, y?, width?, height?, threshold?)` | — | 使用 `findPointByColor()` 的位置参数重载代替. |
| `images.findPointByColorExactly(image, color, x?, y?, width?, height?)` | — | 以阈值 `0` 查找完全匹配的颜色. |
| `images.findColorEquals(image, color, x?, y?, width?, height?)` | — | 使用 `findPointByColorExactly()` 代替. |
| `images.findPointsByColor(image, color, options?)` | — | 全部匹配点 |
| `images.findAllPointsForColor(image, color, options?)` | — | 使用 `findPointsByColor()` 代替. |
| `images.findPointByColors(image, firstColor, paths, options?)` | — | 首个匹配模式的基准点 |
| `images.findMultiColors(image, firstColor, paths, options?)` | — | 使用 `findPointByColors()` 代替. |
| `images.findPointsByColors(image, firstColor, paths, options?)` | — | 全部匹配模式的基准点 |
| `images.countPointsByColor(image, color, options?)` | 6.8.0 | 统计图片 (或 `region` 区域) 中与指定颜色匹配的像素数量, 匹配规则与 [findPointsByColor](#m-images-findpointsbycolor-image-color-options) 相同 (RGB 各分量差值均不超过阈值). |
| `images.getMeanColor(image, region?)` | 6.8.0 | 计算图片 (或 `region` 区域) 各通道的平均值并合成为颜色整数. 灰度图返回不透明灰色, 无透明通道的图片 alpha 为 `255`. |
| `images.findCircles(image, options?)` | — | 使用 OpenCV Hough 圆检测. 非灰度图片会先自动灰度化. |
| `images.findPointByImage(image, template, options?)` | — | 默认值为 `weakThreshold = 0.6`, `threshold = 0.9`, `level = -1`. `level = -1` 表示自动选择图像金字塔层数. |
| `images.findPointByImage(image, template, x?, y?, width?, height?, threshold?)` | — | 此重载仍使用 `weakThreshold = 0.6` 和 `level = -1`. |
| `images.findImage(image, template, options?)` | — | 使用 `findPointByImage(image, template, options?)` 代替. |
| `images.findImageInRegion(image, template, x?, y?, width?, height?, threshold?)` | — | 使用 `findPointByImage()` 的位置参数重载代替. |
| `images.matchTemplate(image, template, options?)` | — | 默认值为 `weakThreshold = 0.6`, `threshold = 0.9`, `level = -1`, `max = 5`, `useTransparentMask = false`. `transparentMask` 是 `useTransparentMask` 的兼容别名, 仅在前者缺失时生效. |
| `images.detectAndComputeFeatures(image, options?)` | — | 检测 `image` (或其 `region` 区域) 的关键点并计算描述子, 返回可反复用于 `matchFeatures()` 的特征对象. |
| `images.matchFeatures(scene, object, options?)` | — | 在场景中寻找目标, 返回目标四角在场景原图坐标系中的位置 (已还原 `region` 偏移与 `scale` 缩放). |
| `images.psnr(imageA, imageB)` | 6.6.0 | 结果通常越大表示差异越小. |
| `images.ssim(imageA, imageB)` | — | 结果通常越接近 `1` 表示结构越相似. |
| `images.mssim(imageA, imageB)` | 6.6.0 | 结果通常越接近 `1` 表示结构越相似. |
| `images.hist(imageA, imageB)` | — | 直方图相关性 |
| `images.mse(imageA, imageB)` | — | 结果越小表示差异越小, 完全相同为 `0`. 8 位图像的最大值为 `65025` (即 `255 * 255`). |
| `images.ncc(imageA, imageB)` | — | 结果通常越接近 `1` 表示相关性越高. |
| `images.isEqual(imageA, imageB)` | — | 尺寸或类型不同时返回 `false`. 从 AutoJs6 6.8.0 起逐通道精确比较, 任一通道 (含 alpha) 相差 `1` 也会返回 `false`. |
| `images.getSimilarity(imageA, imageB, options?)` | — | `metric` 支持 `psnr`, `ssim`, `mssim`, `hist`, `mse` 和 `ncc`, 默认值为 `mssim`. `type` 是兼容别名, 仅在 `metric` 缺失时使用. 拼写 `pnsr` 会兼容为 `psnr`. |
| `images.isRecycled(...images)` | — | 无参数时返回 `true`. 任一参数不是 `ImageWrapper` 或尚未回收时返回 `false`. |
| `images.recycle(...images)` | — | 已回收的图片视为成功. 无参数时返回 `true`. 任一参数不是 `ImageWrapper` 或回收时发生异常, 结果为 `false`. |

### 国际化 (Internationalization) — [原文](https://docs.autojs6.com/#/i18n)

| API | 版本 | 说明 |
| --- | --- | --- |
| `i18n(key, ...parameters)` | — | 按当前区域设置查找并格式化消息. `i18n` 是可调用对象, 同时包含本章列出的属性和方法. |
| `i18n.setPath(relativePath)` | — | 设置 [i18n.load](#m-load) 和 [i18n.loadAll](#m-loadall) 使用的消息目录. 路径必须在调用时已经是目录, 否则抛出异常. |
| `i18n.setLocale(locale)` | — | 设置后续消息查找使用的区域设置. |
| `i18n.getFallbackLocales()` | — | 返回当前区域设置对应的回退区域设置列表. |
| `i18n.getParser()` | — | 返回底层消息解析器对象. |
| `i18n.getPath()` | — | 返回当前消息目录. 当前配置不是目录时, 先尝试将其解析为 `assets` 下的相对路径; 回退路径存在时会同时更新当前配置. |
| `i18n.getLocale()` | — | 返回当前区域设置. |
| `i18n.getFinalFallback()` | — | 返回找不到消息时使用的最终回退区域设置. AutoJs6 创建此模块时将其设为 `default`. |
| `i18n.load(messageSource, locale)` | — | 把消息对象加载到底层消息存储. |
| `i18n.load(messageSource, locale?)` | — | 从 JSON 文件加载消息. |
| `i18n.loadAll()` | — | 加载消息目录第一层的全部 `.json` 文件. 每个文件的不含扩展名部分作为区域设置标识. 此方法不会递归扫描子目录. |

### JavaScript 对象扩展 (Jsox) — [原文](https://docs.autojs6.com/#/jsox)

| API | 版本 | 说明 |
| --- | --- | --- |
| `jsox(...modules)` | 6.6.0 | 将指定扩展模块挂载到对应的 JavaScript 内置对象. |
| `jsox.extend(...modules)` | 6.6.0 | 将指定扩展模块挂载到对应的 JavaScript 内置对象. |
| `jsox.extendAll()` | 6.6.0 | 挂载全部支持的扩展模块. |

### 按键 (Keys) — [原文](https://docs.autojs6.com/#/keys)

| API | 版本 | 说明 |
| --- | --- | --- |
| `automator.back()` | — | 执行系统返回动作. |
| `automator.home()` | — | 执行系统主屏幕动作. |
| `automator.powerDialog()` | — | 打开系统电源菜单. |
| `automator.notifications()` | — | 展开系统通知栏. |
| `automator.quickSettings()` | — | 展开系统快速设置面板. |
| `automator.recents()` | — | 打开系统最近任务界面. |
| `automator.splitScreen()` | — | 执行系统切换分屏动作. 实际结果还取决于设备系统和当前界面是否支持分屏. |
| `Menu()` | — | 发送菜单键, 键码为 `82`. |
| `Home()` | — | 发送主屏幕键, 键码为 `3`. |
| `Back()` | — | 发送返回键, 键码为 `4`. |
| `Up()` | — | 发送方向上键, 键码为 `19`. |
| `Down()` | — | 发送方向下键, 键码为 `20`. |
| `Left()` | — | 发送方向左键, 键码为 `21`. |
| `Right()` | — | 发送方向右键, 键码为 `22`. |
| `OK()` | — | 发送方向中心键, 键码为 `23`. |
| `VolumeUp()` | — | 发送音量上键, 键码为 `24`. |
| `VolumeDown()` | — | 发送音量下键, 键码为 `25`. |
| `Power()` | — | 发送电源键, 键码为 `26`. |
| `Camera()` | — | 发送相机键, 键码为 `27`. |
| `KeyCode(code)` | — | 使用 Shizuku 或 Root Shell 发送一个按键事件. |
| `Text(text)` | — | 通过 Root Shell 执行 `input text`. 参数会直接拼接到 Shell 命令, 不会自动进行引号包裹或 Shell 转义. |
| `Input(text)` | — | [Text](#m-text) 的同义方法. |

### 邮件 (Mail) — [原文](https://docs.autojs6.com/#/mail)

| API | 版本 | 说明 |
| --- | --- | --- |
| `mail.connect(options)` | — | 按账户选项建立与插件的会话. 选项对象会像 `JSON.stringify` 一样序列化后交给插件, 函数 (`tokenProvider` 除外) 与 `undefined` 值被忽略. 选项形态错误 (如缺少 `address`, 既没有 `provider` 也没有任何服务器地址) 在此处抛出 `INVALID_ARGUMENT`. |
| `mail.connect(alias)` | — | 按别名使用插件中保存的账户, 凭据由插件持有, 不会经过脚本. 别名不存在时抛出 `ACCOUNT_NOT_FOUND`. 可用别名可通过 [mail.accounts.list](#p-accounts) 查询. |
| `mail.connectAsync(options)` | — | 兑现值为 [MailClient](mailClientType) |
| `mail.connectAsync(alias)` | — | [mail.connect](#m-connect) 的异步形态. 参数不合法时 Promise 同样被拒绝, 而不是同步抛出. |
| `mail.setDefault(client)` | — | 将客户端设为默认客户端, 之后 [转发方法](#转发方法-forwarded-methods) 与 [mail.close](#m-close) 作用于它. 默认客户端关闭后需再次设置. |
| `mail.setDefault(options)` | — | 新建的客户端 |
| `mail.setDefault(alias)` | — | 相当于 `mail.setDefault(mail.connect(optionsOrAlias))`. |
| `mail.close()` | 6.8.0 | 关闭默认客户端并清除默认设置. 没有默认客户端时不做任何事. 关闭其他客户端请调用其自身的 [close](mailClientType#m-close) 方法. |
| `mail.providers.list()` | 6.8.0 | 返回插件内置的服务商预设列表. 每项包含 `id`, `name`, `domains`, `imap`, `pop3`, `smtp`, `auth`, `authHint`, `autoSavesSent`, `sentFolder`, `requiresClientId`, `idlePush`, `docsUrl` 与 `notes`, 含义见 \[MailAccountOptions\](m |
| `mail.providers.get(id)` | 6.8.0 | 预设, 不存在时为 `null` |
| `mail.providers.resolve(address)` | 6.8.0 | 按地址的域名查找预设. [mail.connect](#m-connect) 不会自动这样推断, 需要时由脚本把结果的 `id` 写入 `provider`. |
| `mail.accounts.list()` | 6.8.0 | 每项包含 `alias`, `address`, `user`, `name`, `provider`, `auth`, `receive`, `imap`, `pop3`, `smtp`, `default` 与 `updatedAt`, 不包含任何凭据. 保存的账户无法解析时只包含 `alias` 与 `error`. 字段含义见 \[MailAccountOptions\](mailAccoun |
| `mail.accounts.has(alias)` | 6.8.0 | if (mail.accounts.has('work')) { |
| `new mail.MailError(message, code?, details?, retryable?)` | 6.8.0 | 邮件模块抛出的所有错误都是 `name` 为 `'MailError'` 的 JavaScript [Error](exceptions#error-对象), 可用 `instanceof mail.MailError` 判断. 脚本也可自行构造, 例如在 `tokenProvider` 中抛出. |
| `mailError.toString()` | 6.8.0 | 形如 `MailError [CODE]: message` |
| `转发方法 (Forwarded Methods)` | — | [MailClient](mailClientType) 的每个网络方法都在 `mail` 上镜像为同名方法, 并转发到默认客户端. 没有默认客户端时抛出 (异步形态则拒绝为) `NO_DEFAULT_ACCOUNT`. |
| `示例 (Examples)` | — | — |

### 多媒体 (Media) — [原文](https://docs.autojs6.com/#/media)

| API | 版本 | 说明 |
| --- | --- | --- |
| `media.scanFile(path)` | — | 请求 Android 媒体扫描器扫描文件. MIME 类型根据文件名推断, 无法识别时使用通配类型. |
| `media.playMusic(path)` | — | 以音量 `1` 播放一次音频文件. |
| `media.playMusic(path, volume)` | — | 以指定音量播放一次音频文件. `volume` 必须是 `0..1` 范围内的有限数值, `0` 表示此播放器静音, 不修改系统媒体音量. |
| `media.playMusic(path, volume, looping)` | — | 从脚本工作线程同步等待服务连接和音频准备, 开始播放后返回. 准备等待上限为 30 秒; 线程被中断, 超时或脚本结束后, 尚未完成的请求会取消, 不会在稍后连接成功时继续播放. 路径不存在, 文件无法读取, 音频格式不受支持或前台播放启动失败时抛出异常. 无法读取的路径和无效音量不会接管另一脚本正在播放的音乐. |
| `media.musicSeekTo(msec)` | — | 调整当前播放器的播放位置. 播放器尚未创建时不执行操作. |
| `media.pauseMusic()` | — | 暂停当前播放器. 播放器尚未创建时不执行操作. |
| `media.resumeMusic()` | — | 继续当前脚本仍持有的播放器, 自然播放结束后从头重播. 恢复时再次尝试启动音乐前台服务, 因此仍需满足系统运行条件. 尚未创建, 已停止或已由其他脚本接管时不执行操作. |
| `media.stopMusic()` | — | 停止当前播放器. 播放器尚未创建时不执行操作. 停止后如需再次播放, 调用 [media.playMusic](#m-playmusic) 重新设置数据源. |
| `media.isMusicPlaying()` | — | 播放器尚未创建时返回 `false`. 返回 `true` 表示 Media3 报告正在播放, 不保证音频未被系统抑制; 参阅 [Android 17 后台音频](#android-17-后台音频). |
| `media.getMusicDuration()` | — | 播放器尚未创建, 已停止或已由其他脚本接管时返回 `0`. 音频时长未知时也返回 `0`. |
| `media.getMusicCurrentPosition()` | — | 播放器尚未创建, 已停止或已由其他脚本接管时返回 `-1`. |

### 媒体信息 (MediaInfo) — [原文](https://docs.autojs6.com/#/mediainfo)

| API | 版本 | 说明 |
| --- | --- | --- |
| `mediainfo.mediainfo(path)` | — | 读取媒体文件信息. |
| `mediainfo.read(path)` | — | 读取媒体文件信息. |
| `mediainfo.get(path, streamKind, parameter, options?)` | 6.8.0 | 流序号从 `0` 开始, 同类流的第 2 条使用 `streamNumber: 1`. 缺省查询第 0 条流的 `TEXT` 值. |
| `mediainfo.countGet(path, streamKind)` | 6.8.0 | 要求 `mediainfo.capabilities().streamCount` 为 `true`. 旧插件不支持时抛出包含 `MEDIAINFO_QUERY_UNSUPPORTED` 的异常. |
| `mediainfo.snapshot(path, options?)` | 6.8.0 | 读取版本化的 JSON 快照. 未指定 `options.schema` 时返回 v1; 如需 v2, 应使用 `mediainfo.SNAPSHOT_SCHEMA_V2` 显式选择. |
| `mediainfo.capabilities()` | 6.8.0 | 返回当前 MediaInfo 插件宣告的快照 schema 和缺省 schema. 插件可用时, v1 始终作为兼容能力出现; v2 只有在插件明确宣告后才会出现. |
| `mediainfo.MediainfoResult#general(parameter?, options?)` | — | 查询 General 流信息. |
| `mediainfo.MediainfoResult#video(parameter?, options?)` | — | 查询 Video 流信息. |
| `mediainfo.MediainfoResult#audio(parameter?, options?)` | — | 查询 Audio 流信息. |
| `mediainfo.MediainfoResult#text(parameter?, options?)` | — | 查询字幕或其他 Text 流信息. |
| `mediainfo.MediainfoResult#other(parameter?, options?)` | — | 查询 Other 流信息. |
| `mediainfo.MediainfoResult#image(parameter?, options?)` | — | 查询 Image 流信息. |
| `mediainfo.MediainfoResult#menu(parameter?, options?)` | — | 查询 Menu 流信息. |
| `mediainfo.MediainfoResult#max(parameter?, options?)` | — | 使用 MediaInfo 的 `MAX` 流类型执行查询. |
| `mediainfo.MediainfoResult#toString()` | 6.7.0 | 返回由已解析章节和字段组成的对象描述. |

### MIME — [原文](https://docs.autojs6.com/#/mime)

| API | 版本 | 说明 |
| --- | --- | --- |
| `mime.mime(mimeString)` | 6.6.0 | 解析 MIME 类型字符串. |
| `mime.getMediaType(mediaType)` | 6.6.0 | 将字符串解析为 `okhttp3.MediaType`. |
| `mime.parseMediaType(mediaType)` | 6.6.0 | 尝试将字符串解析为 `okhttp3.MediaType`. |
| `mime.fromFile(path)` | 6.6.0 | 根据文件扩展名查询 MIME 类型. |
| `mime.fromFileOr(path, defaultType)` | 6.6.0 | 根据文件扩展名查询 MIME 类型. 扩展名无法识别时返回 `defaultType`. |
| `mime.fromFileOrWildcard(path)` | 6.6.0 | 根据文件扩展名查询 MIME 类型. 扩展名无法识别时返回 `*/*`. |
| `mime.JsMime#toString()` | 6.6.0 | 返回包含全部解析字段的多行调试字符串. |
| `mime.JsMime#toStringReadable()` | 6.6.0 | 返回与 [JsMime#toString()](#jsmimetostring) 相同的字符串. |

### 模块 (Module) — [原文](https://docs.autojs6.com/#/modules)

| API | 版本 | 说明 |
| --- | --- | --- |
| `require(id)` | — | 解析并加载模块. |
| `require.resolve(id)` | — | 解析模块但不执行模块代码. |
| `require.paths()` | — | 返回附加搜索目录. 结果始终包含: |

### Nano ID — [原文](https://docs.autojs6.com/#/nanoid)

| API | 版本 | 说明 |
| --- | --- | --- |
| `nanoid.nanoid(size?)` | 6.6.0 | 生成长度为 `size` 的随机字符串 ID. |

### 消息通知 (Notice) — [原文](https://docs.autojs6.com/#/notice)

| API | 版本 | 说明 |
| --- | --- | --- |
| `notice.notice(content)` | — | 发送通知, 包含内容. |
| `notice.notice(title, content)` | — | 发送通知, 包含标题及内容. |
| `notice.notice()` | — | 发送通知, 主要用于测试. |
| `notice.notice(options)` | — | 发送通知, 并进行选项配置. |
| `notice.notice(content, options)` | — | 发送通知, 包含内容, 并进行选项配置. |
| `notice.notice(title, content, options)` | — | 发送通知, 包含标题及内容, 并进行选项配置. |
| `notice.notice(builder)` | — | 使用 `通知构建器 (Notice Builder)` 发送通知. |
| `notice.notice(builder, options)` | — | 使用 `通知构建器 (Notice Builder)` 发送通知, 并进行选项配置. |
| `notice.isEnabled()` | 6.3.0 | 检测 AutoJs6 的通知是否未被阻止 (not blocked). |
| `notice.ensureEnabled()` | 6.3.0 | 确保 AutoJs6 的通知未被阻止 (not blocked). |
| `notice.launchSettings()` | 6.3.0 | 跳转至 AutoJs6 的通知设置页面. |
| `notice.cancel(id)` | 6.3.0 | 消除通知. |
| `notice.getBuilder()` | 6.3.0 | 获取一个简单通知构建器. |
| `notice.config(preset)` | 6.3.0 | 配置通知渠道与通知发送的默认行为. |

### 光学字符识别 (OCR) — [原文](https://docs.autojs6.com/#/ocr)

| API | 版本 | 说明 |
| --- | --- | --- |
| `ocr.ocr(options?)` | — | 识别当前屏幕截图中包含的所有文本, 返回文本数组. |
| `ocr.ocr(region)` | — | 识别当前屏幕截图指定区域内包含的所有文本, 返回文本数组. |
| `ocr.ocr(img, options?)` | — | 识别图像包含的所有文本, 返回文本数组. |
| `ocr.ocr(img, region)` | — | 识别指定区域内图像包含的所有文本, 返回文本数组. |
| `ocr.ocr(imgPath, options?)` | — | 识别指定路径对应图像包含的所有文本, 返回文本数组. |
| `ocr.ocr(imgPath, region)` | — | 识别指定路径对应图像在指定区域内包含的所有文本, 返回文本数组. |
| `ocr.recognizeText(options?)` | — | 识别当前屏幕截图中包含的所有文本, 返回文本数组. |
| `ocr.recognizeText(region)` | — | 识别当前屏幕截图指定区域内包含的所有文本, 返回文本数组. |
| `ocr.recognizeText(img, options?)` | — | 识别图像包含的所有文本, 返回文本数组. |
| `ocr.recognizeText(img, region)` | — | 识别指定区域内图像包含的所有文本, 返回文本数组. |
| `ocr.recognizeText(imgPath, options?)` | — | 识别指定路径对应图像包含的所有文本, 返回文本数组. |
| `ocr.recognizeText(imgPath, region)` | — | 识别指定路径对应图像在指定区域内包含的所有文本, 返回文本数组. |
| `ocr.detect(options?)` | — | 识别当前屏幕截图中包含的所有文本, 返回 [OcrResult](dataTypes#ocrresult) 数组. |
| `ocr.detect(region)` | — | 识别当前屏幕截图指定区域内包含的所有文本, 返回 [OcrResult](dataTypes#ocrresult) 数组. |
| `ocr.detect(img, options?)` | — | 识别图像包含的所有文本, 返回 [OcrResult](dataTypes#ocrresult) 数组. |
| `ocr.detect(img, region)` | — | 识别指定路径对应图像在指定区域内包含的所有文本, 返回 [OcrResult](dataTypes#ocrresult) 数组. |
| `ocr.detect(imgPath, options?)` | — | 识别指定路径对应图像包含的所有文本, 返回 [OcrResult](dataTypes#ocrresult) 数组. |
| `ocr.detect(imgPath, region)` | — | 识别指定路径对应图像在指定区域内包含的所有文本, 返回 [OcrResult](dataTypes#ocrresult) 数组. |
| `ocr.rapid(input?, optionsOrRegion?)` | — | 固定使用 Rapid OCR 引擎识别文本, 不受 [ocr.mode](#p-mode) 影响. 参数重载与 [ocr.recognizeText](#m-recognizetext) 相同. |
| `rapid.recognizeText(input?, optionsOrRegion?)` | — | [ocr.rapid](#m-rapid) 的显式方法形式. |
| `rapid.detect(input?, optionsOrRegion?)` | — | 固定使用 Rapid OCR 引擎并返回完整识别结果. 参数重载与 [ocr.detect](#m-detect) 相同. |
| `ocr.tap(mode)` | — | 设置当前脚本的 OCR 引擎选择方式, 与 [ocr.mode](#p-mode) 的 setter 完全等价. 必须传入一个参数; `tap(undefined)` 恢复自动选择, 不带参数的 `tap()` 会抛出异常. |
| `ocr.summary()` | 6.4.0 | 获取 AutoJs6 OCR 功能的摘要. |
| `ocr.toString()` | 6.7.0 | 返回与 `ocr.summary()` 相同的文本. |

### OpenCC — [原文](https://docs.autojs6.com/#/opencc)

| API | 版本 | 说明 |
| --- | --- | --- |
| `opencc.opencc(s, type)` | 6.5.0 | 将字符串转换为目标类型. |
| `opencc.convert(s, type)` | 6.5.0 | 将字符串转换为目标类型. |
| `opencc.s2t(s)` | 6.5.0 | 字符串转换, 从简体到繁体. |
| `opencc.s2hk(s)` | 6.5.0 | 字符串转换, 从简体到香港繁体 (香港小学学习字词表标准). |
| `opencc.s2tw(s)` | 6.5.0 | 字符串转换, 从简体到台湾正体. |
| `opencc.s2twp(s)` | 6.6.0 | 将简体中文转换为台湾正体并转换常用词汇. |
| `opencc.s2twi(s)` | 6.5.0 | 字符串转换, 从简体到繁体 (台湾正体标准) \[惯用词\]. |
| `opencc.s2jp(s)` | 6.5.0 | 字符串转换, 从简体到日本汉字. |
| `opencc.t2s(s)` | 6.5.0 | 字符串转换, 从繁体到简体. |
| `opencc.t2hk(s)` | 6.5.0 | 字符串转换, 从繁体到香港繁体 (香港小学学习字词表标准). |
| `opencc.t2tw(s)` | 6.5.0 | 字符串转换, 从繁体到台湾正体. |
| `opencc.t2twi(s)` | 6.5.0 | 字符串转换, 从繁体到台湾正体 \[惯用词\]. |
| `opencc.t2jp(s)` | 6.5.0 | 字符串转换, 从繁体到日本汉字. |
| `opencc.hk2s(s)` | 6.5.0 | 字符串转换, 从香港繁体 (香港小学学习字词表标准) 到简体. |
| `opencc.hk2t(s)` | 6.5.0 | 字符串转换, 从香港繁体 (香港小学学习字词表标准) 到繁体. |
| `opencc.hk2tw(s)` | 6.5.0 | 字符串转换, 从香港繁体 (香港小学学习字词表标准) 到台湾正体. |
| `opencc.hk2twi(s)` | 6.5.0 | 字符串转换, 从香港繁体 (香港小学学习字词表标准) 到台湾正体 \[惯用词\]. |
| `opencc.hk2jp(s)` | 6.5.0 | 字符串转换, 从香港繁体 (香港小学学习字词表标准) 到日本汉字. |
| `opencc.tw2s(s)` | 6.5.0 | 字符串转换, 从台湾正体到简体. |
| `opencc.tw2sp(s)` | 6.6.0 | 将台湾正体转换为简体中文并转换常用词汇. |
| `opencc.tw2t(s)` | 6.5.0 | 字符串转换, 从台湾正体到繁体. |
| `opencc.tw2hk(s)` | 6.5.0 | 字符串转换, 从台湾正体到香港繁体 (香港小学学习字词表标准). |
| `opencc.tw2twi(s)` | 6.5.0 | 字符串转换, 从台湾正体到台湾正体 \[惯用词\]. |
| `opencc.tw2jp(s)` | 6.5.0 | 字符串转换, 从台湾正体到日本汉字. |
| `opencc.twi2s(s)` | 6.5.0 | 字符串转换, 从繁体 (台湾正体标准) \[惯用词\] 到简体. |
| `opencc.twi2t(s)` | 6.5.0 | 字符串转换, 从台湾正体 \[惯用词\] 到繁体. |
| `opencc.twi2hk(s)` | 6.5.0 | 字符串转换, 从台湾正体 \[惯用词\] 到香港繁体 (香港小学学习字词表标准). |
| `opencc.twi2tw(s)` | 6.5.0 | 字符串转换, 从台湾正体 \[惯用词\] 到台湾正体. |
| `opencc.twi2jp(s)` | 6.5.0 | 字符串转换, 从台湾正体 \[惯用词\] 到日本汉字. |
| `opencc.jp2s(s)` | 6.5.0 | 字符串转换, 从日本汉字到简体. |
| `opencc.jp2t(s)` | 6.5.0 | 字符串转换, 从日本汉字到繁体. |
| `opencc.jp2hk(s)` | 6.5.0 | 字符串转换, 从日本汉字到香港繁体 (香港小学学习字词表标准). |
| `opencc.jp2tw(s)` | 6.5.0 | 字符串转换, 从日本汉字到台湾正体. |
| `opencc.jp2twi(s)` | 6.5.0 | 字符串转换, 从日本汉字到台湾正体 \[惯用词\]. |

### 文本间距 (Pangu) — [原文](https://docs.autojs6.com/#/pangu)

| API | 版本 | 说明 |
| --- | --- | --- |
| `pangu.spaceText(text)` | 6.8.0 | 同步处理文本并返回结果, 不修改原字符串. 空字符串返回空字符串. |
| `pangu.hasProperSpacing(text)` | 6.8.0 | 结果等同于 `pangu.spaceText(text) === text`. 此检查使用与 `spaceText` 相同的规则, 不修改输入. |

### 拼音 (Pinyin) — [原文](https://docs.autojs6.com/#/pinyin)

| API | 版本 | 说明 |
| --- | --- | --- |
| `pinyin.pinyin(hans, options?)` | — | 将文本转换为拼音. |
| `pinyin.convert(hans, options?)` | — | 将文本转换为拼音. |
| `pinyin.simple(str, enableNumericTone?, enableSegment?)` | — | 使用简化选项将文本转换为拼音字符串. |
| `pinyin.compare(hanA, hanB)` | 6.8.0 | 按拼音比较两段文本, 可直接作为 `Array.prototype.sort` 的比较函数. |
| `pinyin.compact(matrix)` | 6.8.0 | 展开二维候选数组的全部组合. 此顶层方法与 [PinyinResult#compact](#m-pinyinresult-compact) 使用相同实现; 参数必须是严格的二维数组. |
| `pinyin.fromCodePoint(codePoint)` | — | 查询单个 Unicode 代码点对应的拼音记录. |
| `pinyin.fromPhrase(phrase)` | — | 查询插件词组数据中的拼音结果. |

### Pinyin4j — [原文](https://docs.autojs6.com/#/pinyin4j)

| API | 版本 | 说明 |
| --- | --- | --- |
| `pinyin4j.pinyin4j(source, options?)` | — | 将文本转换为拼音字符串. |
| `pinyin4j.of(source, options?)` | — | 将文本转换为拼音字符串. |
| `pinyin4j.as(source)` | — | 将数字声调转换为带声调字符. |
| `pinyin4j.readings(source, options?)` | — | 查询每个 Unicode 码点的全部候选读音. 返回矩阵的每个内层数组对应源文本中的一个 Unicode 码点, 因而代理对表示的补充平面字符也只占一行. 汉字以外的字符通常原样保留为单候选行. |

### 插件 (Plugins) — [原文](https://docs.autojs6.com/#/plugins)

| API | 版本 | 说明 |
| --- | --- | --- |
| `plugins.load(appPluginPackageName)` | — | 加载 [应用插件](#应用插件). |
| `plugins.load(projectPluginName)` | — | 加载 [项目插件](#项目插件). |
| `extend(...moduleNames)` | 6.2.0 | 加载指定的 [内置扩展插件](#内置扩展插件). |
| `extend.exclude(...moduleNames)` | 6.8.0 | 将模块加入排除集合. 排除集合会影响后续 [plugins.extendAll](#m-extendall) 和 [plugins.extendAllBut](#m-extendallbut), 但不阻止通过 [plugins.extend](#m-extend) 显式加载模块. |
| `extend.registerModule(module)` | 6.8.0 | 向当前脚本的扩展注册表添加自定义模块. 对象的每个属性值必须提供可调用的 `extendJsBuildInObjects()` 方法. 注册不会立即加载模块, 可在注册后使用 [plugins.extend](#m-extend) 或 [plugins.extendAll](#m-extendall) 加载. |
| `plugins.extendAll()` | 6.2.0 | 加载所有的 [内置扩展插件](#内置扩展插件). |
| `extendAllBut(...moduleNames)` | 6.2.0 | 加载所有的 [内置扩展插件](#内置扩展插件), 但排除指定的插件. |

### 电源管理 (PowerManager) — [原文](https://docs.autojs6.com/#/powerManager)

| API | 版本 | 说明 |
| --- | --- | --- |
| `powerManager.isIgnoringBatteryOptimizations(packageName?)` | 6.8.0 | 查询指定应用是否已被系统加入电池优化豁免列表. |
| `powerManager.requestIgnoreBatteryOptimizations(forceRequest?, packageName?)` | 6.8.0 | 打开系统电池优化豁免请求页面. |

### 二维码 (QR Code) — [原文](https://docs.autojs6.com/#/qrcode)

| API | 版本 | 说明 |
| --- | --- | --- |
| `qrcode.qrcode(options?, isAll?)` | — | 识别当前屏幕. `isAll` 为 `false` 时返回首个结果的原始文本, 没有结果或该值为 `null` 时返回 `null`; 为 `true` 时返回全部非空原始文本, 未识别到文本时返回空数组. |
| `qrcode.qrcode(isAll)` | — | 使用默认选项识别当前屏幕. |
| `qrcode.qrcode(image, options?, isAll?)` | — | 识别指定图片. 末尾的 `isAll` 会覆盖 `options.isAll`. |
| `qrcode.qrcode(image, isAll)` | — | 使用默认选项识别指定图片. |
| `qrcode.detect(options?, isAll?)` | — | 识别当前屏幕. `isAll` 为 `false` 时返回首个结果, 没有结果时返回 `null`; 为 `true` 时返回全部结果数组. |
| `qrcode.detect(isAll)` | — | 使用默认选项识别当前屏幕. |
| `qrcode.detect(image, options?, isAll?)` | — | 识别指定图片. 末尾的 `isAll` 会覆盖 `options.isAll`. |
| `qrcode.detect(image, isAll)` | — | 使用默认选项识别指定图片. |
| `qrcode.detectAll(options?)` | — | 识别当前屏幕并始终返回数组. 没有结果时返回空数组. |
| `qrcode.detectAll(image, options?)` | — | 识别指定图片并始终返回数组. |
| `qrcode.recognizeText(options?, isAll?)` | — | 识别当前屏幕并返回结果的原始文本. `isAll` 为 `false` 时, 没有结果或首个结果的原始文本为 `null` 均返回 `null`; 为 `true` 时会过滤原始文本为 `null` 的结果. |
| `qrcode.recognizeText(isAll)` | — | 使用默认选项识别当前屏幕. |
| `qrcode.recognizeText(image, options?, isAll?)` | — | 识别指定图片并返回结果的原始文本. 末尾的 `isAll` 会覆盖 `options.isAll`. |
| `qrcode.recognizeText(image, isAll)` | — | 使用默认选项识别指定图片. |
| `qrcode.recognizeTexts(options?)` | — | 识别当前屏幕并始终返回数组. 原始文本为 `null` 的结果会被过滤. |
| `qrcode.recognizeTexts(image, options?)` | — | 识别指定图片并始终返回数组. |

### 记录器 (Recorder) — [原文](https://docs.autojs6.com/#/recorder)

| API | 版本 | 说明 |
| --- | --- | --- |
| `recorder.recorder()` | — | 访问匿名记录的快捷方式. |
| `recorder.recorder(key)` | — | 访问命名记录的快捷方式. |
| `recorder.recorder(key, timestamp)` | — | 访问命名记录. |
| `recorder.recorder(func)` | — | 同步调用一次 `func`, 并返回本次调用经过的时间. |
| `recorder.recorder(func, thisType)` | — | 使用指定 `this` 值同步调用一次 `func`, 并返回本次调用经过的时间. |
| `recorder.save()` | — | 将当前时间戳压入匿名记录栈. |
| `recorder.save(key)` | — | 以 `key` 保存当前时间戳并返回该时间戳. |
| `recorder.save(key, timestamp)` | — | 以 `key` 保存指定时间戳并返回保存结果. |
| `recorder.load()` | — | 弹出最近的匿名记录, 返回当前时间戳与记录时间戳之差. |
| `recorder.load(key)` | — | 返回当前时间戳与 `key` 对应时间戳之差. |
| `recorder.load(key, timestamp)` | — | 返回 `timestamp` 与 `key` 对应时间戳之差. |
| `recorder.isLessThan(key, compare)` | — | 计算当前时间戳与记录时间戳之差, 并执行严格小于比较. |
| `recorder.isGreaterThan(key, compare)` | — | 计算当前时间戳与记录时间戳之差, 并执行严格大于比较. |
| `recorder.has(key)` | 6.0.3 | 检查命名记录 `key` 是否存在. |
| `recorder.remove(key)` | 6.0.3 | 删除命名记录 `key`. |
| `recorder.clear()` | 6.0.3 | 删除当前记录器中的全部命名记录和匿名记录. |

### 传感器 (Sensors) — [原文](https://docs.autojs6.com/#/sensors)

| API | 版本 | 说明 |
| --- | --- | --- |
| `sensors.getSensor(sensorName)` | — | 查询名称对应的系统默认传感器, 但不注册监听器. |
| `sensors.register(sensorName)` | — | 使用 [delay.normal](#p-delaynormal) 注册传感器监听器. |
| `sensors.register(sensorName, delay)` | — | 注册指定传感器, 并返回用于接收事件和注销监听的发射器. |
| `sensors.unregister(emitter)` | — | 注销一个传感器监听器. `emitter` 为 `null` 时不执行操作. |
| `sensors.unregisterAll()` | — | 注销当前 `sensors` 模块创建的全部传感器监听器. |

### 应用设置 (Settings) — [原文](https://docs.autojs6.com/#/settings)

| API | 版本 | 说明 |
| --- | --- | --- |
| `settings.isEnabled(key)` | 6.8.0 | 读取指定布尔型偏好设置. |
| `settings.setEnabled(key, enabled)` | 6.8.0 | 写入指定布尔型偏好设置. |

### Shell — [原文](https://docs.autojs6.com/#/shell)

| API | 版本 | 说明 |
| --- | --- | --- |
| `shell.shell(command, withRoot?)` | — | 命令执行结果 |
| `shell.shell(command, arguments, withRoot?)` | — | 同步执行命令. 命令数组会先以换行符连接. 命令开头的 `adb shell` 和相邻空白会被忽略, 因此以下调用等效: |
| `shell.execCommand(command, withRoot?)` | — | 命令执行结果 |
| `shell.execCommand(command, arguments, withRoot?)` | — | 与直接调用 `shell(...)` 等效. |
| `shell.getCommand(command, withRoot?)` | — | 构造后的命令 |
| `shell.getCommand(command, arguments, withRoot?)` | — | 仅构造命令字符串, 不执行命令. Root 启用时, 返回值以 `su` 和换行符开头. |
| `shell.fromIntent(intent)` | — | 将 Intent 转换为 `-n`, `-a`, `-c`, `-e`, `-f`, `-t` 和 `-d` 等参数组成的字符串. 返回值不包含 `am`, `start` 或其他命令前缀. |
| `shell.kill(app)` | 6.6.0 | 解析应用标识并使用 Root 执行 `am force-stop`. Root 不可用, 应用标识无法解析或命令退出码非 `0` 时返回 `false`. |
| `shell.currentPackage()` | 6.6.1 | 使用 Root 读取 `dumpsys activity activities` 的当前 resumed activity, 并返回组件中 `/` 之前的部分. |
| `shell.currentActivity()` | 6.6.1 | 使用 Root 读取当前 resumed activity. 组件使用完整类名时返回 `/` 之后的类名. 组件使用以 `.` 开头的相对类名时, 当前实现返回完整的 `package/activity` 组件字符串. |
| `shell.currentComponent()` | 6.6.1 | 使用 Root 读取当前 resumed activity. 结果通常采用 `package/activity` 形式. |
| `Shell()` | — | 创建普通权限 Shell, 相当于 `new Shell(false)`. |
| `Shell(withRoot)` | — | Shell 实例 |
| `Shell(context)` | — | 使用指定 Context 创建普通权限 Shell. |
| `Shell(context, withRoot)` | — | Shell 会在 Android 主线程异步创建终端会话. 首次执行命令时可能等待初始化完成. 初始化 `su` 失败时会抛出相应异常. 在 Android UI 主线程中创建实例后, 应等待 `onInitialized()` 再调用命令方法. |

### Shizuku — [原文](https://docs.autojs6.com/#/shizuku)

| API | 版本 | 说明 |
| --- | --- | --- |
| `shizuku.shizuku(command, arguments?, withRoot?)` | — | 使用 Shizuku 同步执行命令. |
| `shizuku.shizuku(commandList, arguments?, withRoot?)` | — | 使用 Shizuku 一次性执行多行命令. 数组元素以换行符连接后再拼接 `arguments`. |
| `shizuku.execCommand(command, arguments?, withRoot?)` | 6.6.0 | 与直接调用 `shizuku(...)` 等价. |
| `shizuku.getCommand(command, arguments?, withRoot?)` | 6.6.0 | 仅构造 Shizuku 将执行的命令字符串, 不执行命令. `withRoot` 以及 `arguments` 中的 `root` 不改变返回结果. |
| `shizuku.kill(app)` | 6.6.1 | 使用 `am force-stop` 停止应用. Shizuku 不可操作, 无法解析包名或命令退出码非 `0` 时返回 `false`. |
| `shizuku.currentPackage()` | 6.6.1 | Shizuku 不可操作或查询失败时返回空字符串. |
| `shizuku.currentActivity()` | 6.6.1 | Shizuku 不可操作或查询失败时返回空字符串. |
| `shizuku.currentComponent()` | 6.6.1 | 结果通常采用 `package/class` 形式. Shizuku 不可操作或查询失败时返回空字符串. |

### SQLite — [原文](https://docs.autojs6.com/#/sqlite)

| API | 版本 | 说明 |
| --- | --- | --- |
| `sqlite.sqlite(databaseFilePath, options?, callback?)` | — | 打开或创建数据库, 并返回 Database 对象. 相对路径按当前脚本工作目录解析. 空路径会使调用抛出异常. |
| `sqlite.open(databaseFilePath, options?, callback?)` | — | 功能与 [sqlite](#sqlite-databasefilepath-options-callback) 相同. |
| `sqlite.onCorruption(database)` | — | Android 检测到数据库损坏时调用. |
| `sqlite.onCreate(database)` | — | 首次创建数据库时调用. 通常在此创建表, 索引和初始数据. |
| `sqlite.onOpen(database)` | — | 每次成功打开数据库后调用. |
| `sqlite.onUpgrade(database, oldVersion, newVersion)` | — | 请求版本高于现有数据库版本时调用. 此回调在 Android 管理的事务内执行, 抛出异常会回滚升级. |
| `sqlite.execSQL(sql)` | — | — |
| `sqlite.execSQL(sql, bindArgs)` | — | 执行不返回结果集的 SQL, 包括 `CREATE TABLE`, `ALTER TABLE`, `DROP TABLE`, `INSERT`, `UPDATE` 和 `DELETE`. |
| `sqlite.compileStatement(sql)` | — | 编译可重复绑定和执行的 SQL 语句. SQLiteStatement 使用完毕后必须调用 `close()`. |
| `sqlite.insert(table, values)` | — | 新行 ID, 失败时为 `-1` |
| `sqlite.insert(table, nullColumnHack, values)` | — | 插入一行. 第一个重载把 `nullColumnHack` 设为 `null`. |
| `sqlite.insertOrThrow(table, nullColumnHack, values)` | — | 插入一行. 与 [insert](#m-database-insert) 不同, 数据库错误会抛出异常. |
| `sqlite.insertWithOnConflict(table, nullColumnHack, values, conflictAlgorithm)` | — | 使用指定冲突算法插入一行. |
| `sqlite.replace(table, nullColumnHack, values)` | — | 使用 `CONFLICT_REPLACE` 语义写入一行. |
| `sqlite.replaceOrThrow(table, nullColumnHack, values)` | — | 使用 `CONFLICT_REPLACE` 语义写入一行. 数据库错误会抛出异常. |
| `sqlite.update(table, values, whereClause, whereArgs)` | — | 更新符合条件的行. `whereClause` 为 `null` 时更新整张表. |
| `sqlite.updateWithOnConflict(table, values, whereClause, whereArgs, conflictAlgorithm)` | — | 使用指定冲突算法更新符合条件的行. |
| `sqlite.delete(table, whereClause, whereArgs)` | — | 删除符合条件的行. `whereClause` 为 `null` 时删除整张表中的全部行. |
| `sqlite.query(table, columns, selection, selectionArgs, groupBy, having, orderBy)` | — | 位于第一行之前的查询游标 |
| `sqlite.query(table, columns, selection, selectionArgs, groupBy, having, orderBy, limit)` | — | 位于第一行之前的查询游标 |
| `sqlite.query(distinct, table, columns, selection, selectionArgs, groupBy, having, orderBy, limit)` | — | 位于第一行之前的查询游标 |
| `sqlite.query(distinct, table, columns, selection, selectionArgs, groupBy, having, orderBy, limit, cancellationSignal)` | — | 构造并执行表查询. `selectionArgs` 依次绑定 `selection` 中的 `?` 占位符. 取消查询时, 带 `cancellationSignal` 的重载会抛出 Android OperationCanceledException. |
| `sqlite.queryWithFactory(cursorFactory, distinct, table, columns, selection, selectionArgs, groupBy, having, orderBy, limit)` | — | 位于第一行之前的查询游标 |
| `sqlite.queryWithFactory(cursorFactory, distinct, table, columns, selection, selectionArgs, groupBy, having, orderBy, limit, cancellationSignal)` | — | 功能与 [Database#query](#m-database-query) 相同, 但允许指定 Android CursorFactory. |
| `sqlite.rawQuery(sql, selectionArgs)` | — | 位于第一行之前的查询游标 |
| `sqlite.rawQuery(sql, selectionArgs, cancellationSignal)` | — | 执行原生查询. `selectionArgs` 按顺序绑定 SQL 中的 `?` 占位符, 并作为字符串传递. 返回的 Cursor 必须由调用方关闭. |
| `sqlite.rawQueryWithFactory(cursorFactory, sql, selectionArgs, editTable)` | — | 位于第一行之前的查询游标 |
| `sqlite.rawQueryWithFactory(cursorFactory, sql, selectionArgs, editTable, cancellationSignal)` | — | 功能与 [Database#rawQuery](#m-database-rawquery) 相同, 但允许指定 Android CursorFactory 和可编辑表名. |
| `sqlite.transaction(callback)` | — | 事务事件发射器 |
| `sqlite.transaction(callback, exclusive)` | — | 同步执行事务. 第一个重载的 `exclusive` 默认为 `true`. 回调正常返回时自动将事务标记为成功并提交. 回调抛出 Exception 时捕获异常并回滚, 异常通过 `error` 事件传递, 不由此方法重新抛出. |
| `sqlite.beginTransaction()` | — | 开始独占事务. 手动事务必须与 [setTransactionSuccessful](#m-database-settransactionsuccessful) 和 [endTransaction](#m-database-endtransaction) 配对. 一般业务代码优先使用 [transaction](#m-database-transaction). |
| `sqlite.beginTransactionNonExclusive()` | — | 开始非独占事务. 在 WAL 模式下, 读取可与事务中的写入并发. |
| `sqlite.beginTransactionWithListener(listener)` | — | 使用监听器开始独占事务. |
| `sqlite.beginTransactionWithListenerNonExclusive(listener)` | — | 使用监听器开始非独占事务. |
| `sqlite.setTransactionSuccessful()` | — | 将当前手动事务标记为成功. 随后调用 [endTransaction](#m-database-endtransaction) 时提交事务. 未标记成功时结束事务会回滚. |
| `sqlite.endTransaction()` | — | 结束当前手动事务, 并根据成功标记提交或回滚. |
| `sqlite.inTransaction()` | — | 当前线程是否处于事务中 |
| `sqlite.yieldIfContendedSafely()` | — | 是否让出并重新开始事务 |
| `sqlite.yieldIfContendedSafely(sleepAfterYieldDelay)` | — | 当前事务存在竞争时, 暂时结束并重新开始事务. 仅应在事务内部调用. |
| `sqlite.close()` | — | 关闭底层 SQLiteDatabase, 并从脚本资源管理器中移除当前 Database. |
| `sqlite.enableWriteAheadLogging()` | — | 启用 write-ahead logging, 允许多个连接上的读取与写入并发. 只读数据库, 内存数据库或附加了其他数据库时可能无法启用并返回 `false`. |
| `sqlite.disableWriteAheadLogging()` | — | 禁用 write-ahead logging. |
| `sqlite.isWriteAheadLoggingEnabled()` | — | 是否已启用 WAL |
| `sqlite.getPath()` | — | 返回底层 SQLiteDatabase 使用的路径. `sqlite` 接受相对路径时, 此处通常返回解析后的路径. |
| `sqlite.isOpen()` | — | 数据库是否仍处于打开状态 |
| `sqlite.isReadOnly()` | — | 此结果反映实际打开状态, 比 SQLiteOpenOptions 的 `readOnly` 请求值更适合判断句柄是否只读. |
| `sqlite.getVersion()` | — | 读取 SQLite `user_version`. |
| `sqlite.setVersion(version)` | — | 直接设置 SQLite `user_version`. 此方法不会执行 [DatabaseCallback#onUpgrade](#m-databasecallback-onupgrade), 也不会自动迁移表结构. 常规版本迁移应通过重新打开数据库时的 `version` 选项和 `onUpgrade` 回调完成. |
| `sqlite.needUpgrade(newVersion)` | — | `newVersion` 是否大于当前版本 |
| `sqlite.getMaximumSize()` | — | 数据库最大容量, 单位为字节 |
| `sqlite.setMaximumSize(numBytes)` | — | 最大容量不能小于数据库当前大小. 返回值按数据库页大小向上取整, 因此可能大于 `numBytes`. |
| `sqlite.getPageSize()` | — | 数据库页大小, 单位为字节 |
| `sqlite.setPageSize(numBytes)` | — | 设置 SQLite 页大小. 值必须是 2 的幂, 且此设置应在新数据库写入任何数据之前完成. |
| `sqlite.getAttachedDbs()` | — | 返回 `android.util.Pair<string, string>` 列表. 每一项的 `first` 是数据库名称, `second` 是完整文件路径, 列表包含主数据库 `main`. |
| `sqlite.isDatabaseIntegrityOk()` | — | 运行 SQLite `integrity_check`. 对大型数据库可能耗时较长. |
| `sqlite.isDbLockedByCurrentThread()` | — | 名称中的 `Locked` 是历史命名. 当前 Android 实现检查的是当前线程是否持有活动连接, 不表示传统意义上的数据库文件锁. |
| `sqlite.setForeignKeyConstraintsEnabled(enable)` | — | 为当前打开会话启用或禁用 SQLite 外键约束. 不得在事务进行中调用. 如需每次打开都启用, 可在 `DatabaseCallback.onOpen` 中调用. |
| `sqlite.setLocale(locale)` | — | 设置数据库本地化排序使用的区域. 只读数据库或使用 `NO_LOCALIZED_COLLATORS` 标志时不生效. |
| `sqlite.setMaxSqlCacheSize(cacheSize)` | — | 调整底层数据库的预编译语句缓存容量. 超出 Android 允许范围或违反底层容量约束时抛出异常. |
| `sqlite.validateSql(sql, cancellationSignal)` | — | 通过编译验证 SELECT 语句. SQL 无效时抛出 SQLiteException, 操作被取消时抛出 OperationCanceledException. |
| `sqlite.acquireReference()` | — | 增加底层 SQLiteDatabase 的引用计数. 这是高级资源管理接口, 每次成功调用都必须与 [releaseReference](#m-database-releasereference) 配对. |
| `sqlite.releaseReference()` | — | 减少底层 SQLiteDatabase 的引用计数. 引用计数归零时会释放底层资源. 不要在未成功调用 [acquireReference](#m-database-acquirereference) 时单独调用. |
| `sqlite.getTypeAdapter()` | — | 返回 AutoJs6 为 SQLite 模块创建的 JavaScript 类型适配器. 常规脚本无需直接调用. |
| `sqlite.toContentValues(value)` | — | Android ContentValues |
| `sqlite.wrapCursor(cursor)` | — | AutoJs6 Cursor 包装对象 |
| `sqlite.succeed()` | — | 调用 [Database#setTransactionSuccessful](#m-database-settransactionsuccessful). `Database#transaction` 在回调正常返回后自动调用此方法, 回调中通常不应再次调用. |
| `sqlite.end()` | — | 调用 [Database#endTransaction](#m-database-endtransaction). `Database#transaction` 在回调结束后自动调用此方法, 回调中通常不应再次调用. |
| `sqlite.get(index)` | — | 按底层字段类型读取当前行的指定列. 类型映射与 [SQLiteRow](#sqliterow) 相同. 游标不位于有效行, 索引越界或底层字段类型未知时抛出异常. |
| `sqlite.getByColumn(column)` | — | 先通过 `getColumnIndexOrThrow(column)` 查找列索引, 再调用 [get](#m-cursor-get). 列不存在时抛出 IllegalArgumentException. |
| `sqlite.all(close = true)` | — | 从当前位置开始反复调用 `moveToNext()`, 将随后到达的每一行转换为 SQLiteRow. 新查询的游标位于第一行之前, 因而直接调用 `all()` 会读取全部行. |
| `sqlite.pick()` | — | 将游标当前行转换为 SQLiteRow, 不移动也不关闭游标. 游标必须已位于有效行. |
| `sqlite.next()` | — | 先调用 `moveToNext()`. 移动成功时返回该行, 否则返回 `null`. 此方法不关闭游标. |
| `sqlite.single()` | — | 调用 [next](#m-cursor-next) 读取一行, 然后关闭游标. 此方法不检查结果集是否只有一行, 其含义是 "读取下一行并关闭". |
| `sqlite.isClosed()` | — | 游标是否已关闭 |

### 标准化 (Standardization) — [原文](https://docs.autojs6.com/#/s13n)

| API | 版本 | 说明 |
| --- | --- | --- |
| `s13n.color(o)` | — | 将颜色参数 `o` 标准化为 [ColorInt](dataTypes#colorint). |
| `s13n.point(source, y?)` | 6.6.0 | 将坐标参数标准化为 `android.graphics.Point`. |
| `s13n.time(source, fromUnit?, toUnit?)` | 6.6.0 | 标准化时间值并按需进行单位换算. 源单位和目标单位默认均为毫秒. |
| `s13n.bytes(source, fromUnit?)` | 6.7.0 | 将字节单位参数标准化为 Byte 数. 单位解析规则与 [cvt.bytes](cvt#m-bytes) 的宽松模式一致. |
| `s13n.throwable(o)` | — | 将可抛异常参数 `o` 标准化为 [java.lang.Throwable](exceptions#java) 类型. |

### 存储 (Storages) — [原文](https://docs.autojs6.com/#/storages)

| API | 版本 | 说明 |
| --- | --- | --- |
| `storages.create(name)` | — | 以 `name` 参数为名称创建一个本地存储, 并返回 [Storage](storageType) 实例: |
| `storages.remove(name)` | — | 清除名为 `name` 的本地存储包含的全部数据. |
| `storages.removeSync(name)` | 6.6.0 | 同步清除指定本地存储的全部数据, 并等待数据写入存储文件. |
| `storages.all()` | 6.6.0 | 返回所有当前包含数据的本地存储. |
| `storages.names()` | 6.6.0 | 返回所有当前包含数据的本地存储名称. |

### 系统属性 (Sysprops) — [原文](https://docs.autojs6.com/#/sysprops)

| API | 版本 | 说明 |
| --- | --- | --- |
| `sysprops.sysprops(propName, defaultValue?)` | — | 读取字符串形式的系统属性. |
| `sysprops.get(propName, defaultValue?)` | — | 读取字符串形式的系统属性. |
| `sysprops.getInt(propName, defaultValue?)` | — | 读取整数形式的系统属性. |
| `sysprops.getBoolean(propName, defaultValue?)` | — | 读取布尔形式的系统属性. |
| `sysprops.getAll(keyFilter?, valueFilter?)` | — | 读取全部系统属性, 并按需过滤结果. |
| `sysprops.getAll(options)` | — | 使用对象形式指定过滤器. |

### 任务 (Tasks) — [原文](https://docs.autojs6.com/#/tasks)

| API | 版本 | 说明 |
| --- | --- | --- |
| `tasks.addTask(task)` | — | 将任务同步写入数据库. 参数为 `null` 时不执行操作并返回 `null`. |
| `tasks.addTimedTask(options)` | 6.8.0 | 按 `options.repeatMode` 创建定时任务. `repeatMode` 兼容字段名为 `repeat`, `type` 和 `mode`, 支持: |
| `tasks.addDailyTask(options)` | — | 创建每日任务. `time` 和 `date` 均省略时使用当前时间. |
| `tasks.addWeeklyTask(options)` | — | 创建每周任务. `daysOfWeek` 是星期数组, 数字使用 `0` 或 `7` 表示星期日, `1` 至 `6` 表示星期一至星期六. 也可使用英文全称, 英文缩写或 `一` 至 `日`. 默认使用今天. |
| `tasks.addMonthlyTask(options)` | 6.8.0 | 创建每月任务. `dayOfMonth` 取值为 `1` 至 `31`, 兼容字段名为 `day`. 也可通过 `date` 提供日期, 默认使用今天的日号. 当目标月份没有指定日号时, 使用该月最后一天. |
| `tasks.addYearlyTask(options)` | 6.8.0 | 创建每年任务. `monthOfYear` 取值为 `1` 至 `12`, `dayOfMonth` 取值为 `1` 至 `31`; 兼容字段名分别为 `month` 和 `day`. 也可通过 `date` 同时提供月份和日号, 默认使用今天. 当目标年月没有指定日号时, 使用该月最后一天. |
| `tasks.addDisposableTask(options)` | — | 创建一次性任务. `time` 和 `date` 均省略时使用当前时刻. 也可使用 [倒计时持续时间](#倒计时持续时间) 字段指定相对时长. |
| `tasks.addCountdownTask(options)` | 6.8.0 | 创建从当前时刻开始计时的一次性倒计时任务. |
| `tasks.addIntentTask(options)` | — | 创建由匹配的广播 Intent 触发的任务. `isLocal` 兼容字段名为 `local`. AutoJs6 启动广播默认使用本地广播, 但显式指定的 `isLocal` 仍具有最终优先级. |
| `tasks.addBroadcastIntentTask(options)` | 6.8.0 | [tasks.addIntentTask](#m-addintenttask) 的兼容别名. |
| `tasks.getTimedTask(id)` | — | 匹配的任务 |
| `tasks.getIntentTask(id)` | — | 匹配的任务 |
| `tasks.removeTask(task)` | — | 同步删除任务. 参数为 `null` 时不执行操作并返回 `null`. |
| `tasks.removeTimedTask(id, options?)` | — | 被删除的任务, `null`, 回调结果或线程对象 |
| `tasks.removeIntentTask(id, options?)` | — | 被删除的任务, `null`, 回调结果或线程对象 |
| `tasks.updateTask(task)` | — | 同步保存任务对象上的修改. 更新 `TimedTask` 时会先清除其已调度状态, 以便调度后端重新安排任务. |
| `tasks.queryTimedTasks(options?)` | — | 查询所有定时任务. 指定 `path` 时仅返回脚本路径匹配的任务. |
| `tasks.queryIntentTasks(options?)` | — | 查询广播触发任务. 同时指定 `path` 和 `action` 时必须全部匹配. |
| `tasks.timeFlagToDays(flag)` | — | 将星期位标志转换为编号数组. 编号 `0` 至 `6` 依次表示星期日到星期六. |
| `tasks.daysToTimeFlag(days)` | — | 将星期编号数组转换为位标志. 仅编号 `0` 至 `6` 参与计算. |

### 线程 (Threads) — [原文](https://docs.autojs6.com/#/threads)

| API | 版本 | 说明 |
| --- | --- | --- |
| `threads.start(action)` | — | 创建一个 TimerThread, 将其加入当前脚本运行环境的子线程集合, 然后立即启动. |
| `threads.interrupt(thread)` | — | 当 `thread` 是仍存活的 AutoJs6 TimerThread 时调用其 |
| `threads.pool(options?)` | 6.6.0 | 创建使用 AutoJs6 TimerThread 作为工作线程的 `ThreadPoolExecutor`. 线程池任务 |
| `threads.shutDownAll()` | — | 中断所有仍登记在当前脚本运行环境中, 且由 [threads.start()](#m-start) 创建的 |
| `threads.currentThread()` | — | 返回当前执行此调用的线程. 具体对象取决于调用位置: |
| `threads.disposable()` | — | 创建一个初始值为 `null` 的 Disposable. 它提供 |
| `threads.atomic(initialValue?)` | — | 创建 `AtomicLong`. 使用其 `get()`, `set()`, `incrementAndGet()`, |
| `threads.lock()` | — | 创建使用默认构造参数的 `ReentrantLock`. 默认锁不是公平锁. 需要在 `finally` |
| `globalThis.sync(func, lock?)` | — | 返回对 `func` 的同步包装. 每次调用包装函数时, Rhino 会先进入指定对象的 Java |
| `Thread#interrupt()` | — | 请求中断线程. 对 TimerThread 调用时, AutoJs6 还会退出该线程的 Android Looper, |
| `Thread#isInterrupted()` |  |  |
