# 脚本引擎: 线程 / 引擎 / 定时器 / 事件

> **类型**: topic
**创建时间**: 2026-09-22
**来源**: [📄 threads](https://docs.autojs6.com/#/threads), [📄 engines](https://docs.autojs6.com/#/engines), [📄 timers](https://docs.autojs6.com/#/timers), [📄 events](https://docs.autojs6.com/#/events), [📄 flow](https://docs.autojs6.com/#/flow)

## 摘要

AutoJs6 允许在一个脚本内开启子线程、启动新的脚本引擎、设置定时器并监听系统事件.
使用这些能力时必须理解 **Rhino 环境的共享语义** 与 **生命周期**:
子线程共享脚本作用域和 Java 对象, 但并非线程安全; 脚本主线程会等待子线程、
线程池任务和定时器结束; 脚本退出时子线程被中断、线程池被关闭.

## 一、threads — 子线程

```js
let worker = threads.start(() => {
    log("worker: " + threads.currentThread());
});
if (worker !== undefined) {
    worker.join();
}
```

要点 (来源 [📄 threads](https://docs.autojs6.com/#/threads)):

- `threads.start(action)` 创建 `TimerThread` 并立即启动, 返回该线程;
  **脚本正在退出或启动被中断时返回 `undefined`**, 故示例中有判空.
- 线程名为当前脚本主线程名加 `(Spawn-N)` 后缀, `N` 从 `0` 递增.
- 传入的函数**不接收参数**; 未捕获异常会写入控制台并结束该线程.
- `threads.interrupt(thread)` 中断仍存活的 TimerThread, 其他对象被忽略.
- `threads.pool(options?)` (**6.6.0**) 返回由当前运行环境管理的线程池
  (`ThreadPoolExecutor`), 支持 `corePoolSize` / `maxPoolSize` / `keepAliveTime`.

### 生命周期与风险

- 脚本主线程会**等待**仍在运行的子线程、线程池任务和定时器.
- 脚本退出时, AutoJs6 会**中断**由当前运行环境创建的子线程, 并**立即关闭**其管理的线程池.
- 线程共享脚本作用域和 Java 对象, 但**普通读写、复合运算、JavaScript 容器不会
  自动成为线程安全操作**.

> ⚠️ **死锁警告**: `join()`、`blockedGet()` 以及锁等待都会阻塞调用线程.
**不应在 Android UI 线程中等待依赖 UI 线程完成的任务**, 否则可能造成界面冻结或死锁.
来源: [📄 threads](https://docs.autojs6.com/#/threads)

> 注: `"nodejs"` 模式**不提供** `threads` 模块.

## 二、engines — 脚本引擎

`execScript` / `execScriptFile` / `execAutoFile` 的 `config` 参数:

| 属性 | 默认值 | 含义 |
| --- | --- | --- |
| `workingDirectory` | `engines.myEngine().cwd()` | 工作目录 |
| `path` | 同 `workingDirectory` | 兼容别名 |
| `delay` | `0` | 首次执行前延迟 (ms) |
| `interval` | `0` | 重复执行间隔 (ms) |
| `loopTimes` | `1` | 执行次数, `0` 表示持续执行 |
| `arguments` | `{}` | 传给新引擎的参数 |

`arguments` 在新引擎中通过 `engines.myEngine().execArgv` 读取.

```js
let execution = engines.execScript('worker', `
    console.log(engines.myEngine().execArgv.message);
`, {
    arguments: { message: 'hello' },
});
console.log(execution.getId());
```

> 来源: [📄 engines](https://docs.autojs6.com/#/engines)

## 三、其他相关模块

| 模块 | 用途 | 原文 |
| --- | --- | --- |
| `timers` | 定时器 | [📄 timers](https://docs.autojs6.com/#/timers) |
| `events` | 监听手机通知 / 按键 / 触摸 | [📄 events](https://docs.autojs6.com/#/events) |
| `flow` | 6.8.0 引入的异步等待与动作链 | [📄 flow](https://docs.autojs6.com/#/flow) |
| `continuation` | 捕获和恢复 Rhino 脚本执行状态 | [📄 continuation](https://docs.autojs6.com/#/continuation) |

`flow` 与 `automator` 的工具集类似, 同时提供同步形式 (全局函数) 与
Flow 形式的接口.

## 编写脚本时的核对要点

## 关联

- 相关概念: \[\[topic-getting-started\]\], \[\[topic-ui-automation\]\]
- 参见: 《文档总目录》, 《API 速查表》

## 引用来源

- \[1\] [📄 threads](https://docs.autojs6.com/#/threads) — 线程语义、start/pool/interrupt、死锁警告
- \[2\] [📄 engines](https://docs.autojs6.com/#/engines) — 执行配置与 execScript 示例
- \[3\] [📄 timers](https://docs.autojs6.com/#/timers) — 定时器
- \[4\] [📄 events](https://docs.autojs6.com/#/events) — 事件监听
- \[5\] [📄 flow](https://docs.autojs6.com/#/flow) — Flow 异步链

## 变更记录

- 2026-09-22: 初始创建, 来源 [📄 threads](https://docs.autojs6.com/#/threads), [📄 engines](https://docs.autojs6.com/#/engines)
